<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Kevin&#39;s Notes</title>
  
  <subtitle>Quick notes</subtitle>
  <link href="https://freemankevin.uk/atom.xml" rel="self"/>
  
  <link href="https://freemankevin.uk/"/>
  <updated>2024-12-23T14:37:32.796Z</updated>
  <id>https://freemankevin.uk/</id>
  
  <author>
    <name>Freeman Kevin</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>ElasticSearch 索引的自动备份</title>
    <link href="https://freemankevin.uk/2024/12/22/backup-es/"/>
    <id>https://freemankevin.uk/2024/12/22/backup-es/</id>
    <published>2024-12-22T04:17:25.000Z</published>
    <updated>2024-12-23T14:37:32.796Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在本文中，我们将介绍如何使用 Docker Compose 部署带有用户和密码认证的 Elasticsearch 服务，并实现索引的自动备份。首先，我们将展示如何准备配置文件并启动 Elasticsearch 服务。接着，我们将讲解如何配置并验证快照仓库。最后，我们提供了一个用于自动备份 Elasticsearch 索引的脚本，并通过 Crontab 定时任务实现自动备份。本文将帮助你确保数据的安全性和可恢复性。</p><span id="more"></span><h2 id="1-部署-Elasticsearch-服务"><a href="#1-部署-Elasticsearch-服务" class="headerlink" title="1. 部署 Elasticsearch 服务"></a>1. 部署 Elasticsearch 服务</h2><p>使用 Docker Compose 部署 Elasticsearch 服务，带有用户、密码认证的单节点配置。</p><h3 id="1-1-准备文件"><a href="#1-1-准备文件" class="headerlink" title="1.1 准备文件"></a>1.1 准备文件</h3><h4 id="env-文件"><a href="#env-文件" class="headerlink" title=".env 文件"></a>.env 文件</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ELASTIC_VERSION=8.10.2</span><br><span class="line">ELASTIC_PASSWORD=yourpassword</span><br><span class="line">ES_JAVA_OPTS=-Xms4g -Xmx4g</span><br></pre></td></tr></table></figure><h4 id="es-yaml-文件"><a href="#es-yaml-文件" class="headerlink" title="es.yaml 文件"></a>es.yaml 文件</h4><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&#x27;3.8&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">elasticsearch:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">docker.elastic.co/elasticsearch/elasticsearch:$&#123;ELASTIC_VERSION&#125;</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">elasticsearch</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">discovery.type=single-node</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">ELASTIC_PASSWORD=$&#123;ELASTIC_PASSWORD&#125;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">xpack.security.enabled=true</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">path.repo=/usr/share/elasticsearch/snapshots</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">path.plugins=/usr/share/elasticsearch/plugins</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="number">9200</span><span class="string">:9200</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">es-data:/usr/share/elasticsearch/data</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">es-snapshots:/usr/share/elasticsearch/snapshots</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">es-plugins:/usr/share/elasticsearch/plugins</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">es-data:</span></span><br><span class="line">  <span class="attr">es-snapshots:</span></span><br><span class="line">  <span class="attr">es-plugins:</span></span><br></pre></td></tr></table></figure><h3 id="1-2-部署服务"><a href="#1-2-部署服务" class="headerlink" title="1.2 部署服务"></a>1.2 部署服务</h3><ol><li>启动 Elasticsearch 服务：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker-compose -f es.yaml up -d</span><br></pre></td></tr></table></figure><ol start="2"><li>验证服务是否运行成功：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -u <span class="string">&quot;elastic:<span class="variable">$&#123;ELASTIC_PASSWORD&#125;</span>&quot;</span> -X GET <span class="string">&quot;http://localhost:9200&quot;</span></span><br></pre></td></tr></table></figure><p>返回集群信息表示服务启动成功。</p><hr><h2 id="2-接口调用与验证"><a href="#2-接口调用与验证" class="headerlink" title="2. 接口调用与验证"></a>2. 接口调用与验证</h2><p>在开始备份前，需要确保以下几点：</p><ol><li>已有访问 Elasticsearch 集群的权限。</li><li>确保集群健康状态为绿色或黄色。</li></ol><h3 id="2-1-测试连接"><a href="#2-1-测试连接" class="headerlink" title="2.1 测试连接"></a>2.1 测试连接</h3><p>通过以下命令测试连接：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -u <span class="string">&quot;elastic:<span class="variable">$&#123;ELASTIC_PASSWORD&#125;</span>&quot;</span> -X GET <span class="string">&quot;http://&lt;your-es-host&gt;:9200&quot;</span></span><br></pre></td></tr></table></figure><p>如果连接成功，将返回集群的基本信息（JSON 格式）。</p><h3 id="2-2-验证快照仓库"><a href="#2-2-验证快照仓库" class="headerlink" title="2.2 验证快照仓库"></a>2.2 验证快照仓库</h3><p>在 ES 中，备份索引需要先配置一个快照仓库。</p><ol><li>创建快照仓库：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -u <span class="string">&quot;elastic:<span class="variable">$&#123;ELASTIC_PASSWORD&#125;</span>&quot;</span> -X PUT <span class="string">&quot;http://&lt;your-es-host&gt;:9200/_snapshot/backup_repo&quot;</span> -H <span class="string">&#x27;Content-Type: application/json&#x27;</span> -d<span class="string">&#x27;</span></span><br><span class="line"><span class="string">&#123;</span></span><br><span class="line"><span class="string">  &quot;type&quot;: &quot;fs&quot;,</span></span><br><span class="line"><span class="string">  &quot;settings&quot;: &#123;</span></span><br><span class="line"><span class="string">    &quot;location&quot;: &quot;/usr/share/elasticsearch/snapshots&quot;,</span></span><br><span class="line"><span class="string">    &quot;compress&quot;: true</span></span><br><span class="line"><span class="string">  &#125;</span></span><br><span class="line"><span class="string">&#125;&#x27;</span></span><br></pre></td></tr></table></figure><ol start="2"><li>验证快照仓库是否可用：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -u <span class="string">&quot;elastic:<span class="variable">$&#123;ELASTIC_PASSWORD&#125;</span>&quot;</span> -X GET <span class="string">&quot;http://&lt;your-es-host&gt;:9200/_snapshot/backup_repo&quot;</span></span><br></pre></td></tr></table></figure><p>返回结果中 <code>&quot;status&quot;: &quot;SUCCESS&quot;</code> 表示仓库配置正确。</p><hr><h2 id="3-备份脚本的实现"><a href="#3-备份脚本的实现" class="headerlink" title="3. 备份脚本的实现"></a>3. 备份脚本的实现</h2><p>以下是一个用于自动备份 Elasticsearch 索引的脚本：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置参数</span></span><br><span class="line">ES_HOST=<span class="string">&quot;http://&lt;your-es-host&gt;:9200&quot;</span></span><br><span class="line">USERNAME=<span class="string">&quot;elastic&quot;</span></span><br><span class="line">PASSWORD=<span class="string">&quot;&lt;yourpassword&gt;&quot;</span></span><br><span class="line">BACKUP_REPO=<span class="string">&quot;backup_repo&quot;</span></span><br><span class="line">BACKUP_NAME=<span class="string">&quot;snapshot_<span class="subst">$(date +%Y-%m-%d-%H-%M)</span>&quot;</span></span><br><span class="line">INDEX_NAME=<span class="string">&quot;&lt;your-index-name&gt;&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建备份</span></span><br><span class="line">curl -u <span class="string">&quot;<span class="variable">$USERNAME</span>:<span class="variable">$PASSWORD</span>&quot;</span> -X PUT <span class="string">&quot;<span class="variable">$ES_HOST</span>/_snapshot/<span class="variable">$BACKUP_REPO</span>/<span class="variable">$BACKUP_NAME</span>&quot;</span> -H <span class="string">&#x27;Content-Type: application/json&#x27;</span> -d<span class="string">&#x27;</span></span><br><span class="line"><span class="string">&#123;</span></span><br><span class="line"><span class="string">  &quot;indices&quot;: &quot;&#x27;</span><span class="variable">$INDEX_NAME</span><span class="string">&#x27;&quot;,</span></span><br><span class="line"><span class="string">  &quot;ignore_unavailable&quot;: true,</span></span><br><span class="line"><span class="string">  &quot;include_global_state&quot;: false</span></span><br><span class="line"><span class="string">&#125;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 检查备份状态</span></span><br><span class="line">curl -u <span class="string">&quot;<span class="variable">$USERNAME</span>:<span class="variable">$PASSWORD</span>&quot;</span> -X GET <span class="string">&quot;<span class="variable">$ES_HOST</span>/_snapshot/<span class="variable">$BACKUP_REPO</span>/<span class="variable">$BACKUP_NAME</span>/_status&quot;</span></span><br></pre></td></tr></table></figure><p>保存脚本为 <code>es_backup.sh</code>，并赋予可执行权限：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chmod</span> +x es_backup.sh</span><br></pre></td></tr></table></figure><hr><h2 id="4-定时任务的配置"><a href="#4-定时任务的配置" class="headerlink" title="4. 定时任务的配置"></a>4. 定时任务的配置</h2><p>通过 <code>crontab</code> 设置定时任务，实现备份脚本的自动执行。</p><ol><li>编辑 crontab：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">crontab -e</span><br></pre></td></tr></table></figure><ol start="2"><li>添加以下条目，设置每天凌晨 2 点执行备份脚本：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0 2 * * * /path/to/es_backup.sh</span><br></pre></td></tr></table></figure><ol start="3"><li>保存并退出。</li></ol><hr><h2 id="5-验证备份"><a href="#5-验证备份" class="headerlink" title="5. 验证备份"></a>5. 验证备份</h2><ol><li>查看已有的快照：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -u <span class="string">&quot;elastic:<span class="variable">$&#123;ELASTIC_PASSWORD&#125;</span>&quot;</span> -X GET <span class="string">&quot;http://&lt;your-es-host&gt;:9200/_snapshot/backup_repo/_all&quot;</span></span><br></pre></td></tr></table></figure><ol start="2"><li>恢复快照：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -u <span class="string">&quot;elastic:<span class="variable">$&#123;ELASTIC_PASSWORD&#125;</span>&quot;</span> -X POST <span class="string">&quot;http://&lt;your-es-host&gt;:9200/_snapshot/backup_repo/&lt;snapshot-name&gt;/_restore&quot;</span> -H <span class="string">&#x27;Content-Type: application/json&#x27;</span> -d<span class="string">&#x27;</span></span><br><span class="line"><span class="string">&#123;</span></span><br><span class="line"><span class="string">  &quot;indices&quot;: &quot;&lt;your-index-name&gt;&quot;,</span></span><br><span class="line"><span class="string">  &quot;ignore_unavailable&quot;: true,</span></span><br><span class="line"><span class="string">  &quot;include_global_state&quot;: false</span></span><br><span class="line"><span class="string">&#125;&#x27;</span></span><br></pre></td></tr></table></figure><hr><h2 id="6-总结"><a href="#6-总结" class="headerlink" title="6. 总结"></a>6. 总结</h2><p>通过本文档的步骤，你可以快速实现 Elasticsearch 索引的自动备份，并通过 Docker Compose 快速部署一个带用户和密码认证的 Elasticsearch 单节点服务。确保备份脚本和定时任务配置正确，以降低数据丢失的风险。在生产环境中，请根据业务需求调整备份策略和存储位置。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在本文中，我们将介绍如何使用 Docker Compose 部署带有用户和密码认证的 Elasticsearch 服务，并实现索引的自动备份。首先，我们将展示如何准备配置文件并启动 Elasticsearch 服务。接着，我们将讲解如何配置并验证快照仓库。最后，我们提供了一个用于自动备份 Elasticsearch 索引的脚本，并通过 Crontab 定时任务实现自动备份。本文将帮助你确保数据的安全性和可恢复性。&lt;/p&gt;</summary>
    
    
    
    <category term="ElasticSearch" scheme="https://freemankevin.uk/categories/ElasticSearch/"/>
    
    
    <category term="Cron" scheme="https://freemankevin.uk/tags/Cron/"/>
    
    <category term="ElasticSearch" scheme="https://freemankevin.uk/tags/ElasticSearch/"/>
    
    <category term="Backup" scheme="https://freemankevin.uk/tags/Backup/"/>
    
  </entry>
  
  <entry>
    <title>CentOS 系统下制作离线安装包</title>
    <link href="https://freemankevin.uk/2024/12/20/centos-offline-package/"/>
    <id>https://freemankevin.uk/2024/12/20/centos-offline-package/</id>
    <published>2024-12-20T04:17:25.000Z</published>
    <updated>2024-12-23T14:37:38.958Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在无法联网的环境中安装软件需要提前准备好所有必要的安装包及其依赖项。本文将指导你如何在 CentOS 7.9 系统上创建一个离线软件安装包。首先，我们会使用 yum 命令下载所需软件及其依赖项，然后创建一个本地仓库。接着，我们会将下载的目录打包并传输到目标离线机器。最后，我们会在离线机器上解压并配置本地仓库，以便顺利安装软件。通过这些步骤，你可以在没有网络连接的情况下方便地安装所需的软件。</p><span id="more"></span><h2 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h2><p>在开始之前，请确保您有一台可以联网的 CentOS 7.9 系统，用于下载软件及其依赖。</p><h2 id="1-下载软件及其依赖"><a href="#1-下载软件及其依赖" class="headerlink" title="1. 下载软件及其依赖"></a>1. 下载软件及其依赖</h2><p>您可以使用 <code>yum</code> 的 <code>--downloadonly</code> 和 <code>--downloaddir</code> 选项来下载软件包和它们的所有依赖。</p><p>例如，如果您想下载 <code>httpd</code>（Apache Web 服务器）及其所有依赖，可以执行以下命令：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install --downloadonly --downloaddir=/path/to/download/directory httpd</span><br></pre></td></tr></table></figure><p>将 <code>/path/to/download/directory</code> 替换为您想要保存下载文件的目录路径。</p><h2 id="2-创建本地仓库"><a href="#2-创建本地仓库" class="headerlink" title="2. 创建本地仓库"></a>2. 创建本地仓库</h2><p>下载所有包后，您需要在下载目录中创建一个本地仓库，这样在离线安装时 <code>yum</code> 能够识别并正确处理依赖关系。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /path/to/download/directory</span><br><span class="line"><span class="comment"># sudo rpm --import /path/to/RPM-GPG-KEY-PGDG # 如果有的话</span></span><br><span class="line">createrepo .</span><br></pre></td></tr></table></figure><p>如果 <code>createrepo</code> 命令不可用，您需要先在线安装 <code>createrepo</code> 包：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install createrepo</span><br></pre></td></tr></table></figure><p>并在同样的联网环境中运行 <code>createrepo</code> 命令。</p><h2 id="3-打包下载目录"><a href="#3-打包下载目录" class="headerlink" title="3. 打包下载目录"></a>3. 打包下载目录</h2><p>将带有软件包和本地仓库元数据的下载目录打包，以便于传输。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -czvf offline-packages.tar.gz -C /path/to/download/directory .</span><br></pre></td></tr></table></figure><h2 id="4-传输到离线机器"><a href="#4-传输到离线机器" class="headerlink" title="4. 传输到离线机器"></a>4. 传输到离线机器</h2><p>使用 USB 驱动器、光盘或其他媒体将 <code>offline-packages.tar.gz</code> 文件传输到离线 CentOS 7.9 机器上。</p><h2 id="5-离线安装软件"><a href="#5-离线安装软件" class="headerlink" title="5. 离线安装软件"></a>5. 离线安装软件</h2><p>在离线机器上，执行以下步骤来安装软件：</p><h3 id="解压软件包"><a href="#解压软件包" class="headerlink" title="解压软件包"></a>解压软件包</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -xzvf /path/to/offline-packages.tar.gz -C /path/to/local/repo</span><br></pre></td></tr></table></figure><h3 id="配置本地仓库"><a href="#配置本地仓库" class="headerlink" title="配置本地仓库"></a>配置本地仓库</h3><p>创建一个新的 YUM 仓库配置文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/yum.repos.d/local.repo</span><br></pre></td></tr></table></figure><p>添加以下内容：</p><figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="section">[local-repo]</span></span><br><span class="line"><span class="attr">name</span>=Local Repository</span><br><span class="line"><span class="attr">baseurl</span>=file:///path/to/local/repo</span><br><span class="line"><span class="attr">enabled</span>=<span class="number">1</span></span><br><span class="line"><span class="attr">gpgcheck</span>=<span class="number">0</span></span><br></pre></td></tr></table></figure><p>确保替换 <code>/path/to/local/repo</code> 为您解压缩软件包的实际目录。</p><h3 id="安装软件"><a href="#安装软件" class="headerlink" title="安装软件"></a>安装软件</h3><p>现在您可以使用 <code>yum</code> 从本地仓库安装软件，<code>yum</code> 会自动处理所有依赖关系。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 可能需要清理其他旧仓库，然后执行清理缓存，yum clean all &amp;&amp; yum makecache fast</span></span><br><span class="line">yum install --disablerepo=<span class="string">&#x27;*&#x27;</span> --enablerepo=<span class="string">&#x27;local-repo&#x27;</span> httpd</span><br></pre></td></tr></table></figure><p>用您实际下载的软件包名替换 <code>httpd</code>。</p><h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><ul><li>请确保在可联网的机器上下载软件包时的 CentOS 版本和离线机器上的版本一致，以免因版本不匹配导致依赖问题。</li><li>如果您下载了大量的包，并且依赖很复杂，建议直接下载整个 CentOS 仓库，并在离线环境中设置本地镜像。</li></ul><p>按照这些步骤，您应该能够在没有网络连接的情况下在 CentOS 7.9 系统上安装软件。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在无法联网的环境中安装软件需要提前准备好所有必要的安装包及其依赖项。本文将指导你如何在 CentOS 7.9 系统上创建一个离线软件安装包。首先，我们会使用 yum 命令下载所需软件及其依赖项，然后创建一个本地仓库。接着，我们会将下载的目录打包并传输到目标离线机器。最后，我们会在离线机器上解压并配置本地仓库，以便顺利安装软件。通过这些步骤，你可以在没有网络连接的情况下方便地安装所需的软件。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Yum" scheme="https://freemankevin.uk/tags/Yum/"/>
    
    <category term="Offline" scheme="https://freemankevin.uk/tags/Offline/"/>
    
    <category term="CentOS" scheme="https://freemankevin.uk/tags/CentOS/"/>
    
  </entry>
  
  <entry>
    <title>Debian 系统下制作离线安装包</title>
    <link href="https://freemankevin.uk/2024/12/20/debian-offline-package/"/>
    <id>https://freemankevin.uk/2024/12/20/debian-offline-package/</id>
    <published>2024-12-20T04:17:25.000Z</published>
    <updated>2024-12-23T15:55:02.387Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;离线安装在 Linux 系统中是一个常见需求，特别是在无法联网的环境中。本文介绍了如何在 Debian 系列操作系统（如 Debian 和 Ubuntu）上创建和使用离线安装包的完整步骤。首先，我们在联网环境下下载所需的软件包及其依赖，并创建本地 APT 仓库。然后，我们将这些包复制到离线系统上，并配置本地仓库以便进行安装。通过这些步骤，你可以方便地在离线环境中安装所需的软件。</p><span id="more"></span><h2 id="在联网的环境下"><a href="#在联网的环境下" class="headerlink" title="在联网的环境下"></a>在联网的环境下</h2><h3 id="1-下载软件包及其依赖"><a href="#1-下载软件包及其依赖" class="headerlink" title="1. 下载软件包及其依赖"></a>1. 下载软件包及其依赖</h3><p>首先，确定你需要的软件包和版本。使用APT的下载功能来获取软件包和它的所有依赖，但不安装它们。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt-get install --download-only &lt;package-name&gt;</span><br></pre></td></tr></table></figure><p>这会将软件包及其依赖下载到 <code>/var/cache/apt/archives/</code> 目录。</p><h3 id="2-复制下载的包"><a href="#2-复制下载的包" class="headerlink" title="2. 复制下载的包"></a>2. 复制下载的包</h3><p>将下载的 <code>.deb</code> 包复制到一个移动存储设备上。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cp</span> /var/cache/apt/archives/*.deb /path/to/your/usb-drive/debian-offline-packages</span><br></pre></td></tr></table></figure><h3 id="3-创建本地APT仓库"><a href="#3-创建本地APT仓库" class="headerlink" title="3. 创建本地APT仓库"></a>3. 创建本地APT仓库</h3><p>在移动存储设备的相同目录下，使用 <code>dpkg-scanpackages</code> 工具来创建 <code>Packages.gz</code> 索引文件。如果没有安装 <code>dpkg-dev</code>，需要先安装它。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt-get install dpkg-dev</span><br><span class="line"><span class="built_in">cd</span> /path/to/your/usb-drive/debian-offline-packages</span><br><span class="line">dpkg-scanpackages . /dev/null | gzip -9c &gt; Packages.gz</span><br></pre></td></tr></table></figure><h2 id="在离线的环境下"><a href="#在离线的环境下" class="headerlink" title="在离线的环境下"></a>在离线的环境下</h2><h3 id="4-准备本地APT仓库"><a href="#4-准备本地APT仓库" class="headerlink" title="4. 准备本地APT仓库"></a>4. 准备本地APT仓库</h3><p>将移动存储设备连接到离线的Debian系统。复制 <code>.deb</code> 包到本地文件系统，并保留目录结构。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p /path/to/local/repo</span><br><span class="line"><span class="built_in">cp</span> /path/to/your/usb-drive/debian-offline-packages/* /path/to/local/repo</span><br></pre></td></tr></table></figure><h3 id="5-添加本地仓库到APT源"><a href="#5-添加本地仓库到APT源" class="headerlink" title="5. 添加本地仓库到APT源"></a>5. 添加本地仓库到APT源</h3><p>创建一个APT源列表文件，以便APT可以使用本地仓库。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;deb [trusted=yes] file:///path/to/local/repo ./&#x27;</span> | <span class="built_in">sudo</span> <span class="built_in">tee</span> /etc/apt/sources.list.d/local-offline-repo.list</span><br></pre></td></tr></table></figure><h3 id="6-更新软件包列表"><a href="#6-更新软件包列表" class="headerlink" title="6. 更新软件包列表"></a>6. 更新软件包列表</h3><p>更新本地包数据库，使APT能够识别新的本地仓库。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt-get update</span><br></pre></td></tr></table></figure><h3 id="7-安装软件"><a href="#7-安装软件" class="headerlink" title="7. 安装软件"></a>7. 安装软件</h3><p>现在，使用 <code>apt-get install</code> 命令安装软件包，APT将解决所有本地依赖。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt-get install &lt;package-name&gt;</span><br></pre></td></tr></table></figure><h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><ul><li>这个过程假定你已经有了移动存储设备，如USB驱动器，并且它已经被挂载到了你的系统上。</li><li><code>&lt;package-name&gt;</code> 是你想要下载并安装的软件包名称。</li><li><code>/path/to/your/usb-drive</code> 是你USB驱动器的挂载点。</li><li><code>/path/to/local/repo</code> 是你在离线机器上创建的本地仓库目录的路径。</li></ul><p>按照这些步骤，你应该能够在Debian系列系统上进行离线软件安装。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;离线安装在 Linux 系统中是一个常见需求，特别是在无法联网的环境中。本文介绍了如何在 Debian 系列操作系统（如 Debian 和 Ubuntu）上创建和使用离线安装包的完整步骤。首先，我们在联网环境下下载所需的软件包及其依赖，并创建本地 APT 仓库。然后，我们将这些包复制到离线系统上，并配置本地仓库以便进行安装。通过这些步骤，你可以方便地在离线环境中安装所需的软件。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Offline" scheme="https://freemankevin.uk/tags/Offline/"/>
    
    <category term="Apt" scheme="https://freemankevin.uk/tags/Apt/"/>
    
    <category term="Debian" scheme="https://freemankevin.uk/tags/Debian/"/>
    
  </entry>
  
  <entry>
    <title>使用 Rufus 创建 Linux 启动 U 盘指南</title>
    <link href="https://freemankevin.uk/2024/12/20/rufus/"/>
    <id>https://freemankevin.uk/2024/12/20/rufus/</id>
    <published>2024-12-20T04:17:25.000Z</published>
    <updated>2024-12-23T14:44:30.704Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;Rufus 是一个免费、开源的工具，用于创建启动 U 盘，非常适合安装各种 Linux 发行版。本文介绍了使用 Rufus 为 Linux 发行版（如 Debian 或 CentOS）创建启动 U 盘的详细步骤。首先，准备好 U 盘、Linux ISO 文件和 Rufus 软件。然后，在 Rufus 中选择设备、配置启动选项和文件系统，并启动创建过程。完成后，使用启动 U 盘进行 Linux 安装。通过这些步骤，你可以轻松创建 Linux 启动 U 盘，并在目标电脑上安装所需的操作系统。</p><span id="more"></span><h2 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h2><p>在开始之前，请确保你已经准备好以下几样东西：</p><ul><li><strong>一个 4 GB 或更大容量的 U 盘</strong>：请注意，所有现有数据都将被清除。</li><li><strong>Linux ISO 文件</strong>：从 Linux 发行版的官网下载你需要安装的 ISO 文件，例如 Debian 或 CentOS。</li><li><strong>下载 Rufus</strong>：访问 <a href="https://rufus.ie/">Rufus 官网</a> 下载最新版本的 Rufus。</li></ul><h2 id="步骤-1-打开-Rufus"><a href="#步骤-1-打开-Rufus" class="headerlink" title="步骤 1: 打开 Rufus"></a>步骤 1: 打开 Rufus</h2><ol><li>插入 U 盘。</li><li>运行你下载的 Rufus.exe 文件。</li><li>如果系统请求权限，请确认。</li></ol><h2 id="步骤-2-配置-Rufus"><a href="#步骤-2-配置-Rufus" class="headerlink" title="步骤 2: 配置 Rufus"></a>步骤 2: 配置 Rufus</h2><p>在 Rufus 的界面中，进行以下配置：</p><ol><li><strong>设备</strong>：确保你的 U 盘在「设备」下被正确选择。</li><li><strong>启动选择</strong>：点击「选择」按钮，浏览并选择你的 Linux ISO 文件。</li><li><strong>分区方案</strong>：选择适合你系统的分区方案。<ul><li><strong>MBR</strong>：用于旧系统或传统 BIOS。</li><li><strong>GPT</strong>：用于较新系统或 UEFI 模式。</li></ul></li><li><strong>目标系统</strong>：根据你选择的分区方案，这里会自动填充「BIOS（或 UEFI-CSM）」或「UEFI（非 CSM）」。</li><li><strong>文件系统</strong>：一般选择「FAT32」（尤其是对于 UEFI 启动），这是大多数 Linux 启动 U 盘推荐的格式。</li><li><strong>卷标</strong>：你可以给 U 盘命名，例如「LINUXINSTALL」。</li></ol><h2 id="步骤-3-启动创建过程"><a href="#步骤-3-启动创建过程" class="headerlink" title="步骤 3: 启动创建过程"></a>步骤 3: 启动创建过程</h2><ol><li>确认所有设置正确无误后，点击「开始」按钮。</li><li>Rufus 将提示你所有数据将被清除。点击「确定」继续。</li><li>等待 Rufus 完成启动 U 盘的创建。这可能需要几分钟时间。</li></ol><h2 id="步骤-4-使用启动-U-盘安装-Linux"><a href="#步骤-4-使用启动-U-盘安装-Linux" class="headerlink" title="步骤 4: 使用启动 U 盘安装 Linux"></a>步骤 4: 使用启动 U 盘安装 Linux</h2><ol><li>完成 U 盘创建后，安全移除 U 盘。</li><li>将 U 盘插入你想要安装 Linux 的电脑。</li><li>重启电脑，并进入 BIOS&#x2F;UEFI 设置。</li><li>在 BIOS&#x2F;UEFI 中设置从 U 盘启动。</li><li>保存设置并退出 BIOS&#x2F;UEFI。</li><li>电脑将从 U 盘启动，并开始 Linux 安装过程。按照屏幕上的指示完成 Linux 的安装。</li></ol><h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><ul><li>确保在创建过程中不要移除 U 盘。</li><li>如果你的电脑在启动时没有提示进入 BIOS&#x2F;UEFI，你可能需要查找特定于你电脑型号的快捷键。</li><li>在使用 U 盘之前，确保备份 U 盘上的所有重要数据。</li></ul><p>通过以上步骤，你应该能够顺利使用 Rufus 创建一个 Linux 启动 U 盘，并使用它来安装 Debian、CentOS 或其他 Linux 操作系统。</p><p>当然，对于安装Windows 操作系统也是类似的过程。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;Rufus 是一个免费、开源的工具，用于创建启动 U 盘，非常适合安装各种 Linux 发行版。本文介绍了使用 Rufus 为 Linux 发行版（如 Debian 或 CentOS）创建启动 U 盘的详细步骤。首先，准备好 U 盘、Linux ISO 文件和 Rufus 软件。然后，在 Rufus 中选择设备、配置启动选项和文件系统，并启动创建过程。完成后，使用启动 U 盘进行 Linux 安装。通过这些步骤，你可以轻松创建 Linux 启动 U 盘，并在目标电脑上安装所需的操作系统。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="Rufus" scheme="https://freemankevin.uk/tags/Rufus/"/>
    
    <category term="Windows" scheme="https://freemankevin.uk/tags/Windows/"/>
    
  </entry>
  
  <entry>
    <title>使用镜像ISO 文件制做本地Yum 源</title>
    <link href="https://freemankevin.uk/2024/12/20/yum/"/>
    <id>https://freemankevin.uk/2024/12/20/yum/</id>
    <published>2024-12-20T04:17:25.000Z</published>
    <updated>2024-12-23T14:50:17.540Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;本文介绍了如何在 CentOS 7 环境中使用镜像 ISO 文件制作本地 YUM 源。通过详细步骤指导，用户可以下载并挂载 CentOS 7.9 的 ISO 文件，创建 YUM 仓库配置文件，清理 YUM 缓存，并测试新的 YUM 仓库配置。文章还介绍了如何设置开机自动挂载 ISO 文件，以确保每次启动时本地 YUM 源都可用。通过这些步骤，用户可以在本地环境中使用 YUM 安装、更新或删除软件包。</p><span id="more"></span><blockquote><p>这里以Centos7 环境为例，其他系统也是类似。</p></blockquote><p>阿里云ISO 镜像下载地址：<a href="https://mirrors.aliyun.com/centos/7.9.2009/isos/x86_64/">https://mirrors.aliyun.com/centos/7.9.2009/isos/x86_64/</a></p><p>要将CentOS 7.9的ISO映像文件挂载到CentOS 7.4系统上，并配置为本地YUM源，你可以按照以下步骤操作：</p><ol><li><p><strong>上传ISO文件</strong>:<br>将CentOS 7.9的ISO文件上传到你的CentOS 7.4服务器上。假设ISO文件位于<code>/path/to/CentOS-7.9.iso</code>。</p></li><li><p><strong>创建挂载点</strong>:<br>创建一个目录来作为ISO文件的挂载点。</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">mkdir</span> /mnt/cdrom</span><br></pre></td></tr></table></figure></li><li><p><strong>挂载ISO文件</strong>:<br>使用<code>mount</code>命令将ISO文件挂载到刚创建的挂载点。</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> mount -o loop /path/to/CentOS-7.9.iso /mnt/cdrom</span><br></pre></td></tr></table></figure><p><code>loop</code>选项用于将文件作为块设备挂载。</p></li><li><p><strong>创建YUM仓库配置文件</strong>:<br>在<code>/etc/yum.repos.d/</code>目录下创建一个新的<code>.repo</code>文件。</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vi /etc/yum.repos.d/CentOS-Base.repo</span><br></pre></td></tr></table></figure><p>在该文件中添加以下内容：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">[local-cdrom]</span><br><span class="line">name=CentOS-7.9 Local Repository</span><br><span class="line">baseurl=file:///mnt/cdrom</span><br><span class="line">enabled=1</span><br><span class="line">gpgcheck=0 # 跳过检查</span><br><span class="line">gpgkey=file:///mnt/cdrom/RPM-GPG-KEY-CentOS-7</span><br></pre></td></tr></table></figure><p>按<code>ESC</code>，然后输入<code>:wq</code>保存并退出vi编辑器。</p></li><li><p><strong>清理YUM缓存</strong>:<br>清理YUM缓存，确保YUM识别新的仓库。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo yum clean all</span><br><span class="line">sudo yum makecache</span><br></pre></td></tr></table></figure></li><li><p><strong>测试新的YUM仓库</strong>:<br>使用YUM repolist命令检查仓库列表，确保本地仓库已经被添加。</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum repolist</span><br></pre></td></tr></table></figure></li><li><p><strong>设置开机自动挂载</strong>:<br>如果你希望每次开机自动挂载ISO文件，可以编辑<code>/etc/fstab</code>文件。</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vi /etc/fstab</span><br></pre></td></tr></table></figure><p>在文件末尾添加以下行：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/path/to/CentOS-7.9.iso /mnt/cdrom iso9660 loop,ro 0 0</span><br></pre></td></tr></table></figure><p>再次按<code>ESC</code>，然后输入<code>:wq</code>保存并退出vi编辑器。</p></li></ol><p>现在你可以使用本地YUM源来安装、更新或删除软件包了。例如：</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum install &lt;package-name&gt;</span><br></pre></td></tr></table></figure><p>确保在执行这些步骤之前，你有足够的权限，以防需要root权限执行某些命令。此外，ISO文件的路径应该是静态的；如果ISO文件移动到其他位置，你需要更新<code>/etc/fstab</code>和<code>/etc/yum.repos.d/CentOS-Base.repo</code>文件中的路径。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;本文介绍了如何在 CentOS 7 环境中使用镜像 ISO 文件制作本地 YUM 源。通过详细步骤指导，用户可以下载并挂载 CentOS 7.9 的 ISO 文件，创建 YUM 仓库配置文件，清理 YUM 缓存，并测试新的 YUM 仓库配置。文章还介绍了如何设置开机自动挂载 ISO 文件，以确保每次启动时本地 YUM 源都可用。通过这些步骤，用户可以在本地环境中使用 YUM 安装、更新或删除软件包。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Yum" scheme="https://freemankevin.uk/tags/Yum/"/>
    
    <category term="CentOS" scheme="https://freemankevin.uk/tags/CentOS/"/>
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
  </entry>
  
  <entry>
    <title>如何在不同 Linux 系统下安装 VNC 服务</title>
    <link href="https://freemankevin.uk/2024/12/19/vnc/"/>
    <id>https://freemankevin.uk/2024/12/19/vnc/</id>
    <published>2024-12-19T02:00:00.000Z</published>
    <updated>2024-12-23T14:49:04.609Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;VNC（Virtual Network Computing）是一个远程桌面访问协议，允许用户通过网络远程访问 Linux 系统的图形化界面。本文介绍了如何在不同的 Linux 发行版（包括 Ubuntu&#x2F;Debian、CentOS&#x2F;RHEL、openSUSE 和 Arch Linux）中安装和配置 VNC 服务。通过详细的步骤指导，包括安装 VNC 软件、配置 VNC 密码、创建启动脚本和启动 VNC 服务，你可以在各个系统上实现远程桌面访问。此外，本文还提供了常见问题的排查方法，以帮助解决可能遇到的连接和显示问题。</p><span id="more"></span><h2 id="1-Ubuntu-Debian-系统"><a href="#1-Ubuntu-Debian-系统" class="headerlink" title="1. Ubuntu&#x2F;Debian 系统"></a>1. Ubuntu&#x2F;Debian 系统</h2><p>Ubuntu 和 Debian 系统可以通过 APT 包管理器安装常见的 VNC 服务。</p><h3 id="安装-TigerVNC"><a href="#安装-TigerVNC" class="headerlink" title="安装 TigerVNC"></a>安装 TigerVNC</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install tigervnc-standalone-server tigervnc-xorg-extension</span><br></pre></td></tr></table></figure><h3 id="配置-VNC"><a href="#配置-VNC" class="headerlink" title="配置 VNC"></a>配置 VNC</h3><ol><li><p>设置 VNC 密码：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncpasswd</span><br></pre></td></tr></table></figure><p>按提示输入密码。</p></li><li><p>创建配置文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ~/.vnc</span><br><span class="line">nano ~/.vnc/xstartup</span><br></pre></td></tr></table></figure><p>添加以下内容（以 GNOME 桌面为例）：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"><span class="built_in">exec</span> /usr/bin/gnome-session &amp;</span><br></pre></td></tr></table></figure><p>保存后为脚本添加可执行权限：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chmod</span> +x ~/.vnc/xstartup</span><br></pre></td></tr></table></figure></li><li><p>启动 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncserver</span><br></pre></td></tr></table></figure></li><li><p>停止 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncserver -<span class="built_in">kill</span> :1</span><br></pre></td></tr></table></figure></li></ol><hr><h2 id="2-CentOS-RHEL-系统"><a href="#2-CentOS-RHEL-系统" class="headerlink" title="2. CentOS&#x2F;RHEL 系统"></a>2. CentOS&#x2F;RHEL 系统</h2><p>CentOS 和 RHEL 通常通过 YUM 或 DNF 安装 VNC 服务。</p><h3 id="安装-TigerVNC-1"><a href="#安装-TigerVNC-1" class="headerlink" title="安装 TigerVNC"></a>安装 TigerVNC</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum install tigervnc-server</span><br></pre></td></tr></table></figure><h3 id="配置-VNC-1"><a href="#配置-VNC-1" class="headerlink" title="配置 VNC"></a>配置 VNC</h3><ol><li><p>编辑配置文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> nano /etc/tigervnc/vncserver.users</span><br></pre></td></tr></table></figure><p>添加如下内容：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">:1=username</span><br></pre></td></tr></table></figure><p>替换 <code>username</code> 为实际用户。</p></li><li><p>设置 VNC 密码：<br>切换到指定用户并设置密码：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">su - username</span><br><span class="line">vncpasswd</span><br></pre></td></tr></table></figure></li><li><p>配置桌面环境（以 GNOME 为例）：<br>编辑用户的 <code>~/.vnc/xstartup</code> 文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nano ~/.vnc/xstartup</span><br></pre></td></tr></table></figure><p>添加以下内容：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"><span class="built_in">exec</span> /usr/bin/gnome-session &amp;</span><br></pre></td></tr></table></figure><p>保存后为脚本添加可执行权限：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chmod</span> +x ~/.vnc/xstartup</span><br></pre></td></tr></table></figure></li><li><p>启动 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start vncserver@:1</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> vncserver@:1</span><br></pre></td></tr></table></figure></li></ol><hr><h2 id="3-openSUSE-系统"><a href="#3-openSUSE-系统" class="headerlink" title="3. openSUSE 系统"></a>3. openSUSE 系统</h2><p>openSUSE 系统也支持安装和配置 VNC 服务。</p><h3 id="安装-TightVNC"><a href="#安装-TightVNC" class="headerlink" title="安装 TightVNC"></a>安装 TightVNC</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> zypper install tightvnc</span><br></pre></td></tr></table></figure><h3 id="配置-VNC-2"><a href="#配置-VNC-2" class="headerlink" title="配置 VNC"></a>配置 VNC</h3><ol><li><p>设置 VNC 密码：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncpasswd</span><br></pre></td></tr></table></figure></li><li><p>创建配置文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ~/.vnc</span><br><span class="line">nano ~/.vnc/xstartup</span><br></pre></td></tr></table></figure><p>添加以下内容：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"><span class="built_in">exec</span> startkde &amp;</span><br></pre></td></tr></table></figure><p>根据桌面环境选择命令，例如 KDE 使用 <code>startkde</code>，GNOME 使用 <code>gnome-session</code>。</p></li><li><p>启动 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncserver</span><br></pre></td></tr></table></figure></li><li><p>停止 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncserver -<span class="built_in">kill</span> :1</span><br></pre></td></tr></table></figure></li></ol><hr><h2 id="4-Arch-Linux"><a href="#4-Arch-Linux" class="headerlink" title="4. Arch Linux"></a>4. Arch Linux</h2><p>在 Arch Linux 上，可以通过 <code>pacman</code> 包管理器安装 VNC 服务。</p><h3 id="安装-TigerVNC-2"><a href="#安装-TigerVNC-2" class="headerlink" title="安装 TigerVNC"></a>安装 TigerVNC</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pacman -S tigervnc</span><br></pre></td></tr></table></figure><h3 id="配置-VNC-3"><a href="#配置-VNC-3" class="headerlink" title="配置 VNC"></a>配置 VNC</h3><ol><li><p>设置 VNC 密码：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncpasswd</span><br></pre></td></tr></table></figure></li><li><p>配置 VNC：<br>编辑 <code>~/.vnc/xstartup</code> 文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nano ~/.vnc/xstartup</span><br></pre></td></tr></table></figure><p>添加以下内容：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"><span class="built_in">exec</span> /usr/bin/xfce4-session &amp;</span><br></pre></td></tr></table></figure><p>根据桌面环境选择启动命令，例如 XFCE 使用 <code>xfce4-session</code>。</p></li><li><p>启动 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncserver</span><br></pre></td></tr></table></figure></li><li><p>停止 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncserver -<span class="built_in">kill</span> :1</span><br></pre></td></tr></table></figure></li></ol><hr><h2 id="5-常见问题排查"><a href="#5-常见问题排查" class="headerlink" title="5. 常见问题排查"></a>5. 常见问题排查</h2><ul><li><p><strong>连接失败</strong>：</p><ul><li>检查防火墙是否允许 VNC 端口（默认 5901）。<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ufw allow 5901</span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong>显示空白屏幕</strong>：</p><ul><li>确保 <code>xstartup</code> 文件正确配置并具有可执行权限。</li></ul></li><li><p><strong>服务未启动</strong>：</p><ul><li>检查日志文件 <code>~/.vnc/*.log</code> 获取更多信息。</li></ul></li></ul><hr><p>通过以上步骤，您可以在不同的 Linux 发行版中安装和配置 VNC 服务，轻松实现远程桌面访问。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;VNC（Virtual Network Computing）是一个远程桌面访问协议，允许用户通过网络远程访问 Linux 系统的图形化界面。本文介绍了如何在不同的 Linux 发行版（包括 Ubuntu&amp;#x2F;Debian、CentOS&amp;#x2F;RHEL、openSUSE 和 Arch Linux）中安装和配置 VNC 服务。通过详细的步骤指导，包括安装 VNC 软件、配置 VNC 密码、创建启动脚本和启动 VNC 服务，你可以在各个系统上实现远程桌面访问。此外，本文还提供了常见问题的排查方法，以帮助解决可能遇到的连接和显示问题。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="VNC" scheme="https://freemankevin.uk/tags/VNC/"/>
    
  </entry>
  
  <entry>
    <title>如何在不同 Linux 系统下安装 Docker</title>
    <link href="https://freemankevin.uk/2024/12/19/docker/"/>
    <id>https://freemankevin.uk/2024/12/19/docker/</id>
    <published>2024-12-19T01:00:00.000Z</published>
    <updated>2024-12-23T14:38:58.766Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;Docker 是一种开源的容器化平台，可以简化应用程序的开发、部署和运行。本文介绍了如何在不同的 Linux 系统中安装 Docker，包括 Ubuntu&#x2F;Debian、CentOS&#x2F;RHEL、openSUSE 和 Arch Linux。我们详细讲解了每个系统的安装步骤，包括更新系统、安装依赖、添加 Docker 软件源、安装 Docker 引擎以及验证安装。最后，我们还介绍了如何配置非 root 用户使用 Docker。通过这些步骤，你可以在各种 Linux 发行版上轻松安装和配置 Docker。</p><span id="more"></span><h2 id="1-Ubuntu-Debian-系统"><a href="#1-Ubuntu-Debian-系统" class="headerlink" title="1. Ubuntu&#x2F;Debian 系统"></a>1. Ubuntu&#x2F;Debian 系统</h2><h3 id="更新系统"><a href="#更新系统" class="headerlink" title="更新系统"></a>更新系统</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt upgrade -y</span><br></pre></td></tr></table></figure><h3 id="安装依赖"><a href="#安装依赖" class="headerlink" title="安装依赖"></a>安装依赖</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install -y \</span><br><span class="line">    ca-certificates \</span><br><span class="line">    curl \</span><br><span class="line">    gnupg \</span><br><span class="line">    lsb-release</span><br></pre></td></tr></table></figure><h3 id="添加-Docker-的官方-GPG-密钥"><a href="#添加-Docker-的官方-GPG-密钥" class="headerlink" title="添加 Docker 的官方 GPG 密钥"></a>添加 Docker 的官方 GPG 密钥</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">mkdir</span> -p /etc/apt/keyrings</span><br><span class="line">curl -fsSL https://download.docker.com/linux/ubuntu/gpg | <span class="built_in">sudo</span> gpg --dearmor -o /etc/apt/keyrings/docker.gpg</span><br></pre></td></tr></table></figure><h3 id="设置-Docker-软件源"><a href="#设置-Docker-软件源" class="headerlink" title="设置 Docker 软件源"></a>设置 Docker 软件源</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> \&quot;deb [<span class="built_in">arch</span>=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \</span><br><span class="line">$(lsb_release -cs) stable\&quot; | <span class="built_in">sudo</span> <span class="built_in">tee</span> /etc/apt/sources.list.d/docker.list &gt; /dev/null</span><br></pre></td></tr></table></figure><h3 id="安装-Docker-引擎"><a href="#安装-Docker-引擎" class="headerlink" title="安装 Docker 引擎"></a>安装 Docker 引擎</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin</span><br></pre></td></tr></table></figure><h3 id="验证安装"><a href="#验证安装" class="headerlink" title="验证安装"></a>验证安装</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker --version</span><br></pre></td></tr></table></figure><hr><h2 id="2-CentOS-RHEL-系统"><a href="#2-CentOS-RHEL-系统" class="headerlink" title="2. CentOS&#x2F;RHEL 系统"></a>2. CentOS&#x2F;RHEL 系统</h2><h3 id="更新系统-1"><a href="#更新系统-1" class="headerlink" title="更新系统"></a>更新系统</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum update -y</span><br></pre></td></tr></table></figure><h3 id="安装依赖-1"><a href="#安装依赖-1" class="headerlink" title="安装依赖"></a>安装依赖</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum install -y yum-utils</span><br></pre></td></tr></table></figure><h3 id="添加-Docker-软件源"><a href="#添加-Docker-软件源" class="headerlink" title="添加 Docker 软件源"></a>添加 Docker 软件源</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo</span><br></pre></td></tr></table></figure><h3 id="安装-Docker-引擎-1"><a href="#安装-Docker-引擎-1" class="headerlink" title="安装 Docker 引擎"></a>安装 Docker 引擎</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum install -y docker-ce docker-ce-cli containerd.io</span><br></pre></td></tr></table></figure><h3 id="启动-Docker-服务"><a href="#启动-Docker-服务" class="headerlink" title="启动 Docker 服务"></a>启动 Docker 服务</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start docker</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> docker</span><br></pre></td></tr></table></figure><h3 id="验证安装-1"><a href="#验证安装-1" class="headerlink" title="验证安装"></a>验证安装</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker --version</span><br></pre></td></tr></table></figure><hr><h2 id="3-openSUSE-系统"><a href="#3-openSUSE-系统" class="headerlink" title="3. openSUSE 系统"></a>3. openSUSE 系统</h2><h3 id="更新系统-2"><a href="#更新系统-2" class="headerlink" title="更新系统"></a>更新系统</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> zypper refresh</span><br></pre></td></tr></table></figure><h3 id="安装-Docker"><a href="#安装-Docker" class="headerlink" title="安装 Docker"></a>安装 Docker</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> zypper install -y docker</span><br></pre></td></tr></table></figure><h3 id="启动-Docker-服务-1"><a href="#启动-Docker-服务-1" class="headerlink" title="启动 Docker 服务"></a>启动 Docker 服务</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start docker</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> docker</span><br></pre></td></tr></table></figure><h3 id="验证安装-2"><a href="#验证安装-2" class="headerlink" title="验证安装"></a>验证安装</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker --version</span><br></pre></td></tr></table></figure><hr><h2 id="4-Arch-Linux"><a href="#4-Arch-Linux" class="headerlink" title="4. Arch Linux"></a>4. Arch Linux</h2><h3 id="更新系统-3"><a href="#更新系统-3" class="headerlink" title="更新系统"></a>更新系统</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pacman -Syu</span><br></pre></td></tr></table></figure><h3 id="安装-Docker-1"><a href="#安装-Docker-1" class="headerlink" title="安装 Docker"></a>安装 Docker</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pacman -S docker</span><br></pre></td></tr></table></figure><h3 id="启动-Docker-服务-2"><a href="#启动-Docker-服务-2" class="headerlink" title="启动 Docker 服务"></a>启动 Docker 服务</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start docker</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> docker</span><br></pre></td></tr></table></figure><h3 id="验证安装-3"><a href="#验证安装-3" class="headerlink" title="验证安装"></a>验证安装</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker --version</span><br></pre></td></tr></table></figure><hr><h2 id="5-配置非-root-用户使用-Docker"><a href="#5-配置非-root-用户使用-Docker" class="headerlink" title="5. 配置非 root 用户使用 Docker"></a>5. 配置非 root 用户使用 Docker</h2><p>默认情况下，只有 root 用户或具有 sudo 权限的用户可以运行 Docker。你可以通过以下步骤允许非 root 用户使用 Docker：</p><h3 id="添加用户到-Docker-组"><a href="#添加用户到-Docker-组" class="headerlink" title="添加用户到 Docker 组"></a>添加用户到 Docker 组</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> usermod -aG docker <span class="variable">$USER</span></span><br></pre></td></tr></table></figure><h3 id="重新登录"><a href="#重新登录" class="headerlink" title="重新登录"></a>重新登录</h3><p>重新登录后，非 root 用户即可运行 Docker 命令。</p><h3 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run hello-world</span><br></pre></td></tr></table></figure><hr><p>通过以上步骤，你可以在不同的 Linux 系统中安装和配置 Docker。安装完成后，可以开始体验 Docker 的强大功能，如运行容器、构建镜像等。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;Docker 是一种开源的容器化平台，可以简化应用程序的开发、部署和运行。本文介绍了如何在不同的 Linux 系统中安装 Docker，包括 Ubuntu&amp;#x2F;Debian、CentOS&amp;#x2F;RHEL、openSUSE 和 Arch Linux。我们详细讲解了每个系统的安装步骤，包括更新系统、安装依赖、添加 Docker 软件源、安装 Docker 引擎以及验证安装。最后，我们还介绍了如何配置非 root 用户使用 Docker。通过这些步骤，你可以在各种 Linux 发行版上轻松安装和配置 Docker。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Docker" scheme="https://freemankevin.uk/tags/Docker/"/>
    
    <category term="Container" scheme="https://freemankevin.uk/tags/Container/"/>
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
  </entry>
  
  <entry>
    <title>如何在不同 Linux 系统下开启防火墙</title>
    <link href="https://freemankevin.uk/2024/12/13/firewalld/"/>
    <id>https://freemankevin.uk/2024/12/13/firewalld/</id>
    <published>2024-12-13T03:34:15.000Z</published>
    <updated>2024-12-23T14:39:51.974Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;防火墙是保护系统安全的重要组成部分，它可以帮助阻止未授权的访问。本文介绍了如何在不同的 Linux 系统中开启和管理防火墙，包括 Ubuntu&#x2F;Debian、CentOS&#x2F;RHEL、openSUSE 和 Arch Linux 系统。我们详细讲解了每个系统中常用的防火墙工具和命令，例如 ufw、firewalld、SuSEfirewall2、iptables 和 nftables。通过这些步骤，你可以在各种 Linux 发行版上有效地开启防火墙并配置规则，从而增强系统的安全性。</p><span id="more"></span><h2 id="1-Ubuntu-Debian-系统"><a href="#1-Ubuntu-Debian-系统" class="headerlink" title="1. Ubuntu&#x2F;Debian 系统"></a>1. Ubuntu&#x2F;Debian 系统</h2><p>Ubuntu 和 Debian 系统默认使用 <code>ufw</code>（Uncomplicated Firewall）作为防火墙工具。</p><h3 id="检查防火墙状态"><a href="#检查防火墙状态" class="headerlink" title="检查防火墙状态"></a>检查防火墙状态</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ufw status</span><br></pre></td></tr></table></figure><h3 id="开启防火墙"><a href="#开启防火墙" class="headerlink" title="开启防火墙"></a>开启防火墙</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ufw <span class="built_in">enable</span></span><br></pre></td></tr></table></figure><h3 id="禁用防火墙"><a href="#禁用防火墙" class="headerlink" title="禁用防火墙"></a>禁用防火墙</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ufw <span class="built_in">disable</span></span><br></pre></td></tr></table></figure><h3 id="配置示例"><a href="#配置示例" class="headerlink" title="配置示例"></a>配置示例</h3><p>允许 SSH 连接：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ufw allow ssh</span><br></pre></td></tr></table></figure><hr><h2 id="2-CentOS-RHEL-系统"><a href="#2-CentOS-RHEL-系统" class="headerlink" title="2. CentOS&#x2F;RHEL 系统"></a>2. CentOS&#x2F;RHEL 系统</h2><p>CentOS 和 RHEL 系统默认使用 <code>firewalld</code> 作为防火墙工具。</p><h3 id="检查防火墙状态-1"><a href="#检查防火墙状态-1" class="headerlink" title="检查防火墙状态"></a>检查防火墙状态</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl status firewalld</span><br></pre></td></tr></table></figure><h3 id="开启防火墙-1"><a href="#开启防火墙-1" class="headerlink" title="开启防火墙"></a>开启防火墙</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start firewalld</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> firewalld</span><br></pre></td></tr></table></figure><h3 id="禁用防火墙-1"><a href="#禁用防火墙-1" class="headerlink" title="禁用防火墙"></a>禁用防火墙</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl stop firewalld</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">disable</span> firewalld</span><br></pre></td></tr></table></figure><h3 id="配置示例-1"><a href="#配置示例-1" class="headerlink" title="配置示例"></a>配置示例</h3><p>允许 HTTP 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> firewall-cmd --permanent --add-service=http</span><br><span class="line"><span class="built_in">sudo</span> firewall-cmd --reload</span><br></pre></td></tr></table></figure><hr><h2 id="3-openSUSE-系统"><a href="#3-openSUSE-系统" class="headerlink" title="3. openSUSE 系统"></a>3. openSUSE 系统</h2><p>openSUSE 系统默认使用 <code>firewalld</code>，但某些版本可能使用 <code>SuSEfirewall2</code>。</p><h3 id="使用-firewalld"><a href="#使用-firewalld" class="headerlink" title="使用 firewalld"></a>使用 firewalld</h3><h4 id="检查防火墙状态-2"><a href="#检查防火墙状态-2" class="headerlink" title="检查防火墙状态"></a>检查防火墙状态</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl status firewalld</span><br></pre></td></tr></table></figure><h4 id="开启防火墙-2"><a href="#开启防火墙-2" class="headerlink" title="开启防火墙"></a>开启防火墙</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start firewalld</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> firewalld</span><br></pre></td></tr></table></figure><h4 id="配置示例-2"><a href="#配置示例-2" class="headerlink" title="配置示例"></a>配置示例</h4><p>允许 HTTPS 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> firewall-cmd --permanent --add-service=https</span><br><span class="line"><span class="built_in">sudo</span> firewall-cmd --reload</span><br></pre></td></tr></table></figure><h3 id="使用-SuSEfirewall2"><a href="#使用-SuSEfirewall2" class="headerlink" title="使用 SuSEfirewall2"></a>使用 SuSEfirewall2</h3><h4 id="检查防火墙状态-3"><a href="#检查防火墙状态-3" class="headerlink" title="检查防火墙状态"></a>检查防火墙状态</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> SuSEfirewall2 status</span><br></pre></td></tr></table></figure><h4 id="开启防火墙-3"><a href="#开启防火墙-3" class="headerlink" title="开启防火墙"></a>开启防火墙</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> SuSEfirewall2 start</span><br></pre></td></tr></table></figure><hr><h2 id="4-Arch-Linux"><a href="#4-Arch-Linux" class="headerlink" title="4. Arch Linux"></a>4. Arch Linux</h2><p>Arch Linux 通常使用 <code>iptables</code> 或 <code>nftables</code> 来管理防火墙。</p><h3 id="使用-iptables"><a href="#使用-iptables" class="headerlink" title="使用 iptables"></a>使用 iptables</h3><h4 id="检查防火墙规则"><a href="#检查防火墙规则" class="headerlink" title="检查防火墙规则"></a>检查防火墙规则</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> iptables -L</span><br></pre></td></tr></table></figure><h4 id="添加规则并保存"><a href="#添加规则并保存" class="headerlink" title="添加规则并保存"></a>添加规则并保存</h4><p>允许所有来自端口 22 的连接：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> iptables -A INPUT -p tcp --dport 22 -j ACCEPT</span><br><span class="line"><span class="built_in">sudo</span> iptables-save &gt; /etc/iptables/iptables.rules</span><br></pre></td></tr></table></figure><h4 id="开启防火墙-4"><a href="#开启防火墙-4" class="headerlink" title="开启防火墙"></a>开启防火墙</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start iptables</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> iptables</span><br></pre></td></tr></table></figure><h3 id="使用-nftables"><a href="#使用-nftables" class="headerlink" title="使用 nftables"></a>使用 nftables</h3><h4 id="检查防火墙状态-4"><a href="#检查防火墙状态-4" class="headerlink" title="检查防火墙状态"></a>检查防火墙状态</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> nft list ruleset</span><br></pre></td></tr></table></figure><h4 id="添加规则并保存-1"><a href="#添加规则并保存-1" class="headerlink" title="添加规则并保存"></a>添加规则并保存</h4><p>允许 HTTP 流量：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> nft add rule ip filter input tcp dport 80 accept</span><br><span class="line"><span class="built_in">sudo</span> nft list ruleset &gt; /etc/nftables.conf</span><br></pre></td></tr></table></figure><h4 id="启用-nftables-服务"><a href="#启用-nftables-服务" class="headerlink" title="启用 nftables 服务"></a>启用 nftables 服务</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start nftables</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> nftables</span><br></pre></td></tr></table></figure><hr><h2 id="5-常见问题排查"><a href="#5-常见问题排查" class="headerlink" title="5. 常见问题排查"></a>5. 常见问题排查</h2><ul><li><p><strong>端口未生效</strong>：<br>确保规则已永久保存并重新加载防火墙配置。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ufw reload  <span class="comment"># 适用于 ufw</span></span><br><span class="line"><span class="built_in">sudo</span> firewall-cmd --reload  <span class="comment"># 适用于 firewalld</span></span><br></pre></td></tr></table></figure></li><li><p><strong>服务未运行</strong>：<br>确保防火墙服务已启动并设置为开机自启。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> firewalld</span><br><span class="line"><span class="built_in">sudo</span> systemctl start firewalld</span><br></pre></td></tr></table></figure></li></ul><hr><p>通过以上命令，你可以在不同的 Linux 系统中有效地开启防火墙并配置规则，从而增强系统的安全性。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;防火墙是保护系统安全的重要组成部分，它可以帮助阻止未授权的访问。本文介绍了如何在不同的 Linux 系统中开启和管理防火墙，包括 Ubuntu&amp;#x2F;Debian、CentOS&amp;#x2F;RHEL、openSUSE 和 Arch Linux 系统。我们详细讲解了每个系统中常用的防火墙工具和命令，例如 ufw、firewalld、SuSEfirewall2、iptables 和 nftables。通过这些步骤，你可以在各种 Linux 发行版上有效地开启防火墙并配置规则，从而增强系统的安全性。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Firewall" scheme="https://freemankevin.uk/tags/Firewall/"/>
    
    <category term="Ufw" scheme="https://freemankevin.uk/tags/Ufw/"/>
    
    <category term="Iptables" scheme="https://freemankevin.uk/tags/Iptables/"/>
    
  </entry>
  
  <entry>
    <title>数据盘挂载与LVM 创建</title>
    <link href="https://freemankevin.uk/2024/12/11/mount-hd-lvm/"/>
    <id>https://freemankevin.uk/2024/12/11/mount-hd-lvm/</id>
    <published>2024-12-11T04:17:25.000Z</published>
    <updated>2024-12-23T14:43:27.307Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在服务器环境中，使用逻辑卷管理器（LVM）配置和挂载数据盘是一种灵活高效的方式。本文介绍了在 CentOS 和其他常见 Linux 系统中，如何使用 LVM 将新数据盘挂载到 &#x2F;data 目录。我们详细讲解了从安装 LVM 工具、初始化物理卷、创建卷组和逻辑卷，到创建文件系统、挂载逻辑卷以及设置开机自动挂载的步骤。此外，还提供了适用于不同 Linux 系统的注意事项和扩展管理技巧。通过这些步骤，你可以实现数据盘的灵活管理和高效利用。</p><span id="more"></span><h3 id="前置检查"><a href="#前置检查" class="headerlink" title="前置检查"></a><strong>前置检查</strong></h3><ol><li>确保目标硬盘（<code>vdb</code>）是新磁盘或非关键数据盘。</li><li>验证系统是否检测到硬盘：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsblk</span><br></pre></td></tr></table></figure>找到 <code>vdb</code>，确认其未分区或挂载。</li></ol><hr><h3 id="安装-LVM-工具"><a href="#安装-LVM-工具" class="headerlink" title="安装 LVM 工具"></a><strong>安装 LVM 工具</strong></h3><ul><li><p><strong>CentOS&#x2F;Red Hat 系统</strong>:</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum install -y lvm2</span><br></pre></td></tr></table></figure></li><li><p><strong>Debian&#x2F;Ubuntu 系统</strong>:</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install -y lvm2</span><br></pre></td></tr></table></figure></li><li><p><strong>OpenSUSE 系统</strong>:</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> zypper install -y lvm2</span><br></pre></td></tr></table></figure></li></ul><hr><h3 id="LVM-配置步骤"><a href="#LVM-配置步骤" class="headerlink" title="LVM 配置步骤"></a><strong>LVM 配置步骤</strong></h3><h4 id="1-初始化物理卷-PV"><a href="#1-初始化物理卷-PV" class="headerlink" title="1. 初始化物理卷 (PV)"></a>1. 初始化物理卷 (PV)</h4><p>将 <code>vdb</code> 初始化为 LVM 的物理卷：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pvcreate /dev/vdb</span><br></pre></td></tr></table></figure><p>验证创建是否成功：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pvdisplay</span><br></pre></td></tr></table></figure><hr><h4 id="2-创建卷组-VG"><a href="#2-创建卷组-VG" class="headerlink" title="2. 创建卷组 (VG)"></a>2. 创建卷组 (VG)</h4><p>创建一个新的卷组 <code>vg_data</code>，并将 <code>vdb</code> 添加到该卷组：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vgcreate vg_data /dev/vdb</span><br></pre></td></tr></table></figure><p>检查卷组信息：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vgdisplay</span><br></pre></td></tr></table></figure><hr><h4 id="3-创建逻辑卷-LV"><a href="#3-创建逻辑卷-LV" class="headerlink" title="3. 创建逻辑卷 (LV)"></a>3. 创建逻辑卷 (LV)</h4><p>在卷组 <code>vg_data</code> 中创建逻辑卷 <code>lv_data</code>：</p><ul><li>使用所有剩余空间：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> lvcreate -l +100%FREE -n lv_data vg_data</span><br></pre></td></tr></table></figure></li><li>或者指定大小（如 100GB）：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> lvcreate -L 100G -n lv_data vg_data</span><br></pre></td></tr></table></figure></li></ul><p>验证逻辑卷：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> lvdisplay</span><br></pre></td></tr></table></figure><hr><h4 id="4-创建文件系统"><a href="#4-创建文件系统" class="headerlink" title="4. 创建文件系统"></a>4. 创建文件系统</h4><p>为逻辑卷 <code>lv_data</code> 创建 EXT4 文件系统：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> mkfs.ext4 /dev/vg_data/lv_data</span><br></pre></td></tr></table></figure><hr><h4 id="5-挂载逻辑卷"><a href="#5-挂载逻辑卷" class="headerlink" title="5. 挂载逻辑卷"></a>5. 挂载逻辑卷</h4><ol><li><p>创建挂载点：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">mkdir</span> -p /data</span><br></pre></td></tr></table></figure></li><li><p>挂载逻辑卷到 <code>/data</code>：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> mount /dev/vg_data/lv_data /data</span><br></pre></td></tr></table></figure></li><li><p>确认挂载成功：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">df</span> -h | grep /data</span><br></pre></td></tr></table></figure></li></ol><hr><h4 id="6-设置开机自动挂载"><a href="#6-设置开机自动挂载" class="headerlink" title="6. 设置开机自动挂载"></a>6. 设置开机自动挂载</h4><p>编辑 <code>/etc/fstab</code> 文件，使逻辑卷在开机时自动挂载：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vim /etc/fstab</span><br></pre></td></tr></table></figure><p>在文件末尾添加以下内容：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/dev/vg_data/lv_data /data ext4 defaults 0 0</span><br></pre></td></tr></table></figure><p>测试 <code>fstab</code> 配置是否正确：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> umount /data</span><br><span class="line"><span class="built_in">sudo</span> mount -a</span><br></pre></td></tr></table></figure><p>如果没有错误，挂载设置无误。</p><hr><h3 id="其他-Linux-系统的注意事项"><a href="#其他-Linux-系统的注意事项" class="headerlink" title="其他 Linux 系统的注意事项"></a><strong>其他 Linux 系统的注意事项</strong></h3><ol><li><p><strong>文件系统支持</strong>:<br>不同发行版可能默认支持不同文件系统（如 <code>XFS</code> 或 <code>Btrfs</code>）。可以根据需求替换 <code>mkfs.ext4</code> 为适合的文件系统命令，例如：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> mkfs.xfs /dev/vg_data/lv_data</span><br></pre></td></tr></table></figure></li><li><p><strong>卷组管理</strong>:<br>如果有多个硬盘，可将多个物理卷（如 <code>/dev/vdb</code> 和 <code>/dev/vdc</code>）加入同一卷组：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vgextend vg_data /dev/vdc</span><br></pre></td></tr></table></figure></li><li><p><strong>调整逻辑卷大小</strong>:<br>如果需要扩展逻辑卷：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> lvextend -L +50G /dev/vg_data/lv_data</span><br><span class="line"><span class="built_in">sudo</span> resize2fs /dev/vg_data/lv_data</span><br></pre></td></tr></table></figure></li></ol><hr><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a><strong>总结</strong></h3><p>通过以上步骤，我们成功配置了数据盘并挂载到 <code>/data</code>。这种使用 LVM 的方式不仅提供了灵活的卷管理能力，还能在未来需要时动态扩展存储，避免繁琐的重新分区操作。</p><p>确保在生产环境操作前备份重要数据，执行步骤时严格按照实际需求调整命令。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在服务器环境中，使用逻辑卷管理器（LVM）配置和挂载数据盘是一种灵活高效的方式。本文介绍了在 CentOS 和其他常见 Linux 系统中，如何使用 LVM 将新数据盘挂载到 &amp;#x2F;data 目录。我们详细讲解了从安装 LVM 工具、初始化物理卷、创建卷组和逻辑卷，到创建文件系统、挂载逻辑卷以及设置开机自动挂载的步骤。此外，还提供了适用于不同 Linux 系统的注意事项和扩展管理技巧。通过这些步骤，你可以实现数据盘的灵活管理和高效利用。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="Mount" scheme="https://freemankevin.uk/tags/Mount/"/>
    
    <category term="LVM" scheme="https://freemankevin.uk/tags/LVM/"/>
    
  </entry>
  
  <entry>
    <title>GitHub 推送代码失败的解决方案</title>
    <link href="https://freemankevin.uk/2024/12/11/github-push-error/"/>
    <id>https://freemankevin.uk/2024/12/11/github-push-error/</id>
    <published>2024-12-11T04:17:25.000Z</published>
    <updated>2024-12-23T14:41:05.347Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在使用 Git 推送代码到 GitHub 时，常常会遇到推送失败的问题，即使网络带宽正常。主要原因是 Git 默认不会通过本地代理，即使代理设置为全局模式也无效。本文介绍了通过设置 Git 的代理来解决推送问题的具体步骤。首先，修改全局 Git 配置文件，添加代理设置。然后，通过命令验证代理配置是否生效。通过这些步骤，你可以解决 Git 推送代码到 GitHub 时遇到的问题。</p><span id="more"></span><h3 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h3><p>在使用 Git 推送代码到 GitHub 时，<strong>推送经常失败</strong>，但带宽测试显示网络正常，上传和下载速度都没问题。经过排查发现，<strong>Git 默认不会通过本地代理</strong>，即使代理设置为全局模式也无效。这导致 GitHub 的访问不稳定。</p><hr><h3 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h3><p>通过设置 Git 的代理，可以解决推送问题。</p><h4 id="1-修改全局-Git-配置"><a href="#1-修改全局-Git-配置" class="headerlink" title="1. 修改全局 Git 配置"></a>1. 修改全局 Git 配置</h4><p>执行以下命令编辑 Git 的全局配置文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global --edit</span><br></pre></td></tr></table></figure><p>添加如下内容，将 <code>127.0.0.1:7890</code> 替换为你的代理地址和端口：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[http]</span><br><span class="line">proxy = http://127.0.0.1:7890</span><br><span class="line"></span><br><span class="line">[https]</span><br><span class="line">proxy = https://127.0.0.1:7890</span><br></pre></td></tr></table></figure><h4 id="2-验证代理配置"><a href="#2-验证代理配置" class="headerlink" title="2. 验证代理配置"></a>2. 验证代理配置</h4><p>使用以下命令检查配置是否生效：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global --get https.proxy</span><br></pre></td></tr></table></figure><p>若返回代理地址（如 <code>https://127.0.0.1:7890</code>），说明设置成功。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在使用 Git 推送代码到 GitHub 时，常常会遇到推送失败的问题，即使网络带宽正常。主要原因是 Git 默认不会通过本地代理，即使代理设置为全局模式也无效。本文介绍了通过设置 Git 的代理来解决推送问题的具体步骤。首先，修改全局 Git 配置文件，添加代理设置。然后，通过命令验证代理配置是否生效。通过这些步骤，你可以解决 Git 推送代码到 GitHub 时遇到的问题。&lt;/p&gt;</summary>
    
    
    
    <category term="GitHub" scheme="https://freemankevin.uk/categories/GitHub/"/>
    
    
    <category term="GitHub" scheme="https://freemankevin.uk/tags/GitHub/"/>
    
    <category term="Git" scheme="https://freemankevin.uk/tags/Git/"/>
    
    <category term="Proxy" scheme="https://freemankevin.uk/tags/Proxy/"/>
    
  </entry>
  
  <entry>
    <title>在没有 Swap 的服务器上创建 Swap</title>
    <link href="https://freemankevin.uk/2024/12/11/swap/"/>
    <id>https://freemankevin.uk/2024/12/11/swap/</id>
    <published>2024-12-11T04:17:25.000Z</published>
    <updated>2024-12-23T14:47:31.503Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在服务器上创建 Swap 文件是缓解内存不足的一种有效方法。本文介绍了如何在没有 Swap 分区的服务器上创建 Swap 文件，以提供额外的虚拟内存，从而保证系统的稳定性和性能。首先，我们检查系统是否已有 Swap 分区，然后通过创建和配置 Swap 文件来实现虚拟内存的扩展。最后，我们确保 Swap 文件在系统启动时自动启用。通过这些步骤，你可以在没有预配置 Swap 分区的服务器上创建 Swap 文件，确保系统在高负载情况下的稳定运行。</p><span id="more"></span><blockquote><p>这里以16核、32GB内存、300GB数据盘的服务器进行举例。</p></blockquote><h4 id="1-检查当前系统是否有-Swap"><a href="#1-检查当前系统是否有-Swap" class="headerlink" title="1. 检查当前系统是否有 Swap"></a>1. 检查当前系统是否有 Swap</h4><p>首先，我们需要检查当前系统是否有 Swap 分区。执行以下命令：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">swapon --show</span><br></pre></td></tr></table></figure><p>如果没有输出，说明当前系统没有 Swap 分区。</p><h4 id="2-创建-Swap-文件"><a href="#2-创建-Swap-文件" class="headerlink" title="2. 创建 Swap 文件"></a>2. 创建 Swap 文件</h4><p>为了在没有 Swap 分区的服务器上创建 Swap，我们可以使用文件来模拟 Swap。以下是创建 Swap 文件的详细步骤：</p><ol><li><p><strong>创建 Swap 文件</strong>：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">dd</span> <span class="keyword">if</span>=/dev/zero of=/data/swapfile bs=1M count=20480</span><br></pre></td></tr></table></figure><p>解释：</p><ul><li><code>if=/dev/zero</code>：从零设备读取数据，用于创建文件。</li><li><code>of=/data/swapfile</code>：指定目标文件名为 <code>/data/swapfile</code>，挂载在数据盘 <code>/data</code> 下。</li><li><code>bs=1M</code>：设置块大小为1MB。</li><li><code>count=20480</code>：指定文件大小为20GB（32GB内存 &#x2F; 1.6 &#x3D; 20GB）。</li></ul></li><li><p><strong>更改 Swap 文件权限</strong>：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">chmod</span> 600 /data/swapfile</span><br></pre></td></tr></table></figure></li><li><p><strong>设置文件为Swap</strong>：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> mkswap /data/swapfile</span><br></pre></td></tr></table></figure></li><li><p><strong>激活Swap文件</strong>：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> swapon /data/swapfile</span><br></pre></td></tr></table></figure></li></ol><h4 id="3-配置开机自启"><a href="#3-配置开机自启" class="headerlink" title="3. 配置开机自启"></a>3. 配置开机自启</h4><p>为了确保每次系统启动时自动启用 Swap 文件，我们需要在 <code>/etc/fstab</code> 文件中添加一行：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/data/swapfile   none    swap    sw    0   0</span><br></pre></td></tr></table></figure><p>使用以下命令将其添加：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> nano /etc/fstab</span><br></pre></td></tr></table></figure><p>在文件末尾添加上面的一行，并保存退出。</p><h4 id="4-验证-Swap-文件创建成功"><a href="#4-验证-Swap-文件创建成功" class="headerlink" title="4. 验证 Swap 文件创建成功"></a>4. 验证 Swap 文件创建成功</h4><p>最后，我们可以验证 Swap 文件是否创建并成功启用：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">swapon --show</span><br></pre></td></tr></table></figure><p>如果输出中出现 <code>/data/swapfile</code>，则说明创建成功。</p><h4 id="5-总结"><a href="#5-总结" class="headerlink" title="5. 总结"></a>5. 总结</h4><p>通过本文中的步骤，你可以在没有 Swap 分区的服务器上创建一个Swap文件，从而有效缓解内存不足的情况。这种方法适用于内存充足但没有预配置Swap分区的服务器，确保系统稳定运行。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在服务器上创建 Swap 文件是缓解内存不足的一种有效方法。本文介绍了如何在没有 Swap 分区的服务器上创建 Swap 文件，以提供额外的虚拟内存，从而保证系统的稳定性和性能。首先，我们检查系统是否已有 Swap 分区，然后通过创建和配置 Swap 文件来实现虚拟内存的扩展。最后，我们确保 Swap 文件在系统启动时自动启用。通过这些步骤，你可以在没有预配置 Swap 分区的服务器上创建 Swap 文件，确保系统在高负载情况下的稳定运行。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="Mount" scheme="https://freemankevin.uk/tags/Mount/"/>
    
    <category term="Swap" scheme="https://freemankevin.uk/tags/Swap/"/>
    
  </entry>
  
</feed>

<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Kevin&#39;s Notes</title>
  
  <subtitle>Quick notes</subtitle>
  <link href="https://freemankevin.uk/atom.xml" rel="self"/>
  
  <link href="https://freemankevin.uk/"/>
  <updated>2025-01-02T12:38:09.097Z</updated>
  <id>https://freemankevin.uk/</id>
  
  <author>
    <name>Freeman Kevin</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>数据盘挂载与 MBR 分区</title>
    <link href="https://freemankevin.uk/2025/01/01/mount-hd-mbr/"/>
    <id>https://freemankevin.uk/2025/01/01/mount-hd-mbr/</id>
    <published>2025-01-01T01:37:20.000Z</published>
    <updated>2025-01-02T12:38:09.097Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; 本文详细讲解了如何在 Linux 系统中挂载数据盘并使用 MBR 分区表进行分区的完整流程，包括使用 <code>fdisk</code> 创建分区、格式化为 <code>ext4</code> 文件系统，以及配置永久挂载设置。文中提供了从创建挂载点到编辑 <code>/etc/fstab</code> 的具体命令，同时介绍了使用 <code>lsblk</code>、<code>blkid</code> 等工具确认磁盘挂载状态的方法。通过这篇指南，您可以轻松完成数据盘的挂载和管理，确保磁盘稳定性和数据安全性。</p><span id="more"></span><h3 id="划分磁盘"><a href="#划分磁盘" class="headerlink" title="划分磁盘"></a>划分磁盘</h3><p>使用 <code>fdisk</code> 工具对 <code>/dev/sdb</code> 磁盘进行分区：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">fdisk /dev/sdb</span><br></pre></td></tr></table></figure><p>在 <code>fdisk</code> 命令模式下，您可以使用以下命令进行分区：</p><ul><li><code>n</code> 创建新分区</li><li><code>p</code> 选择主分区</li><li><code>1</code> 分区号</li><li>回车两次接受默认的起始和结束扇区</li><li><code>w</code> 保存并退出</li></ul><h3 id="格式化分区"><a href="#格式化分区" class="headerlink" title="格式化分区"></a>格式化分区</h3><p>格式化新创建的分区为 <code>ext4</code> 文件系统：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mkfs.ext4 /dev/sdb1</span><br></pre></td></tr></table></figure><h3 id="挂载分区"><a href="#挂载分区" class="headerlink" title="挂载分区"></a>挂载分区</h3><p>创建挂载点并挂载新分区：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> /data</span><br><span class="line">mount /dev/sdb1 /data</span><br></pre></td></tr></table></figure><h3 id="永久挂载设置"><a href="#永久挂载设置" class="headerlink" title="永久挂载设置"></a>永久挂载设置</h3><p>编辑 <code>/etc/fstab</code> 文件，添加以下行以实现开机自动挂载：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&quot;/dev/sdb1 /data ext4 defaults 0 2&quot;</span> &gt;&gt; /etc/fstab</span><br></pre></td></tr></table></figure><p>运行 <code>mount -a</code> 来挂载所有在 <code>/etc/fstab</code> 中定义的文件系统，确保没有错误：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mount -a</span><br></pre></td></tr></table></figure><h3 id="确认磁盘已成功挂载"><a href="#确认磁盘已成功挂载" class="headerlink" title="确认磁盘已成功挂载"></a>确认磁盘已成功挂载</h3><p>使用 <code>df -Th</code> 查看所有文件系统及其类型：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">df</span> -Th</span><br></pre></td></tr></table></figure><p>这将显示所有挂载的文件系统，您应该能看到 <code>/dev/sdb1</code> 在 <code>/data</code> 目录下。</p><h3 id="查看块设备信息"><a href="#查看块设备信息" class="headerlink" title="查看块设备信息"></a>查看块设备信息</h3><p>使用 <code>lsblk</code> 查看所有块设备的挂载点和大小：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsblk</span><br></pre></td></tr></table></figure><p>确认 <code>/dev/sdb1</code> 是否正确挂载在 <code>/data</code>。</p><h3 id="获取磁盘-UUID"><a href="#获取磁盘-UUID" class="headerlink" title="获取磁盘 UUID"></a>获取磁盘 UUID</h3><p>使用 <code>blkid</code> 获取 <code>/dev/sdb1</code> 的 UUID：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">blkid /dev/sdb1</span><br></pre></td></tr></table></figure><p>这个 UUID 可用于 <code>/etc/fstab</code> 中，以代替设备名，使挂载更稳定：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">UUID=[你的UUID] /data ext4 defaults 0 2</span><br></pre></td></tr></table></figure><p>替换 <code>[你的UUID]</code> 为实际得到的 UUID，然后再次运行 <code>mount -a</code> 确认设置正确。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>以上步骤提供了完整的流程，从分区、格式化到挂载以及确认设置的正确性。务必在每次操作后使用检查命令确认配置的正确性，确保数据盘的稳定性和数据的安全。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 本文详细讲解了如何在 Linux 系统中挂载数据盘并使用 MBR 分区表进行分区的完整流程，包括使用 &lt;code&gt;fdisk&lt;/code&gt; 创建分区、格式化为 &lt;code&gt;ext4&lt;/code&gt; 文件系统，以及配置永久挂载设置。文中提供了从创建挂载点到编辑 &lt;code&gt;/etc/fstab&lt;/code&gt; 的具体命令，同时介绍了使用 &lt;code&gt;lsblk&lt;/code&gt;、&lt;code&gt;blkid&lt;/code&gt; 等工具确认磁盘挂载状态的方法。通过这篇指南，您可以轻松完成数据盘的挂载和管理，确保磁盘稳定性和数据安全性。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="Mount" scheme="https://freemankevin.uk/tags/Mount/"/>
    
    <category term="MBR" scheme="https://freemankevin.uk/tags/MBR/"/>
    
  </entry>
  
  <entry>
    <title>如何优化 Linux 系统性能以支持高并发</title>
    <link href="https://freemankevin.uk/2024/12/27/linux-optimization/"/>
    <id>https://freemankevin.uk/2024/12/27/linux-optimization/</id>
    <published>2024-12-27T07:00:00.000Z</published>
    <updated>2024-12-27T13:51:09.116Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在本篇文章中，我们将详细介绍针对高并发场景下的系统性能瓶颈的优化实践，涉及网络层（TCP参数、Offloading）、文件描述符、CPU调度、内存管理（HugePages）、I&#x2F;O调度、容器化（Docker）以及日志监控等关键技术点，旨在提升系统在高负载下的吞吐量和响应速度。</p><span id="more"></span><blockquote><p>这里以 <code>CentOS 7.9</code> 环境举例。</p></blockquote><h2 id="文件系统"><a href="#文件系统" class="headerlink" title="文件系统"></a>文件系统</h2><h3 id="文件描述符"><a href="#文件描述符" class="headerlink" title="文件描述符"></a>文件描述符</h3><p>在高并发环境中，文件描述符的限制往往成为性能瓶颈。通过配置系统的软硬文件描述符限制，可以避免文件描述符不足的问题。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 配置文件描述符限制</span></span><br><span class="line">grep -qxF <span class="string">&#x27;* soft nofile 1048576&#x27;</span> /etc/security/limits.conf || <span class="built_in">echo</span> <span class="string">&#x27;* soft nofile 1048576&#x27;</span> &gt;&gt; /etc/security/limits.conf</span><br><span class="line">grep -qxF <span class="string">&#x27;* hard nofile 1048576&#x27;</span> /etc/security/limits.conf || <span class="built_in">echo</span> <span class="string">&#x27;* hard nofile 1048576&#x27;</span> &gt;&gt; /etc/security/limits.conf</span><br></pre></td></tr></table></figure><p>在 <code>sysctl.conf</code> 中也需要增加相关配置：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 文件描述符限制</span></span><br><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">fs.file-max = 1048576</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h2 id="CPU"><a href="#CPU" class="headerlink" title="CPU"></a>CPU</h2><h3 id="CPU-调度器"><a href="#CPU-调度器" class="headerlink" title="CPU 调度器"></a>CPU 调度器</h3><p>在多核系统上，可以通过调整 CPU 调度器来提高并发性能。使用 <code>isolcpus</code> 参数将一些 CPU 核心从系统调度中隔离出来，使其专门用于高负载任务，避免系统其他任务和中断占用这些资源。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 隔离特定的 CPU 核心</span></span><br><span class="line">GRUB_CMDLINE_LINUX=<span class="string">&quot;isolcpus=2,3&quot;</span></span><br><span class="line">update-grub</span><br><span class="line">reboot</span><br></pre></td></tr></table></figure><h2 id="内存"><a href="#内存" class="headerlink" title="内存"></a>内存</h2><h3 id="内存大页"><a href="#内存大页" class="headerlink" title="内存大页"></a>内存大页</h3><p>启用 HugePages 可以减少内存分配和释放的开销，尤其对数据库和内存密集型应用非常有利。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看当前 HugePages 配置</span></span><br><span class="line"><span class="built_in">cat</span> /proc/meminfo | grep HugePages</span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置 HugePages 数量</span></span><br><span class="line"><span class="built_in">echo</span> 2048 &gt; /proc/sys/vm/nr_hugepages</span><br><span class="line"></span><br><span class="line"><span class="comment"># 永久配置，修改 /etc/sysctl.conf 或使用 sysctl</span></span><br><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">vm.nr_hugepages = 2048</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h3 id="内存管理"><a href="#内存管理" class="headerlink" title="内存管理"></a>内存管理</h3><p>通过调整内存管理相关参数，可以提高系统性能。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 内存管理</span></span><br><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">vm.drop_caches = 3</span></span><br><span class="line"><span class="string">vm.max_map_count = 262144</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h2 id="内核"><a href="#内核" class="headerlink" title="内核"></a>内核</h2><h3 id="调试与日志"><a href="#调试与日志" class="headerlink" title="调试与日志"></a>调试与日志</h3><p>禁用一些内核调试信息和日志，以减少不必要的系统开销。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 内核调试信息与日志</span></span><br><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">kernel.sysrq = 0</span></span><br><span class="line"><span class="string">kernel.dmesg_restrict = 1</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h3 id="信号量"><a href="#信号量" class="headerlink" title="信号量"></a>信号量</h3><p>调整信号量参数，优化系统性能。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 信号量设置</span></span><br><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">kernel.sem = 250 32000 100 128</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h2 id="网络"><a href="#网络" class="headerlink" title="网络"></a>网络</h2><h3 id="网络中断"><a href="#网络中断" class="headerlink" title="网络中断"></a>网络中断</h3><p>为了提高网络性能，启用 <code>irqbalance</code> 服务，它可以自动平衡网络中断处理，减少单个 CPU 核心的负载，从而提高整体性能。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 启动 irqbalance 服务</span></span><br><span class="line">systemctl <span class="built_in">enable</span> irqbalance</span><br><span class="line">systemctl start irqbalance</span><br></pre></td></tr></table></figure><h3 id="常规配置"><a href="#常规配置" class="headerlink" title="常规配置"></a>常规配置</h3><h4 id="启用-IP-转发"><a href="#启用-IP-转发" class="headerlink" title="启用 IP 转发"></a>启用 IP 转发</h4><p>启用 IP 转发，如果服务器需要作为路由器或网关，则需要启用此选项。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">net.ipv4.ip_forward = 1</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h4 id="禁用-ICMP-重定向"><a href="#禁用-ICMP-重定向" class="headerlink" title="禁用 ICMP 重定向"></a>禁用 ICMP 重定向</h4><p>禁用 ICMP 重定向可以防止中间人攻击，并减少网络流量。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">net.ipv4.conf.all.send_redirects = 0</span></span><br><span class="line"><span class="string">net.ipv4.conf.default.send_redirects = 0</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h4 id="禁用源路由"><a href="#禁用源路由" class="headerlink" title="禁用源路由"></a>禁用源路由</h4><p>禁用源路由是一种安全措施，可以防止攻击者伪造源 IP 地址。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">net.ipv4.conf.all.accept_source_route = 0</span></span><br><span class="line"><span class="string">net.ipv4.conf.default.accept_source_route = 0</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h4 id="禁用接受重定向"><a href="#禁用接受重定向" class="headerlink" title="禁用接受重定向"></a>禁用接受重定向</h4><p>禁用接受重定向也是一种安全措施，类似于禁用发送重定向。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">net.ipv4.conf.all.accept_redirects = 0</span></span><br><span class="line"><span class="string">net.ipv4.conf.default.accept_redirects = 0</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h4 id="禁用安全重定向"><a href="#禁用安全重定向" class="headerlink" title="禁用安全重定向"></a>禁用安全重定向</h4><p>禁用安全重定向，进一步提高网络安全性。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">net.ipv4.conf.all.secure_redirects = 0</span></span><br><span class="line"><span class="string">net.ipv4.conf.default.secure_redirects = 0</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h4 id="忽略-ICMP-广播回显请求"><a href="#忽略-ICMP-广播回显请求" class="headerlink" title="忽略 ICMP 广播回显请求"></a>忽略 ICMP 广播回显请求</h4><p>忽略 ICMP 广播回显请求可以防止 Smurf 攻击。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">net.ipv4.icmp_echo_ignore_broadcasts = 1</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h4 id="忽略错误的-ICMP-错误响应"><a href="#忽略错误的-ICMP-错误响应" class="headerlink" title="忽略错误的 ICMP 错误响应"></a>忽略错误的 ICMP 错误响应</h4><p>忽略错误的 ICMP 错误响应。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">net.ipv4.icmp_ignore_bogus_error_responses = 1</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h4 id="启用反向路径过滤"><a href="#启用反向路径过滤" class="headerlink" title="启用反向路径过滤"></a>启用反向路径过滤</h4><p>启用反向路径过滤，可以防止 IP 地址欺骗。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">net.ipv4.conf.all.rp_filter = 1</span></span><br><span class="line"><span class="string">net.ipv4.conf.default.rp_filter = 1</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h4 id="启用-TCP-同步-Cookie"><a href="#启用-TCP-同步-Cookie" class="headerlink" title="启用 TCP 同步 Cookie"></a>启用 TCP 同步 Cookie</h4><p>启用 TCP 同步 Cookie，可以防止 SYN Flood 攻击。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">net.ipv4.tcp_syncookies = 1</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h4 id="设置本地端口范围"><a href="#设置本地端口范围" class="headerlink" title="设置本地端口范围"></a>设置本地端口范围</h4><p>设置本地端口范围。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">net.ipv4.ip_local_port_range = 1024 65535</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h4 id="设置接收和发送缓冲区大小"><a href="#设置接收和发送缓冲区大小" class="headerlink" title="设置接收和发送缓冲区大小"></a>设置接收和发送缓冲区大小</h4><p>设置接收和发送缓冲区的默认值和最大值，这些值与 TCP 缓冲区大小相关，但影响的是所有 socket。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">net.core.rmem_default = 16777216</span></span><br><span class="line"><span class="string">net.core.rmem_max = 16777216</span></span><br><span class="line"><span class="string">net.core.wmem_default = 16777216</span></span><br><span class="line"><span class="string">net.core.wmem_max = 16777216</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h3 id="TCP-连接管理"><a href="#TCP-连接管理" class="headerlink" title="TCP 连接管理"></a>TCP 连接管理</h3><p>调整 TCP 参数，以优化连接管理。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 优化 TCP 连接管理</span></span><br><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">net.ipv4.tcp_fin_timeout = 30  # 减少 TIME_WAIT 状态持续时间</span></span><br><span class="line"><span class="string">net.ipv4.tcp_tw_reuse = 1  # 启用 TCP 连接复用</span></span><br><span class="line"><span class="string">net.ipv4.tcp_tw_recycle = 1  # 启用 TCP TIME_WAIT 连接快速回收</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h3 id="调整-TCP-参数"><a href="#调整-TCP-参数" class="headerlink" title="调整 TCP 参数"></a>调整 TCP 参数</h3><p>调整 TCP 参数能显著提高网络吞吐量并减少延迟，特别是在高并发场景下。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 启用 TCP 快速打开（TCP Fast Open）</span></span><br><span class="line"><span class="built_in">tee</span> -a /etc/sysctl.conf &lt;&lt; <span class="string">EOF</span></span><br><span class="line"><span class="string">net.ipv4.tcp_fastopen = 3</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 调整 TCP buffer 大小</span></span><br><span class="line"><span class="string">net.core.rmem_max = 16777216</span></span><br><span class="line"><span class="string">net.core.wmem_max = 16777216</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 增大 TCP 监听队列长度，避免连接排队</span></span><br><span class="line"><span class="string">net.ipv4.tcp_max_syn_backlog = 2048</span></span><br><span class="line"><span class="string">EOF</span></span><br></pre></td></tr></table></figure><h3 id="启用-TCP-Offloading"><a href="#启用-TCP-Offloading" class="headerlink" title="启用 TCP Offloading"></a>启用 TCP Offloading</h3><p>现代网卡支持 TCP Offloading 技术，可以将部分 TCP 处理工作交给硬件完成，减轻 CPU 的负担。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看网络设备支持的 Offloading 功能</span></span><br><span class="line">ethtool -S eth0</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启用 TCP Segmentation Offload (TSO)</span></span><br><span class="line">ethtool -K eth0 tso on</span><br></pre></td></tr></table></figure><h2 id="磁盘"><a href="#磁盘" class="headerlink" title="磁盘"></a>磁盘</h2><h3 id="调度器"><a href="#调度器" class="headerlink" title="调度器"></a>调度器</h3><p>在高并发场景中，优化磁盘调度器可以减少 I&#x2F;O 请求的延迟，尤其是在 SSD 上，<code>noop</code> 调度器通常是最合适的。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置磁盘调度器为 noop</span></span><br><span class="line"><span class="built_in">echo</span> noop &gt; /sys/block/vda/queue/scheduler  <span class="comment"># vda 为磁盘名，根据实际情况调整</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 永久配置（通过 udev 或系统服务）</span></span><br><span class="line"><span class="comment"># /etc/udev/rules.d/60-io-scheduler.rules</span></span><br><span class="line">ACTION==<span class="string">&quot;add|change&quot;</span>, KERNEL==<span class="string">&quot;vd[a-z]&quot;</span>, ATTR&#123;queue/scheduler&#125;=<span class="string">&quot;noop&quot;</span></span><br></pre></td></tr></table></figure><h3 id="请求队列"><a href="#请求队列" class="headerlink" title="请求队列"></a>请求队列</h3><p>通过增加磁盘的 I&#x2F;O 请求队列长度，可以提高高并发时的磁盘性能。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看当前磁盘队列长度</span></span><br><span class="line"><span class="built_in">cat</span> /sys/block/vda/queue/nr_requests</span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置更大的队列长度</span></span><br><span class="line"><span class="built_in">echo</span> 128 &gt; /sys/block/vda/queue/nr_requests</span><br></pre></td></tr></table></figure><h2 id="容器化"><a href="#容器化" class="headerlink" title="容器化"></a>容器化</h2><h3 id="服务配置"><a href="#服务配置" class="headerlink" title="服务配置"></a>服务配置</h3><p>为了提升 Docker 容器在高并发环境下的性能，可以采取以下措施：</p><ul><li>使用 <code>overlay2</code> 存储驱动，而不是 <code>aufs</code> 或 <code>devicemapper</code>。</li><li>增加容器的 <code>ulimit</code> 设置，避免因文件描述符限制导致容器崩溃。</li></ul><figure class="highlight json"><table><tr><td class="code"><pre><span class="line"># 修改 Docker 配置文件（/etc/docker/daemon.json）</span><br><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;storage-driver&quot;</span><span class="punctuation">:</span> <span class="string">&quot;overlay2&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;max-concurrent-downloads&quot;</span><span class="punctuation">:</span> <span class="number">10</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;max-concurrent-uploads&quot;</span><span class="punctuation">:</span> <span class="number">10</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><h3 id="资源限制"><a href="#资源限制" class="headerlink" title="资源限制"></a>资源限制</h3><p>为每个容器设置合适的 CPU 和内存限制，可以避免容器过度消耗主机资源，影响整体性能。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 限制容器的 CPU 使用</span></span><br><span class="line">docker run --cpus=<span class="string">&quot;2.0&quot;</span> ...</span><br><span class="line"></span><br><span class="line"><span class="comment"># 限制容器的内存使用</span></span><br><span class="line">docker run --memory=<span class="string">&quot;4g&quot;</span> ...</span><br></pre></td></tr></table></figure><h2 id="日志和监控"><a href="#日志和监控" class="headerlink" title="日志和监控"></a>日志和监控</h2><h3 id="禁用日志"><a href="#禁用日志" class="headerlink" title="禁用日志"></a>禁用日志</h3><p>高并发时，过多的日志记录会增加系统负担，建议禁用不必要的日志，尤其是系统日志。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 停止不必要的日志服务</span></span><br><span class="line">systemctl stop rsyslog</span><br></pre></td></tr></table></figure><h3 id="监控工具"><a href="#监控工具" class="headerlink" title="监控工具"></a>监控工具</h3><p>配置合适的监控工具如 <code>Prometheus</code>、<code>Grafana </code>或 <code>Netdata</code>，可以实时监控系统的各项指标，如 CPU、内存、磁盘、网络等，及时发现性能瓶颈。</p><h2 id="刷新生效"><a href="#刷新生效" class="headerlink" title="刷新生效"></a>刷新生效</h2><pre><code class="shell">sysctl -p# 建议重启sync;reboot</code></pre>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在本篇文章中，我们将详细介绍针对高并发场景下的系统性能瓶颈的优化实践，涉及网络层（TCP参数、Offloading）、文件描述符、CPU调度、内存管理（HugePages）、I&amp;#x2F;O调度、容器化（Docker）以及日志监控等关键技术点，旨在提升系统在高负载下的吞吐量和响应速度。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Container" scheme="https://freemankevin.uk/tags/Container/"/>
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="Optimization" scheme="https://freemankevin.uk/tags/Optimization/"/>
    
  </entry>
  
  <entry>
    <title>如何给 Ubuntu 系统做 LVM 拓展</title>
    <link href="https://freemankevin.uk/2024/12/27/ubuntu-lvm/"/>
    <id>https://freemankevin.uk/2024/12/27/ubuntu-lvm/</id>
    <published>2024-12-27T04:57:25.000Z</published>
    <updated>2024-12-27T13:51:09.116Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在本篇文章中，我们将详细介绍如何在 Ubuntu 系统中扩展 LVM 分区。通过具体演示 Ubuntu 22.04 LTS 环境下的磁盘配置，我们将展示如何检查现有的逻辑卷和卷组状态，并为逻辑卷分配剩余空间。在完成这些步骤后，我们还将调整文件系统大小，以确保能够充分利用新分配的空间。</p><span id="more"></span><blockquote><p>这里以<code>Ubuntu 22.04 LTS</code> 环境举例。</p></blockquote><h3 id="问题场景"><a href="#问题场景" class="headerlink" title="问题场景"></a>问题场景</h3><p>这里是磁盘情况：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">root@ubuntu:~# lsblk</span><br><span class="line">NAME                      MAJ:MIN RM   SIZE RO TYPE MOUNTPOINTS</span><br><span class="line">loop0                       7:0    0  63.9M  1 loop /snap/core20/2318</span><br><span class="line">loop1                       7:1    0  63.7M  1 loop /snap/core20/2434</span><br><span class="line">loop2                       7:2    0    87M  1 loop /snap/lxd/29351</span><br><span class="line">loop3                       7:3    0  89.4M  1 loop /snap/lxd/31333</span><br><span class="line">loop4                       7:4    0  38.8M  1 loop /snap/snapd/21759</span><br><span class="line">loop5                       7:5    0  44.3M  1 loop /snap/snapd/23258</span><br><span class="line">nvme0n1                   259:0    0 931.5G  0 disk</span><br><span class="line">├─nvme0n1p1               259:1    0     1G  0 part /boot/efi</span><br><span class="line">├─nvme0n1p2               259:2    0     2G  0 part /boot</span><br><span class="line">└─nvme0n1p3               259:3    0 928.5G  0 part</span><br><span class="line">  └─ubuntu-vg-ubuntu-lv   253:0    0   100G  0 lvm  /</span><br></pre></td></tr></table></figure><p>可以看到，当前有一个分区 <code>nvme0n1p3</code>，大小为 <code>928.5G</code>，但只使用了 <code>100G</code>，所以需要将现在 lv 空间拓展到100%。</p><h3 id="检查分区和逻辑卷"><a href="#检查分区和逻辑卷" class="headerlink" title="检查分区和逻辑卷"></a>检查分区和逻辑卷</h3><p>运行以下命令检查现有的逻辑卷和卷组状态：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">lsblk</span><br><span class="line">df -Th</span><br><span class="line">sudo vgdisplay</span><br><span class="line">sudo lvdisplay</span><br></pre></td></tr></table></figure><p>确认 <code>ubuntu-vg</code> 是卷组名称，<code>ubuntu-lv</code> 是逻辑卷名称。</p><hr><h3 id="扩展逻辑卷"><a href="#扩展逻辑卷" class="headerlink" title="扩展逻辑卷"></a>扩展逻辑卷</h3><p>为逻辑卷 <code>ubuntu-vg/ubuntu-lv</code> 分配全部剩余空间：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo lvextend -l +100%FREE /dev/ubuntu-vg/ubuntu-lv</span><br></pre></td></tr></table></figure><hr><h3 id="查看文件系统"><a href="#查看文件系统" class="headerlink" title="查看文件系统"></a>查看文件系统</h3><p>运行以下命令查看文件系统类型：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">lsblk -f</span><br><span class="line">df -Th</span><br></pre></td></tr></table></figure><p>输出中会显示 <code>FSTYPE</code> 列，指示逻辑卷（如 <code>/dev/mapper/ubuntu-vg-ubuntu-lv</code>）的文件系统类型。 通常，是<code>ext4</code>。</p><hr><h3 id="5-调整文件系统大小"><a href="#5-调整文件系统大小" class="headerlink" title="5. 调整文件系统大小"></a>5. 调整文件系统大小</h3><p>扩展逻辑卷后，需要调整文件系统大小以使用新分配的空间。</p><h4 id="如果是-Ext4-文件系统："><a href="#如果是-Ext4-文件系统：" class="headerlink" title="如果是 Ext4 文件系统："></a>如果是 Ext4 文件系统：</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo resize2fs /dev/ubuntu-vg/ubuntu-lv</span><br></pre></td></tr></table></figure><h4 id="如果是-XFS-文件系统："><a href="#如果是-XFS-文件系统：" class="headerlink" title="如果是 XFS 文件系统："></a>如果是 XFS 文件系统：</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo xfs_growfs /dev/ubuntu-vg/ubuntu-lv</span><br></pre></td></tr></table></figure><hr><h3 id="6-验证扩展结果"><a href="#6-验证扩展结果" class="headerlink" title="6. 验证扩展结果"></a>6. 验证扩展结果</h3><p>运行以下命令确认 <code>/</code> 的大小已扩展：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">df -Th </span><br><span class="line">lsblk</span><br></pre></td></tr></table></figure><hr><h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><ul><li><strong>备份数据</strong>：执行分区操作前请备份重要数据。</li><li><strong>运行环境</strong>：确保没有其他服务占用相关的磁盘或逻辑卷，以免影响操作。</li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在本篇文章中，我们将详细介绍如何在 Ubuntu 系统中扩展 LVM 分区。通过具体演示 Ubuntu 22.04 LTS 环境下的磁盘配置，我们将展示如何检查现有的逻辑卷和卷组状态，并为逻辑卷分配剩余空间。在完成这些步骤后，我们还将调整文件系统大小，以确保能够充分利用新分配的空间。&lt;/p&gt;</summary>
    
    
    
    <category term="Ubuntu" scheme="https://freemankevin.uk/categories/Ubuntu/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="LVM" scheme="https://freemankevin.uk/tags/LVM/"/>
    
    <category term="Ubuntu" scheme="https://freemankevin.uk/tags/Ubuntu/"/>
    
  </entry>
  
  <entry>
    <title>如何在 Ubuntu 系统上安装 NVIDIA 驱动</title>
    <link href="https://freemankevin.uk/2024/12/27/ubuntu-nvidia-driver/"/>
    <id>https://freemankevin.uk/2024/12/27/ubuntu-nvidia-driver/</id>
    <published>2024-12-27T03:23:25.000Z</published>
    <updated>2024-12-27T13:51:09.117Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;本文详细介绍了如何在 Ubuntu 系统中安装和配置 NVIDIA 显卡驱动，特别是针对多卡配置（如双卡 SLI 或 CUDA）。通过步骤化指导，从确认硬件兼容性、安装必要依赖、添加官方 PPA 到安装驱动，确保驱动正确安装并验证。还包括如何配置多卡环境（SLI 或 CUDA），以及常见问题的解决方法，帮助用户充分发挥 NVIDIA 显卡的性能。</p><span id="more"></span><blockquote><p>这里以<code>Ubuntu 22.04 LTS</code> 环境举例。</p></blockquote><p><strong>硬件配置</strong>： <code>Intel 酷睿i9 14900K 24核心32线程 主频2.4G 128G DDR5 1T固态硬盘 NVIDIA Geforce RTX4090D 24G *2</code></p><h3 id="考虑因素"><a href="#考虑因素" class="headerlink" title="考虑因素"></a>考虑因素</h3><ol><li>显卡型号: <code>NVIDIA Geforce RTX4090D</code>，驱动版本需要与硬件匹配。</li><li>显卡数量: <code>*2</code> 双卡配置，需要支持双卡。</li><li>驱动版本: 可以通过<a href="https://www.nvidia.com/Download/index.aspx">官方网站</a>来查询。</li><li>操作系统: <code>Ubuntu 22.04 LTS</code> , 需要server 版而非 desktop 版，以免不稳定。另外，没有特殊需要，不安装桌面。</li></ol><hr><h3 id="确认系统和硬件信息"><a href="#确认系统和硬件信息" class="headerlink" title="确认系统和硬件信息"></a>确认系统和硬件信息</h3><p>运行以下命令确认你的 NVIDIA 显卡是否被识别：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">lspci | grep -i nvidia</span><br></pre></td></tr></table></figure><p>检查当前是否已安装 NVIDIA 驱动：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">nvidia-smi</span><br></pre></td></tr></table></figure><p>如果没有显示正确的驱动信息，继续以下步骤。</p><hr><h3 id="更新系统并安装必要工具"><a href="#更新系统并安装必要工具" class="headerlink" title="更新系统并安装必要工具"></a>更新系统并安装必要工具</h3><p>更新软件包并安装依赖：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt update &amp;&amp; sudo apt upgrade -y</span><br><span class="line">sudo apt install -y build-essential dkms gcc make</span><br></pre></td></tr></table></figure><hr><h3 id="添加官方-NVIDIA-驱动-PPA"><a href="#添加官方-NVIDIA-驱动-PPA" class="headerlink" title="添加官方 NVIDIA 驱动 PPA"></a>添加官方 NVIDIA 驱动 PPA</h3><p>使用官方 PPA 安装 NVIDIA 驱动：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo add-apt-repository ppa:graphics-drivers/ppa</span><br><span class="line">sudo apt update</span><br></pre></td></tr></table></figure><hr><h3 id="安装-NVIDIA-驱动"><a href="#安装-NVIDIA-驱动" class="headerlink" title="安装 NVIDIA 驱动"></a>安装 NVIDIA 驱动</h3><p>查看推荐的 NVIDIA 驱动版本：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">ubuntu-drivers devices</span><br></pre></td></tr></table></figure><p>根据输出选择推荐的驱动版本（例如 <code>nvidia-driver-535</code>），然后运行：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt install nvidia-driver-535</span><br></pre></td></tr></table></figure><blockquote><p><strong>注意</strong>：如果你知道具体需要的驱动版本，也可以替换为对应的版本号。</p></blockquote><hr><h3 id="验证驱动安装"><a href="#验证驱动安装" class="headerlink" title="验证驱动安装"></a>验证驱动安装</h3><p>安装完成后，重启系统：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo sync;reboot</span><br></pre></td></tr></table></figure><p>然后检查 NVIDIA 驱动是否正常工作：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">nvidia-smi</span><br></pre></td></tr></table></figure><p>如果输出显示 GPU 信息，则驱动安装成功。</p><hr><h3 id="多卡配置（SLI-或-CUDA）"><a href="#多卡配置（SLI-或-CUDA）" class="headerlink" title="多卡配置（SLI 或 CUDA）"></a>多卡配置（SLI 或 CUDA）</h3><p>如果你计划使用双卡的 SLI 模式（用于渲染加速）或 CUDA（用于深度学习或计算），请根据用途安装附加工具。</p><h4 id="安装-CUDA-工具包"><a href="#安装-CUDA-工具包" class="headerlink" title="安装 CUDA 工具包"></a>安装 CUDA 工具包</h4><p>安装 CUDA 工具包和开发环境：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt install -y nvidia-cuda-toolkit</span><br></pre></td></tr></table></figure><p>检查 CUDA 是否可用：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">nvcc --version</span><br></pre></td></tr></table></figure><h4 id="配置-SLI（可选）"><a href="#配置-SLI（可选）" class="headerlink" title="配置 SLI（可选）"></a>配置 SLI（可选）</h4><p>确保你的主板支持 SLI，并在 BIOS 中启用相关功能。安装 NVIDIA 控制面板工具：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt install nvidia-settings</span><br></pre></td></tr></table></figure><p>运行 NVIDIA 设置工具配置 SLI：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo nvidia-settings</span><br></pre></td></tr></table></figure><hr><h3 id="常见问题排查"><a href="#常见问题排查" class="headerlink" title="常见问题排查"></a>常见问题排查</h3><ul><li><p><strong>黑屏或无法进入桌面</strong>：<br>（如果你安装了桌面）按 <code>Ctrl+Alt+F2</code> 进入终端，然后重新安装驱动：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt purge nvidia*</span><br><span class="line">sudo apt install nvidia-driver-535</span><br></pre></td></tr></table></figure></li><li><p><strong>需要最新驱动</strong>：<br>如果官方 PPA 不包含最新驱动，可以从 NVIDIA 官方下载 <code>.run</code> 文件手动安装：</p><ol><li><p>下载对应版本驱动：<a href="https://www.nvidia.com/Download/index.aspx">NVIDIA 官网</a>。</p></li><li><p>禁用当前驱动：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo systemctl stop gdm</span><br><span class="line">sudo systemctl stop lightdm</span><br></pre></td></tr></table></figure></li><li><p>安装驱动：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo bash NVIDIA-Linux-x86_64-*.run</span><br></pre></td></tr></table></figure></li></ol></li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;本文详细介绍了如何在 Ubuntu 系统中安装和配置 NVIDIA 显卡驱动，特别是针对多卡配置（如双卡 SLI 或 CUDA）。通过步骤化指导，从确认硬件兼容性、安装必要依赖、添加官方 PPA 到安装驱动，确保驱动正确安装并验证。还包括如何配置多卡环境（SLI 或 CUDA），以及常见问题的解决方法，帮助用户充分发挥 NVIDIA 显卡的性能。&lt;/p&gt;</summary>
    
    
    
    <category term="Ubuntu" scheme="https://freemankevin.uk/categories/Ubuntu/"/>
    
    
    <category term="Ubuntu" scheme="https://freemankevin.uk/tags/Ubuntu/"/>
    
    <category term="Driver" scheme="https://freemankevin.uk/tags/Driver/"/>
    
    <category term="NVIDIA" scheme="https://freemankevin.uk/tags/NVIDIA/"/>
    
  </entry>
  
  <entry>
    <title>如何给 Ubuntu 系统配置国内源</title>
    <link href="https://freemankevin.uk/2024/12/27/ubuntu-repo/"/>
    <id>https://freemankevin.uk/2024/12/27/ubuntu-repo/</id>
    <published>2024-12-27T02:45:25.000Z</published>
    <updated>2024-12-27T13:51:09.117Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在本文中，我们将介绍如何将 Ubuntu 系统的默认源替换为国内镜像源，以加速软件包的下载速度。以清华源为例，我们提供了详细的步骤，从备份原有源配置、编辑源文件到更新软件包索引等操作。此外，还介绍了如何使用阿里云、网易云和中科大的源地址作为替代，帮助用户提高软件安装和系统更新的效率。</p><span id="more"></span><blockquote><p>这里以<code>Ubuntu 22.04 LTS</code> 举例。 </p></blockquote><h3 id="备份原有的源配置"><a href="#备份原有的源配置" class="headerlink" title="备份原有的源配置"></a>备份原有的源配置</h3><p>首先，备份当前的源配置文件：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo cp /etc/apt/sources.list&#123;,.bak&#125;</span><br></pre></td></tr></table></figure><hr><h3 id="编辑源配置文件"><a href="#编辑源配置文件" class="headerlink" title="编辑源配置文件"></a>编辑源配置文件</h3><p>使用文本编辑器打开源配置文件：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo vim /etc/apt/sources.list # 如果没有vim ，可以使用nano</span><br></pre></td></tr></table></figure><hr><h3 id="替换为清华源"><a href="#替换为清华源" class="headerlink" title="替换为清华源"></a>替换为清华源</h3><p>将文件内容替换为以下清华源配置：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">tsinghua repo <span class="keyword">for</span> Ubuntu 22.04 LTS</span> </span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy main restricted universe multiverse</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-updates main restricted universe multiverse</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-backports main restricted universe multiverse</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-security main restricted universe multiverse</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">tsinghua src repo <span class="keyword">for</span> Ubuntu 22.04 LTS</span> </span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy main restricted universe multiverse</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-updates main restricted universe multiverse</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-backports main restricted universe multiverse</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-security main restricted universe multiverse</span></span><br></pre></td></tr></table></figure><p>保存并退出编辑器。</p><hr><h3 id="更新软件包索引"><a href="#更新软件包索引" class="headerlink" title="更新软件包索引"></a>更新软件包索引</h3><p>运行以下命令更新软件包索引，以验证新的源配置是否正常：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt update </span><br></pre></td></tr></table></figure><p>如果新的源配置正确，应该能够成功拉取软件包列表。</p><hr><h3 id="升级系统（可选）"><a href="#升级系统（可选）" class="headerlink" title="升级系统（可选）"></a>升级系统（可选）</h3><p>如果需要升级系统软件，可以运行以下命令：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt upgrade</span><br></pre></td></tr></table></figure><hr><h3 id="验证新源是否生效"><a href="#验证新源是否生效" class="headerlink" title="验证新源是否生效"></a>验证新源是否生效</h3><p>查看下载软件包时的源地址：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt install &#x27;任意软件包名称&#x27; -y   # 正常会显示来自： mirrors.tuna.tsinghua.edu.cn</span><br></pre></td></tr></table></figure><hr><h3 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h3><ul><li><p>无法访问清华源: 检查网络连通性，使用以下命令测试：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">ping mirrors.tuna.tsinghua.edu.cn</span><br></pre></td></tr></table></figure></li><li><p><strong>需要替换为其他国内镜像源</strong>：例如阿里云、网易或中科大等，可以根据需要修改为对应的镜像地址。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">阿里云</span></span><br><span class="line">sudo sed -i &#x27;s|http://archive.ubuntu.com|http://mirrors.aliyun.com|g&#x27; /etc/apt/sources.list</span><br><span class="line">sudo sed -i &#x27;s|http://security.ubuntu.com|http://mirrors.aliyun.com|g&#x27; /etc/apt/sources.list</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">网易云</span></span><br><span class="line">sudo sed -i &#x27;s|http://archive.ubuntu.com|http://mirrors.163.com|g&#x27; /etc/apt/sources.list</span><br><span class="line">sudo sed -i &#x27;s|http://security.ubuntu.com|http://mirrors.163.com|g&#x27; /etc/apt/sources.list</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">中科大</span></span><br><span class="line">sudo sed -i &#x27;s|http://archive.ubuntu.com|http://mirrors.ustc.edu.cn|g&#x27; /etc/apt/sources.list</span><br><span class="line">sudo sed -i &#x27;s|http://security.ubuntu.com|http://mirrors.ustc.edu.cn|g&#x27; /etc/apt/sources.list</span><br></pre></td></tr></table></figure></li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在本文中，我们将介绍如何将 Ubuntu 系统的默认源替换为国内镜像源，以加速软件包的下载速度。以清华源为例，我们提供了详细的步骤，从备份原有源配置、编辑源文件到更新软件包索引等操作。此外，还介绍了如何使用阿里云、网易云和中科大的源地址作为替代，帮助用户提高软件安装和系统更新的效率。&lt;/p&gt;</summary>
    
    
    
    <category term="Ubuntu" scheme="https://freemankevin.uk/categories/Ubuntu/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="Ubuntu" scheme="https://freemankevin.uk/tags/Ubuntu/"/>
    
    <category term="Netplan" scheme="https://freemankevin.uk/tags/Netplan/"/>
    
  </entry>
  
  <entry>
    <title>如何给 Ubuntu 系统配置静态 IP 地址</title>
    <link href="https://freemankevin.uk/2024/12/27/ubuntu-netplan/"/>
    <id>https://freemankevin.uk/2024/12/27/ubuntu-netplan/</id>
    <published>2024-12-27T02:21:25.000Z</published>
    <updated>2024-12-27T13:51:09.117Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;本文将详细介绍如何在 Ubuntu 系统中通过 <code>Netplan</code> 配置静态 IP 地址。<code>Netplan</code> 是当前 Ubuntu 默认的网络管理工具，支持简便的 YAML 配置文件格式。在这篇文章中，你将学习如何查找网卡名称、编辑 <code>Netplan</code> 配置文件、设置静态 IP、应用配置并进行验证。通过本教程，你可以确保服务器或工作站的网络配置稳定可靠。</p><span id="more"></span><blockquote><p>这里以<code>Ubuntu 22.04 LTS</code> 环境举例。</p></blockquote><h3 id="查找网络接口名称"><a href="#查找网络接口名称" class="headerlink" title="查找网络接口名称"></a>查找网络接口名称</h3><p>运行以下命令查看网卡名称：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">ip a</span><br></pre></td></tr></table></figure><p>通常，网卡名称类似于 <code>eth0</code>、<code>enp0s3</code> 或 <code>ens33</code>。</p><h3 id="编辑-Netplan-配置文件"><a href="#编辑-Netplan-配置文件" class="headerlink" title="编辑 Netplan 配置文件"></a>编辑 Netplan 配置文件</h3><p><code>Netplan</code> 的配置文件通常位于 <code>/etc/netplan/</code> 目录下，并以 <code>.yaml</code> 结尾。</p><p>列出配置文件：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">cd /etc/netplan/</span><br><span class="line"></span><br><span class="line">ls -lash *.yaml</span><br></pre></td></tr></table></figure><p>假设文件名为 <code>50-cloud.yaml</code>，用文本编辑器打开它：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo vim /etc/netplan/50-cloud.yaml # 没有vim，可以使用nano</span><br></pre></td></tr></table></figure><hr><h3 id="配置静态-IP"><a href="#配置静态-IP" class="headerlink" title="配置静态 IP"></a>配置静态 IP</h3><p>将文件内容修改为类似以下配置：</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">network:</span></span><br><span class="line">  <span class="attr">version:</span> <span class="number">2</span></span><br><span class="line">  <span class="attr">ethernets:</span></span><br><span class="line">    <span class="attr">enp0s3:</span>  <span class="comment"># 替换为你的网络接口名称</span></span><br><span class="line">      <span class="attr">addresses:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="number">192.168</span><span class="number">.1</span><span class="number">.100</span><span class="string">/24</span>  <span class="comment"># 设置静态IP地址和子网掩码</span></span><br><span class="line">      <span class="attr">gateway4:</span> <span class="number">192.168</span><span class="number">.1</span><span class="number">.1</span>  <span class="comment"># 设置网关</span></span><br><span class="line">      <span class="attr">nameservers:</span></span><br><span class="line">        <span class="attr">addresses:</span></span><br><span class="line">          <span class="bullet">-</span> <span class="number">8.8</span><span class="number">.8</span><span class="number">.8</span>           <span class="comment"># 设置首选DNS</span></span><br><span class="line">          <span class="bullet">-</span> <span class="number">8.8</span><span class="number">.4</span><span class="number">.4</span>           <span class="comment"># 设置备用DNS</span></span><br><span class="line">      <span class="attr">dhcp4:</span> <span class="literal">false</span>            <span class="comment"># 禁用 DHCP</span></span><br></pre></td></tr></table></figure><blockquote><p><strong>注意</strong>： </p><ul><li><code>192.168.1.100</code> 是静态 IP 地址，请替换为适合你网络的 IP。</li><li><code>192.168.1.1</code> 是网关地址，请根据你的网络实际情况修改。</li></ul></blockquote><hr><h3 id="应用配置"><a href="#应用配置" class="headerlink" title="应用配置"></a>应用配置</h3><p>保存文件后，应用 <code>Netplan</code> 配置：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo netplan apply </span><br></pre></td></tr></table></figure><p>如果没有报错，静态 IP 应该已经生效。</p><p>这里最好重启下机器以确保完全生效。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo sync;reboot</span><br></pre></td></tr></table></figure><hr><h3 id="验证网络配置"><a href="#验证网络配置" class="headerlink" title="验证网络配置"></a>验证网络配置</h3><p>使用以下命令检查网络配置是否正确：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">ip a</span><br></pre></td></tr></table></figure><p>查看是否分配了静态 IP。</p><p>测试网络连通性：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">ping 8.8.8.8</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">ping www.google.com</span> </span><br><span class="line">ping baidu.com</span><br></pre></td></tr></table></figure><hr><h3 id="常见问题排查"><a href="#常见问题排查" class="headerlink" title="常见问题排查"></a>常见问题排查</h3><p>执行下面命令</p>  <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo netplan apply</span><br></pre></td></tr></table></figure><ul><li><p>如果遇到错误：</p><ul><li>检查 <code>.yaml</code> 文件是否缩进正确（使用空格，不要用 Tab）。</li><li>确保接口名称正确无误。</li></ul></li><li><p>如果网络不可用：</p><ul><li>检查网关和 DNS 是否正确。</li><li>确认 IP 地址没有与网络中的其他设备冲突。</li></ul></li></ul><hr><p>如果你的服务器使用的是 WiFi 而非有线连接，请参考 WiFi 的配置方法。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;本文将详细介绍如何在 Ubuntu 系统中通过 &lt;code&gt;Netplan&lt;/code&gt; 配置静态 IP 地址。&lt;code&gt;Netplan&lt;/code&gt; 是当前 Ubuntu 默认的网络管理工具，支持简便的 YAML 配置文件格式。在这篇文章中，你将学习如何查找网卡名称、编辑 &lt;code&gt;Netplan&lt;/code&gt; 配置文件、设置静态 IP、应用配置并进行验证。通过本教程，你可以确保服务器或工作站的网络配置稳定可靠。&lt;/p&gt;</summary>
    
    
    
    <category term="Ubuntu" scheme="https://freemankevin.uk/categories/Ubuntu/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="Ubuntu" scheme="https://freemankevin.uk/tags/Ubuntu/"/>
    
    <category term="Netplan" scheme="https://freemankevin.uk/tags/Netplan/"/>
    
  </entry>
  
  <entry>
    <title>数据盘挂载与 GPT 分区</title>
    <link href="https://freemankevin.uk/2024/12/26/mount-hd-gpt/"/>
    <id>https://freemankevin.uk/2024/12/26/mount-hd-gpt/</id>
    <published>2024-12-26T04:00:00.000Z</published>
    <updated>2024-12-26T13:08:04.641Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在 Linux 系统中，磁盘分区和挂载是常见的管理任务，尤其在添加新的数据盘时。对于大于 2TB 的磁盘，通常需要使用 GPT（GUID Partition Table）分区模式，因为传统的 MBR（Master Boot Record）分区表不支持超过 2TB 的磁盘容量。在本文中，我们将展示如何使用 GPT 分区表创建分区，格式化为 <code>ext4</code> 文件系统，并挂载到指定目录。此外，还会介绍如何配置系统，以确保在系统重启后能够自动挂载磁盘。最终，您将学会如何有效地管理 Linux 系统中的大容量磁盘，确保数据安全与系统稳定。</p><span id="more"></span><h3 id="创建-GPT-分区表"><a href="#创建-GPT-分区表" class="headerlink" title="创建 GPT 分区表"></a>创建 GPT 分区表</h3><p>使用 <code>parted</code> 工具在 <code>/dev/sdb</code> 磁盘上创建一个新的 GPT 分区表：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">parted /dev/sdb mklabel gpt</span><br></pre></td></tr></table></figure><h3 id="创建分区"><a href="#创建分区" class="headerlink" title="创建分区"></a>创建分区</h3><p>创建一个占用整个磁盘的主分区，并格式化为 <code>ext4</code> 文件系统：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">parted /dev/sdb mkpart primary ext4 0% 100%</span><br></pre></td></tr></table></figure><p>等待分区操作完成。</p><h3 id="格式化分区"><a href="#格式化分区" class="headerlink" title="格式化分区"></a>格式化分区</h3><p>格式化新创建的分区 <code>/dev/sdb1</code>：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mkfs.ext4 /dev/sdb1</span><br></pre></td></tr></table></figure><h3 id="挂载分区"><a href="#挂载分区" class="headerlink" title="挂载分区"></a>挂载分区</h3><p>创建一个挂载点 <code>/data</code> 并挂载新分区：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mkdir /data</span><br><span class="line">mount /dev/sdb1 /data</span><br></pre></td></tr></table></figure><h3 id="永久挂载设置"><a href="#永久挂载设置" class="headerlink" title="永久挂载设置"></a>永久挂载设置</h3><p>为了使挂载在启动时自动进行，需要编辑 <code>/etc/fstab</code> 文件：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">echo &quot;/dev/sdb1 /data ext4 defaults 0 2&quot; &gt;&gt; /etc/fstab</span><br></pre></td></tr></table></figure><p>运行以下命令以验证配置是否正确：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mount -a</span><br></pre></td></tr></table></figure><h3 id="确认磁盘已成功挂载"><a href="#确认磁盘已成功挂载" class="headerlink" title="确认磁盘已成功挂载"></a>确认磁盘已成功挂载</h3><p>使用 <code>df -Th</code> 命令查看所有文件系统及其类型，确认 <code>/dev/sdb1</code> 是否正确挂载在 <code>/data</code> 下：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">df -Th</span><br></pre></td></tr></table></figure><h3 id="查看块设备信息"><a href="#查看块设备信息" class="headerlink" title="查看块设备信息"></a>查看块设备信息</h3><p>使用 <code>lsblk</code> 命令查看所有块设备的挂载点和大小，确认挂载情况：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">lsblk</span><br></pre></td></tr></table></figure><h3 id="获取磁盘-UUID"><a href="#获取磁盘-UUID" class="headerlink" title="获取磁盘 UUID"></a>获取磁盘 UUID</h3><p>使用 <code>blkid</code> 命令获取 <code>/dev/sdb1</code> 的 UUID，这有助于 <code>/etc/fstab</code> 中的稳定性配置：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">blkid /dev/sdb1</span><br></pre></td></tr></table></figure><p>可以使用这个 UUID 替换 <code>/etc/fstab</code> 中的设备名，以确保即使设备名变化，挂载依然稳定：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">echo &quot;UUID=$(blkid -s UUID -o value /dev/sdb1) /data ext4 defaults 0 2&quot; &gt;&gt; /etc/fstab</span><br></pre></td></tr></table></figure><hr><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>此文档提供了从创建 GPT 分区表到挂载和确认分区的完整步骤。在每一步操作后，建议使用校验命令（如 <code>lsblk</code> 和 <code>df -Th</code>）确保配置的正确性。这种分区和挂载方式可以适应多种场景，并确保数据存储的安全与稳定。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在 Linux 系统中，磁盘分区和挂载是常见的管理任务，尤其在添加新的数据盘时。对于大于 2TB 的磁盘，通常需要使用 GPT（GUID Partition Table）分区模式，因为传统的 MBR（Master Boot Record）分区表不支持超过 2TB 的磁盘容量。在本文中，我们将展示如何使用 GPT 分区表创建分区，格式化为 &lt;code&gt;ext4&lt;/code&gt; 文件系统，并挂载到指定目录。此外，还会介绍如何配置系统，以确保在系统重启后能够自动挂载磁盘。最终，您将学会如何有效地管理 Linux 系统中的大容量磁盘，确保数据安全与系统稳定。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="Mount" scheme="https://freemankevin.uk/tags/Mount/"/>
    
    <category term="GPT" scheme="https://freemankevin.uk/tags/GPT/"/>
    
  </entry>
  
  <entry>
    <title>如何优化 PostgreSQL 数据库以支持高并发</title>
    <link href="https://freemankevin.uk/2024/12/26/postgresql-optimization/"/>
    <id>https://freemankevin.uk/2024/12/26/postgresql-optimization/</id>
    <published>2024-12-26T03:34:15.000Z</published>
    <updated>2024-12-27T13:51:09.116Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;本文将介绍如何优化 PostgreSQL 数据库以支持高并发场景。我们会针对内存缓存、连接数、索引、日志、查询优化等方面进行详细的优化建议，以帮助提升数据库的性能。</p><span id="more"></span><blockquote><p>这里以16C 核心、32GB 物理内存和 20GB 虚拟内存的服务器举例。</p></blockquote><h2 id="常规考虑"><a href="#常规考虑" class="headerlink" title="常规考虑"></a>常规考虑</h2><h3 id="共享缓冲区"><a href="#共享缓冲区" class="headerlink" title="共享缓冲区"></a>共享缓冲区</h3><ul><li><p><strong>作用</strong>：<code>shared_buffers</code> 是 PostgreSQL 用来缓存数据页的内存区域。如果这个值设置得过小，数据库需要频繁访问磁盘，导致性能下降。</p></li><li><p><strong>优化建议</strong>：设置为物理内存的 25%-40%。对于 32GB 的物理内存，可以设置为 8GB 到 12GB 之间。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">shared_buffers = 8GB  # 可根据内存进行调整</span><br></pre></td></tr></table></figure></li></ul><h3 id="工作内存"><a href="#工作内存" class="headerlink" title="工作内存"></a>工作内存</h3><ul><li><p><strong>作用</strong>：<code>work_mem</code> 定义了单个查询操作（如排序、哈希连接等）所能使用的内存。对于大查询，适当增加 <code>work_mem</code> 可以减少磁盘交换。</p></li><li><p><strong>优化建议</strong>: 增加每个查询的内存使用量，但不要过高，以免消耗过多内存。一般可以设置为 4MB 到 16MB，具体根据查询复杂度调整。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">work_mem = 16MB  # 根据实际负载进行调整</span><br></pre></td></tr></table></figure></li></ul><h3 id="维护工作内存"><a href="#维护工作内存" class="headerlink" title="维护工作内存"></a>维护工作内存</h3><ul><li><p><strong>作用</strong>：<code>maintenance_work_mem</code> 定义了 PostgreSQL 用于执行某些维护操作（如创建索引、VACUUM、分析等）的内存大小。增加此值可以加速这些操作。</p></li><li><p><strong>优化建议</strong>: 增加该值可以提高 <code>VACUUM</code> 和索引创建的速度。对于 32GB 内存的机器，可以设置为 512MB 到 1GB。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">maintenance_work_mem = 512MB</span><br></pre></td></tr></table></figure></li></ul><h3 id="自动工作内存"><a href="#自动工作内存" class="headerlink" title="自动工作内存"></a>自动工作内存</h3><ul><li><p><strong>作用</strong>：<code>effective_cache_size</code> 告诉 PostgreSQL 查询优化器可用的操作系统缓存大小，以帮助它估计缓存命中率。</p></li><li><p><strong>优化建议</strong>: 设置为系统中可用于缓存的数据量，通常为系统总内存的 50%-75%。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">effective_cache_size = 24GB  # 根据实际可用内存设置</span><br></pre></td></tr></table></figure></li></ul><h3 id="最大连接数"><a href="#最大连接数" class="headerlink" title="最大连接数"></a>最大连接数</h3><ul><li><p><strong>作用</strong>：<code>max_connections</code> 定义了数据库可以同时接入的最大客户端连接数。如果这个值设置得过高，可能导致内存和 CPU 资源过度消耗。</p></li><li><p><strong>优化建议</strong>: 适当增加最大连接数，但要确保服务器有足够的内存来处理这些连接。假设你需要支持 500-1000 个并发连接，通常设置在 200 到 500 之间。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">max_connections = 500</span><br></pre></td></tr></table></figure></li></ul><h3 id="WAL-日志设置"><a href="#WAL-日志设置" class="headerlink" title="WAL 日志设置"></a>WAL 日志设置</h3><ul><li><p><strong>作用</strong>：<code>wal_buffers</code> 控制 PostgreSQL 在写入 WAL（Write-Ahead Logging）日志时的缓冲区大小。如果设置得过小，会影响数据库写入性能。</p></li><li><p><strong>优化建议</strong>: 对于大型数据库，设置一个更大的 <code>wal_buffers</code> 可以提高写入性能。可以考虑将其设置为 16MB 或更大。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">wal_buffers = 16MB</span><br></pre></td></tr></table></figure></li></ul><h3 id="WAL-日志检查点频率"><a href="#WAL-日志检查点频率" class="headerlink" title="WAL 日志检查点频率"></a>WAL 日志检查点频率</h3><ul><li><p><strong>作用</strong>：<code>checkpoint_segments</code> 决定了日志切换点之间可以有多少个日志文件。如果这个值设置得太小，可能会导致频繁的磁盘 I&#x2F;O 操作。</p></li><li><p><strong>优化建议</strong>：</p><ul><li>增加 <code>checkpoint_segments</code> 可以减少 checkpoint 的频率，从而减少磁盘 I&#x2F;O。</li><li>默认的 <code>checkpoint_timeout</code> 可以保持在 5 分钟左右。<code>checkpoint_completion_target</code> 控制检查点完成的目标时间，如果系统负载较高，考虑将其设为较低值。</li></ul><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">checkpoint_timeout = 5min</span><br><span class="line">checkpoint_completion_target = 0.9</span><br><span class="line">checkpoint_segments = 64</span><br></pre></td></tr></table></figure></li></ul><h3 id="同步提交"><a href="#同步提交" class="headerlink" title="同步提交"></a>同步提交</h3><ul><li><p><strong>作用</strong>：<code>synchronous_commit</code> 控制事务是否需要等待 WAL 日志完全写入磁盘。默认情况下，这个参数是启用的，会确保数据一致性，但也可能影响性能。</p></li><li><p><strong>优化建议</strong>：如果对数据一致性要求不是极其严格，可以考虑设置为 <code>off</code>，以提高写入性能。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">synchronous_commit = off</span><br></pre></td></tr></table></figure></li></ul><h3 id="并行查询设置"><a href="#并行查询设置" class="headerlink" title="并行查询设置"></a>并行查询设置</h3><ul><li><p><strong>作用</strong>：控制单个查询操作中使用的最大并行工作进程数。在多核 CPU 上启用并行查询可以大大提高查询性能。</p></li><li><p><strong>优化建议</strong>：在较多核心的机器上，可以适当增加该值以加速查询。假设你有多个核心，可以设置为 4 或更高。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">max_parallel_workers_per_gather = 4</span><br></pre></td></tr></table></figure></li></ul><h3 id="查询优化器设置"><a href="#查询优化器设置" class="headerlink" title="查询优化器设置"></a>查询优化器设置</h3><ul><li><p><strong>作用</strong>：<code>random_page_cost</code> 和 <code>seq_page_cost</code> 控制查询优化器对磁盘访问的成本估算。调优这两个参数可以帮助优化器做出更好的查询计划。</p></li><li><p><strong>优化建议</strong>：如果使用的是 SSD 硬盘，<code>random_page_cost</code> 和 <code>seq_page_cost</code> 可以适当降低，因为 SSD 具有较低的随机访问延迟。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">random_page_cost = 1.1  # 如果使用 SSD 硬盘</span><br><span class="line">seq_page_cost = 1.0</span><br></pre></td></tr></table></figure></li></ul><h3 id="日志记录设置"><a href="#日志记录设置" class="headerlink" title="日志记录设置"></a>日志记录设置</h3><ul><li><p><strong>作用</strong>：该参数控制哪些查询会被记录到日志中。设置该参数可以帮助你识别慢查询，进行进一步的优化。</p></li><li><p><strong>优化建议</strong>：设置一个较低的时间阈值（例如 500ms），以记录可能导致性能瓶颈的慢查询。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">log_min_duration_statement = 500  # 记录执行超过500ms的查询</span><br></pre></td></tr></table></figure></li></ul><h3 id="自动清理设置"><a href="#自动清理设置" class="headerlink" title="自动清理设置"></a>自动清理设置</h3><ul><li><p><strong>作用</strong>：<code>autovacuum</code> 用于自动清理死行（dead tuples）并进行数据库的碎片整理。如果未适当配置，可能会导致数据库膨胀，影响查询性能。</p></li><li><p><strong>优化建议</strong>：确保启用 <code>autovacuum</code> 并根据负载调整其参数，以确保定期清理死行和索引碎片。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">autovacuum = on</span><br><span class="line">autovacuum_vacuum_cost_delay = 20ms  # 减少自动清理对性能的影响</span><br><span class="line">autovacuum_vacuum_scale_factor = 0.2  # 当表数据增长 20% 时触发自动清理</span><br></pre></td></tr></table></figure></li></ul><h3 id="优化-IO-操作"><a href="#优化-IO-操作" class="headerlink" title="优化 IO 操作"></a>优化 IO 操作</h3><ul><li><p><strong>作用</strong>：<code>effective_io_concurrency</code> 控制 IO 操作的并发级别，对于多磁盘系统和使用 RAID 或 SSD 时特别有用。</p></li><li><p><strong>优化建议</strong>：如果使用 SSD 或 RAID 磁盘阵列，适当增加并发级别。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">effective_io_concurrency = 200  # 根据磁盘性能进行调整</span><br></pre></td></tr></table></figure></li></ul><h3 id="分离存储路径"><a href="#分离存储路径" class="headerlink" title="分离存储路径"></a>分离存储路径</h3><ul><li><p><strong>作用</strong>：将日志存储路径与数据存储路径分开，可以减少磁盘 I&#x2F;O 竞争，提升数据库的写入性能，尤其是在高并发写入时，能有效减少 I&#x2F;O 阻塞。</p></li><li><p><strong>优化建议</strong>： </p><ul><li>将数据存储路径和 WAL 日志存储路径分开，最好使用不同的磁盘或分区。</li><li>将 WAL 日志存储在 SSD 上，数据存储在机械硬盘（HDD）上。</li></ul><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">数据存储路径</span></span><br><span class="line">data_directory = &#x27;/data/postgresql/data&#x27;  # 数据存储在磁盘 A</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">日志存储路径</span></span><br><span class="line">log_directory = &#x27;/var/log/postgresql/logs&#x27;  # WAL 日志存储在磁盘 B（SSD）</span><br></pre></td></tr></table></figure></li></ul><hr><h2 id="灵活调整"><a href="#灵活调整" class="headerlink" title="灵活调整"></a>灵活调整</h2><blockquote><p>适用于较低配置或与人共用资源的服务器</p></blockquote><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">shared_buffers = 4GB</span><br><span class="line">work_mem = 4MB</span><br><span class="line">maintenance_work_mem = 128MB </span><br><span class="line">effective_cache_size = 6GB</span><br><span class="line">max_parallel_workers_per_gather = 16</span><br><span class="line">max_parallel_workers = 16</span><br><span class="line">autovacuum = off</span><br><span class="line">log_min_duration_statement = -1 </span><br><span class="line">wal_level = minimal</span><br><span class="line">synchronous_commit = off</span><br><span class="line">checkpoint_timeout = 30min</span><br></pre></td></tr></table></figure><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>针对 PostgreSQL 数据库的优化，需要根据具体的硬件配置和应用场景进行调整。上述建议主要针对提高数据库在高并发场景下的性能，特别是内存缓存、连接数、索引、日志、查询优化等方面。适当的内存配置和优化索引、查询及并行性设置，能有效提高数据库的并发处理能力。</p><p>如果你有更多具体的应用场景或需要进一步的调优，随时告诉我！</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;本文将介绍如何优化 PostgreSQL 数据库以支持高并发场景。我们会针对内存缓存、连接数、索引、日志、查询优化等方面进行详细的优化建议，以帮助提升数据库的性能。&lt;/p&gt;</summary>
    
    
    
    <category term="PostgreSQL" scheme="https://freemankevin.uk/categories/PostgreSQL/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="Memory" scheme="https://freemankevin.uk/tags/Memory/"/>
    
    <category term="PostgreSQL" scheme="https://freemankevin.uk/tags/PostgreSQL/"/>
    
  </entry>
  
  <entry>
    <title>如何使用 MkDocs 创建和部署博客站点</title>
    <link href="https://freemankevin.uk/2024/12/25/mkdocs-started/"/>
    <id>https://freemankevin.uk/2024/12/25/mkdocs-started/</id>
    <published>2024-12-25T01:00:00.000Z</published>
    <updated>2024-12-25T15:41:47.168Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;本指南将帮助你从头开始创建一个使用 MkDocs 的博客站点，并将其托管在 GitHub Pages 上。我们将涵盖以下内容：首先，我们会介绍如何安装 MkDocs，然后创建一个新的 MkDocs 项目。接下来，我们会配置和编辑内容，之后将站点部署到 GitHub Pages。我们还将介绍如何配置自定义域名，最后会添加 <code>.gitignore</code> 文件来忽略不必要的文件。</p><span id="more"></span><h2 id="安装-MkDocs"><a href="#安装-MkDocs" class="headerlink" title="安装 MkDocs"></a>安装 MkDocs</h2><p>首先，你需要确保已经安装了 MkDocs。你可以使用 <code>pip</code> 来安装 MkDocs：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">pip install mkdocs</span><br></pre></td></tr></table></figure><h2 id="创建-MkDocs-项目"><a href="#创建-MkDocs-项目" class="headerlink" title="创建 MkDocs 项目"></a>创建 MkDocs 项目</h2><p>使用 MkDocs 的命令行工具创建一个新的项目：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mkdocs new my-blog</span><br><span class="line">cd my-blog</span><br></pre></td></tr></table></figure><p>这将创建一个名为 <code>my-blog</code> 的目录，其中包含一个基本的 MkDocs 配置文件和一些示例内容。</p><h2 id="配置和编辑内容"><a href="#配置和编辑内容" class="headerlink" title="配置和编辑内容"></a>配置和编辑内容</h2><h3 id="启动开发服务器"><a href="#启动开发服务器" class="headerlink" title="启动开发服务器"></a>启动开发服务器</h3><p>启动 MkDocs 的开发服务器，这样你可以在本地查看你的站点：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mkdocs serve</span><br></pre></td></tr></table></figure><p>运行此命令后，打开浏览器并访问 <code>http://127.0.0.1:8000/</code>，你应该能够看到 MkDocs 生成的默认页面。</p><h3 id="编辑内容"><a href="#编辑内容" class="headerlink" title="编辑内容"></a>编辑内容</h3><p>项目目录中有一个 <code>docs</code> 文件夹，里面包含一个 <code>index.md</code> 文件。你可以编辑这个文件来修改主页内容，并添加更多的 Markdown 文件来创建其他页面。</p><h3 id="配置站点"><a href="#配置站点" class="headerlink" title="配置站点"></a>配置站点</h3><p>项目目录中有一个名为 <code>mkdocs.yml</code> 的文件，这是 MkDocs 的配置文件。你可以编辑这个文件来配置站点的各种设置，例如站点名称、主题、导航等。</p><p>示例 <code>mkdocs.yml</code> 配置文件：</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">site_name:</span> <span class="string">My</span> <span class="string">Blog</span></span><br><span class="line"><span class="attr">nav:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">Home:</span> <span class="string">index.md</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">About:</span> <span class="string">about.md</span></span><br><span class="line"><span class="attr">theme:</span> <span class="string">readthedocs</span></span><br></pre></td></tr></table></figure><h2 id="部署到-GitHub-Pages"><a href="#部署到-GitHub-Pages" class="headerlink" title="部署到 GitHub Pages"></a>部署到 GitHub Pages</h2><p>当你对内容和配置满意后，可以将站点部署到 GitHub Pages。</p><h3 id="推送代码到-GitHub"><a href="#推送代码到-GitHub" class="headerlink" title="推送代码到 GitHub"></a>推送代码到 GitHub</h3><p>首先，初始化一个新的 Git 仓库并推送到 GitHub：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git init</span><br><span class="line">git remote add origin https://github.com/&lt;your-username&gt;/&lt;your-repo&gt;.git</span><br><span class="line">git add .</span><br><span class="line">git commit -m &quot;Initial commit&quot;</span><br><span class="line">git push -u origin main</span><br></pre></td></tr></table></figure><h3 id="部署到-GitHub-Pages-1"><a href="#部署到-GitHub-Pages-1" class="headerlink" title="部署到 GitHub Pages"></a>部署到 GitHub Pages</h3><p>使用 <code>mkdocs gh-deploy</code> 命令来部署站点：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mkdocs gh-deploy</span><br></pre></td></tr></table></figure><h2 id="配置自定义域名"><a href="#配置自定义域名" class="headerlink" title="配置自定义域名"></a>配置自定义域名</h2><p>如果你已经购买了自定义域名，可以按照以下步骤配置：</p><h3 id="配置域名-DNS"><a href="#配置域名-DNS" class="headerlink" title="配置域名 DNS"></a>配置域名 DNS</h3><p>在你的域名注册服务商的管理面板中，添加一个 CNAME 记录，指向你的 GitHub Pages URL。</p><p>示例：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Type: CNAME</span><br><span class="line">Name: www</span><br><span class="line">Value: &lt;your-github-username&gt;.github.io</span><br></pre></td></tr></table></figure><p>如果你想使用裸域名（例如 <code>yourdomain.com</code>），你需要配置 A 记录指向 GitHub 的 IP 地址：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Type: A</span><br><span class="line">Name: @</span><br><span class="line">Value: 185.199.108.153</span><br><span class="line">Value: 185.199.109.153</span><br><span class="line">Value: 185.199.110.153</span><br><span class="line">Value: 185.199.111.153</span><br></pre></td></tr></table></figure><h3 id="创建-CNAME-文件"><a href="#创建-CNAME-文件" class="headerlink" title="创建 CNAME 文件"></a>创建 CNAME 文件</h3><p>在 <code>docs</code> 目录下创建一个名为 <code>CNAME</code> 的文件，并添加你的域名：</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&quot;yourdomain.com&quot;</span> &gt; docs/CNAME</span><br></pre></td></tr></table></figure><p>然后重新构建并部署：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mkdocs build</span><br><span class="line">mkdocs gh-deploy</span><br></pre></td></tr></table></figure><h2 id="添加-gitignore-文件"><a href="#添加-gitignore-文件" class="headerlink" title="添加 .gitignore 文件"></a>添加 <code>.gitignore</code> 文件</h2><p>为了避免将不必要的文件推送到仓库中，创建一个 <code>.gitignore</code> 文件并添加以下内容：</p><p>plaintext</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 忽略操作系统生成的文件</span><br><span class="line"># macOS</span><br><span class="line">.DS_Store</span><br><span class="line"></span><br><span class="line"># Windows</span><br><span class="line">Thumbs.db</span><br><span class="line">ehthumbs.db</span><br><span class="line">Desktop.ini</span><br><span class="line"></span><br><span class="line"># VS Code 设置文件</span><br><span class="line">.vscode/</span><br><span class="line"></span><br><span class="line"># Python</span><br><span class="line">*.pyc</span><br><span class="line">*.pyo</span><br><span class="line">*.pyd</span><br><span class="line">__pycache__/</span><br><span class="line">*.env</span><br><span class="line">*.venv</span><br><span class="line">env/</span><br><span class="line">venv/</span><br><span class="line">ENV/</span><br><span class="line">env.bak/</span><br><span class="line">venv.bak/</span><br><span class="line"></span><br><span class="line"># MkDocs 构建输出</span><br><span class="line">site/</span><br><span class="line"></span><br><span class="line"># MkDocs 配置文件备份</span><br><span class="line">mkdocs.yml.bak</span><br><span class="line"></span><br><span class="line"># 忽略日志文件和数据库</span><br><span class="line">*.log</span><br><span class="line">*.sql</span><br><span class="line">*.sqlite</span><br><span class="line"></span><br><span class="line"># 忽略挂载目录</span><br><span class="line">mnt/</span><br><span class="line">media/</span><br></pre></td></tr></table></figure><p>将 <code>.gitignore</code> 文件添加到 Git 仓库并提交：</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">git add .gitignore</span><br><span class="line">git commit -m <span class="string">&quot;Add .gitignore file&quot;</span></span><br><span class="line">git push origin main</span><br></pre></td></tr></table></figure><p>通过以上步骤，你就可以创建并部署一个使用 MkDocs 的博客站点，并配置自定义域名和忽略不必要的文件。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;本指南将帮助你从头开始创建一个使用 MkDocs 的博客站点，并将其托管在 GitHub Pages 上。我们将涵盖以下内容：首先，我们会介绍如何安装 MkDocs，然后创建一个新的 MkDocs 项目。接下来，我们会配置和编辑内容，之后将站点部署到 GitHub Pages。我们还将介绍如何配置自定义域名，最后会添加 &lt;code&gt;.gitignore&lt;/code&gt; 文件来忽略不必要的文件。&lt;/p&gt;</summary>
    
    
    
    <category term="MkDocs" scheme="https://freemankevin.uk/categories/MkDocs/"/>
    
    
    <category term="GitHub" scheme="https://freemankevin.uk/tags/GitHub/"/>
    
    <category term="MkDocs" scheme="https://freemankevin.uk/tags/MkDocs/"/>
    
    <category term="GitHub Pages" scheme="https://freemankevin.uk/tags/GitHub-Pages/"/>
    
  </entry>
  
  <entry>
    <title>ElasticSearch 索引的自动备份</title>
    <link href="https://freemankevin.uk/2024/12/22/backup-es/"/>
    <id>https://freemankevin.uk/2024/12/22/backup-es/</id>
    <published>2024-12-22T04:17:25.000Z</published>
    <updated>2024-12-25T15:44:45.363Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在本文中，我们将介绍如何使用 Docker Compose 部署带有用户和密码认证的 Elasticsearch 服务，并实现索引的自动备份。首先，我们将展示如何准备配置文件并启动 Elasticsearch 服务。接着，我们将讲解如何配置并验证快照仓库。最后，我们提供了一个用于自动备份 Elasticsearch 索引的脚本，并通过 Crontab 定时任务实现自动备份。本文将帮助你确保数据的安全性和可恢复性。</p><span id="more"></span><h2 id="部署-Elasticsearch-服务"><a href="#部署-Elasticsearch-服务" class="headerlink" title="部署 Elasticsearch 服务"></a>部署 Elasticsearch 服务</h2><p>使用 Docker Compose 部署 Elasticsearch 服务，带有用户、密码认证的单节点配置。</p><h3 id="准备文件"><a href="#准备文件" class="headerlink" title="准备文件"></a>准备文件</h3><h4 id="env-文件"><a href="#env-文件" class="headerlink" title=".env 文件"></a>.env 文件</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ELASTIC_VERSION=8.10.2</span><br><span class="line">ELASTIC_PASSWORD=yourpassword</span><br><span class="line">ES_JAVA_OPTS=-Xms4g -Xmx4g</span><br></pre></td></tr></table></figure><h4 id="es-yaml-文件"><a href="#es-yaml-文件" class="headerlink" title="es.yaml 文件"></a>es.yaml 文件</h4><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&#x27;3.8&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">elasticsearch:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">docker.elastic.co/elasticsearch/elasticsearch:$&#123;ELASTIC_VERSION&#125;</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">elasticsearch</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">discovery.type=single-node</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">ELASTIC_PASSWORD=$&#123;ELASTIC_PASSWORD&#125;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">xpack.security.enabled=true</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">path.repo=/usr/share/elasticsearch/snapshots</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">path.plugins=/usr/share/elasticsearch/plugins</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="number">9200</span><span class="string">:9200</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">es-data:/usr/share/elasticsearch/data</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">es-snapshots:/usr/share/elasticsearch/snapshots</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">es-plugins:/usr/share/elasticsearch/plugins</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">es-data:</span></span><br><span class="line">  <span class="attr">es-snapshots:</span></span><br><span class="line">  <span class="attr">es-plugins:</span></span><br></pre></td></tr></table></figure><h3 id="部署服务"><a href="#部署服务" class="headerlink" title="部署服务"></a>部署服务</h3><ol><li>启动 Elasticsearch 服务：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker-compose -f es.yaml up -d</span><br></pre></td></tr></table></figure><ol start="2"><li>验证服务是否运行成功：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -u <span class="string">&quot;elastic:<span class="variable">$&#123;ELASTIC_PASSWORD&#125;</span>&quot;</span> -X GET <span class="string">&quot;http://localhost:9200&quot;</span></span><br></pre></td></tr></table></figure><p>返回集群信息表示服务启动成功。</p><hr><h2 id="接口调用与验证"><a href="#接口调用与验证" class="headerlink" title="接口调用与验证"></a>接口调用与验证</h2><p>在开始备份前，需要确保以下几点：</p><ol><li>已有访问 Elasticsearch 集群的权限。</li><li>确保集群健康状态为绿色或黄色。</li></ol><h3 id="测试连接"><a href="#测试连接" class="headerlink" title="测试连接"></a>测试连接</h3><p>通过以下命令测试连接：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -u <span class="string">&quot;elastic:<span class="variable">$&#123;ELASTIC_PASSWORD&#125;</span>&quot;</span> -X GET <span class="string">&quot;http://&lt;your-es-host&gt;:9200&quot;</span></span><br></pre></td></tr></table></figure><p>如果连接成功，将返回集群的基本信息（JSON 格式）。</p><h3 id="验证快照仓库"><a href="#验证快照仓库" class="headerlink" title="验证快照仓库"></a>验证快照仓库</h3><p>在 ES 中，备份索引需要先配置一个快照仓库。</p><ol><li>创建快照仓库：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -u <span class="string">&quot;elastic:<span class="variable">$&#123;ELASTIC_PASSWORD&#125;</span>&quot;</span> -X PUT <span class="string">&quot;http://&lt;your-es-host&gt;:9200/_snapshot/backup_repo&quot;</span> -H <span class="string">&#x27;Content-Type: application/json&#x27;</span> -d<span class="string">&#x27;</span></span><br><span class="line"><span class="string">&#123;</span></span><br><span class="line"><span class="string">  &quot;type&quot;: &quot;fs&quot;,</span></span><br><span class="line"><span class="string">  &quot;settings&quot;: &#123;</span></span><br><span class="line"><span class="string">    &quot;location&quot;: &quot;/usr/share/elasticsearch/snapshots&quot;,</span></span><br><span class="line"><span class="string">    &quot;compress&quot;: true</span></span><br><span class="line"><span class="string">  &#125;</span></span><br><span class="line"><span class="string">&#125;&#x27;</span></span><br></pre></td></tr></table></figure><ol start="2"><li>验证快照仓库是否可用：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -u <span class="string">&quot;elastic:<span class="variable">$&#123;ELASTIC_PASSWORD&#125;</span>&quot;</span> -X GET <span class="string">&quot;http://&lt;your-es-host&gt;:9200/_snapshot/backup_repo&quot;</span></span><br></pre></td></tr></table></figure><p>返回结果中 <code>&quot;status&quot;: &quot;SUCCESS&quot;</code> 表示仓库配置正确。</p><hr><h2 id="备份脚本的实现"><a href="#备份脚本的实现" class="headerlink" title="备份脚本的实现"></a>备份脚本的实现</h2><p>以下是一个用于自动备份 Elasticsearch 索引的脚本：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置参数</span></span><br><span class="line">ES_HOST=<span class="string">&quot;http://&lt;your-es-host&gt;:9200&quot;</span></span><br><span class="line">USERNAME=<span class="string">&quot;elastic&quot;</span></span><br><span class="line">PASSWORD=<span class="string">&quot;&lt;yourpassword&gt;&quot;</span></span><br><span class="line">BACKUP_REPO=<span class="string">&quot;backup_repo&quot;</span></span><br><span class="line">BACKUP_NAME=<span class="string">&quot;snapshot_<span class="subst">$(date +%Y-%m-%d-%H-%M)</span>&quot;</span></span><br><span class="line">INDEX_NAME=<span class="string">&quot;&lt;your-index-name&gt;&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建备份</span></span><br><span class="line">curl -u <span class="string">&quot;<span class="variable">$USERNAME</span>:<span class="variable">$PASSWORD</span>&quot;</span> -X PUT <span class="string">&quot;<span class="variable">$ES_HOST</span>/_snapshot/<span class="variable">$BACKUP_REPO</span>/<span class="variable">$BACKUP_NAME</span>&quot;</span> -H <span class="string">&#x27;Content-Type: application/json&#x27;</span> -d<span class="string">&#x27;</span></span><br><span class="line"><span class="string">&#123;</span></span><br><span class="line"><span class="string">  &quot;indices&quot;: &quot;&#x27;</span><span class="variable">$INDEX_NAME</span><span class="string">&#x27;&quot;,</span></span><br><span class="line"><span class="string">  &quot;ignore_unavailable&quot;: true,</span></span><br><span class="line"><span class="string">  &quot;include_global_state&quot;: false</span></span><br><span class="line"><span class="string">&#125;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 检查备份状态</span></span><br><span class="line">curl -u <span class="string">&quot;<span class="variable">$USERNAME</span>:<span class="variable">$PASSWORD</span>&quot;</span> -X GET <span class="string">&quot;<span class="variable">$ES_HOST</span>/_snapshot/<span class="variable">$BACKUP_REPO</span>/<span class="variable">$BACKUP_NAME</span>/_status&quot;</span></span><br></pre></td></tr></table></figure><p>保存脚本为 <code>es_backup.sh</code>，并赋予可执行权限：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chmod</span> +x es_backup.sh</span><br></pre></td></tr></table></figure><hr><h2 id="定时任务的配置"><a href="#定时任务的配置" class="headerlink" title="定时任务的配置"></a>定时任务的配置</h2><p>通过 <code>crontab</code> 设置定时任务，实现备份脚本的自动执行。</p><ol><li>编辑 crontab：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">crontab -e</span><br></pre></td></tr></table></figure><ol start="2"><li>添加以下条目，设置每天凌晨 2 点执行备份脚本：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0 2 * * * /path/to/es_backup.sh</span><br></pre></td></tr></table></figure><ol start="3"><li>保存并退出。</li></ol><hr><h2 id="验证备份"><a href="#验证备份" class="headerlink" title="验证备份"></a>验证备份</h2><ol><li>查看已有的快照：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -u <span class="string">&quot;elastic:<span class="variable">$&#123;ELASTIC_PASSWORD&#125;</span>&quot;</span> -X GET <span class="string">&quot;http://&lt;your-es-host&gt;:9200/_snapshot/backup_repo/_all&quot;</span></span><br></pre></td></tr></table></figure><ol start="2"><li>恢复快照：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -u <span class="string">&quot;elastic:<span class="variable">$&#123;ELASTIC_PASSWORD&#125;</span>&quot;</span> -X POST <span class="string">&quot;http://&lt;your-es-host&gt;:9200/_snapshot/backup_repo/&lt;snapshot-name&gt;/_restore&quot;</span> -H <span class="string">&#x27;Content-Type: application/json&#x27;</span> -d<span class="string">&#x27;</span></span><br><span class="line"><span class="string">&#123;</span></span><br><span class="line"><span class="string">  &quot;indices&quot;: &quot;&lt;your-index-name&gt;&quot;,</span></span><br><span class="line"><span class="string">  &quot;ignore_unavailable&quot;: true,</span></span><br><span class="line"><span class="string">  &quot;include_global_state&quot;: false</span></span><br><span class="line"><span class="string">&#125;&#x27;</span></span><br></pre></td></tr></table></figure><hr><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>通过本文档的步骤，你可以快速实现 Elasticsearch 索引的自动备份，并通过 Docker Compose 快速部署一个带用户和密码认证的 Elasticsearch 单节点服务。确保备份脚本和定时任务配置正确，以降低数据丢失的风险。在生产环境中，请根据业务需求调整备份策略和存储位置。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在本文中，我们将介绍如何使用 Docker Compose 部署带有用户和密码认证的 Elasticsearch 服务，并实现索引的自动备份。首先，我们将展示如何准备配置文件并启动 Elasticsearch 服务。接着，我们将讲解如何配置并验证快照仓库。最后，我们提供了一个用于自动备份 Elasticsearch 索引的脚本，并通过 Crontab 定时任务实现自动备份。本文将帮助你确保数据的安全性和可恢复性。&lt;/p&gt;</summary>
    
    
    
    <category term="ElasticSearch" scheme="https://freemankevin.uk/categories/ElasticSearch/"/>
    
    
    <category term="Cron" scheme="https://freemankevin.uk/tags/Cron/"/>
    
    <category term="ElasticSearch" scheme="https://freemankevin.uk/tags/ElasticSearch/"/>
    
    <category term="Backup" scheme="https://freemankevin.uk/tags/Backup/"/>
    
  </entry>
  
  <entry>
    <title>CentOS 系统下制作离线安装包</title>
    <link href="https://freemankevin.uk/2024/12/20/centos-offline-package/"/>
    <id>https://freemankevin.uk/2024/12/20/centos-offline-package/</id>
    <published>2024-12-20T04:17:25.000Z</published>
    <updated>2024-12-25T15:43:47.537Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在无法联网的环境中安装软件需要提前准备好所有必要的安装包及其依赖项。本文将指导你如何在 CentOS 7.9 系统上创建一个离线软件安装包。首先，我们会使用 yum 命令下载所需软件及其依赖项，然后创建一个本地仓库。接着，我们会将下载的目录打包并传输到目标离线机器。最后，我们会在离线机器上解压并配置本地仓库，以便顺利安装软件。通过这些步骤，你可以在没有网络连接的情况下方便地安装所需的软件。</p><span id="more"></span><h2 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h2><p>在开始之前，请确保您有一台可以联网的 CentOS 7.9 系统，用于下载软件及其依赖。</p><h2 id="下载软件及其依赖"><a href="#下载软件及其依赖" class="headerlink" title="下载软件及其依赖"></a>下载软件及其依赖</h2><p>您可以使用 <code>yum</code> 的 <code>--downloadonly</code> 和 <code>--downloaddir</code> 选项来下载软件包和它们的所有依赖。</p><p>例如，如果您想下载 <code>httpd</code>（Apache Web 服务器）及其所有依赖，可以执行以下命令：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install --downloadonly --downloaddir=/path/to/download/directory httpd</span><br></pre></td></tr></table></figure><p>将 <code>/path/to/download/directory</code> 替换为您想要保存下载文件的目录路径。</p><h2 id="创建本地仓库"><a href="#创建本地仓库" class="headerlink" title="创建本地仓库"></a>创建本地仓库</h2><p>下载所有包后，您需要在下载目录中创建一个本地仓库，这样在离线安装时 <code>yum</code> 能够识别并正确处理依赖关系。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /path/to/download/directory</span><br><span class="line"><span class="comment"># sudo rpm --import /path/to/RPM-GPG-KEY-PGDG # 如果有的话</span></span><br><span class="line">createrepo .</span><br></pre></td></tr></table></figure><p>如果 <code>createrepo</code> 命令不可用，您需要先在线安装 <code>createrepo</code> 包：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install createrepo</span><br></pre></td></tr></table></figure><p>并在同样的联网环境中运行 <code>createrepo</code> 命令。</p><h2 id="打包下载目录"><a href="#打包下载目录" class="headerlink" title="打包下载目录"></a>打包下载目录</h2><p>将带有软件包和本地仓库元数据的下载目录打包，以便于传输。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -czvf offline-packages.tar.gz -C /path/to/download/directory .</span><br></pre></td></tr></table></figure><h2 id="传输到离线机器"><a href="#传输到离线机器" class="headerlink" title="传输到离线机器"></a>传输到离线机器</h2><p>使用 USB 驱动器、光盘或其他媒体将 <code>offline-packages.tar.gz</code> 文件传输到离线 CentOS 7.9 机器上。</p><h2 id="离线安装软件"><a href="#离线安装软件" class="headerlink" title="离线安装软件"></a>离线安装软件</h2><p>在离线机器上，执行以下步骤来安装软件：</p><h3 id="解压软件包"><a href="#解压软件包" class="headerlink" title="解压软件包"></a>解压软件包</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -xzvf /path/to/offline-packages.tar.gz -C /path/to/local/repo</span><br></pre></td></tr></table></figure><h3 id="配置本地仓库"><a href="#配置本地仓库" class="headerlink" title="配置本地仓库"></a>配置本地仓库</h3><p>创建一个新的 YUM 仓库配置文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/yum.repos.d/local.repo</span><br></pre></td></tr></table></figure><p>添加以下内容：</p><figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="section">[local-repo]</span></span><br><span class="line"><span class="attr">name</span>=Local Repository</span><br><span class="line"><span class="attr">baseurl</span>=file:///path/to/local/repo</span><br><span class="line"><span class="attr">enabled</span>=<span class="number">1</span></span><br><span class="line"><span class="attr">gpgcheck</span>=<span class="number">0</span></span><br></pre></td></tr></table></figure><p>确保替换 <code>/path/to/local/repo</code> 为您解压缩软件包的实际目录。</p><h3 id="安装软件"><a href="#安装软件" class="headerlink" title="安装软件"></a>安装软件</h3><p>现在您可以使用 <code>yum</code> 从本地仓库安装软件，<code>yum</code> 会自动处理所有依赖关系。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 可能需要清理其他旧仓库，然后执行清理缓存，yum clean all &amp;&amp; yum makecache fast</span></span><br><span class="line">yum install --disablerepo=<span class="string">&#x27;*&#x27;</span> --enablerepo=<span class="string">&#x27;local-repo&#x27;</span> httpd</span><br></pre></td></tr></table></figure><p>用您实际下载的软件包名替换 <code>httpd</code>。</p><h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><ul><li>请确保在可联网的机器上下载软件包时的 CentOS 版本和离线机器上的版本一致，以免因版本不匹配导致依赖问题。</li><li>如果您下载了大量的包，并且依赖很复杂，建议直接下载整个 CentOS 仓库，并在离线环境中设置本地镜像。</li></ul><p>按照这些步骤，您应该能够在没有网络连接的情况下在 CentOS 7.9 系统上安装软件。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在无法联网的环境中安装软件需要提前准备好所有必要的安装包及其依赖项。本文将指导你如何在 CentOS 7.9 系统上创建一个离线软件安装包。首先，我们会使用 yum 命令下载所需软件及其依赖项，然后创建一个本地仓库。接着，我们会将下载的目录打包并传输到目标离线机器。最后，我们会在离线机器上解压并配置本地仓库，以便顺利安装软件。通过这些步骤，你可以在没有网络连接的情况下方便地安装所需的软件。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Yum" scheme="https://freemankevin.uk/tags/Yum/"/>
    
    <category term="Offline" scheme="https://freemankevin.uk/tags/Offline/"/>
    
    <category term="CentOS" scheme="https://freemankevin.uk/tags/CentOS/"/>
    
  </entry>
  
  <entry>
    <title>Debian 系统下制作离线安装包</title>
    <link href="https://freemankevin.uk/2024/12/20/debian-offline-package/"/>
    <id>https://freemankevin.uk/2024/12/20/debian-offline-package/</id>
    <published>2024-12-20T04:17:25.000Z</published>
    <updated>2024-12-25T15:43:06.877Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;离线安装在 Linux 系统中是一个常见需求，特别是在无法联网的环境中。本文介绍了如何在 Debian 系列操作系统（如 Debian 和 Ubuntu）上创建和使用离线安装包的完整步骤。首先，我们在联网环境下下载所需的软件包及其依赖，并创建本地 APT 仓库。然后，我们将这些包复制到离线系统上，并配置本地仓库以便进行安装。通过这些步骤，你可以方便地在离线环境中安装所需的软件。</p><span id="more"></span><h2 id="在联网的环境下"><a href="#在联网的环境下" class="headerlink" title="在联网的环境下"></a>在联网的环境下</h2><h3 id="下载软件包及其依赖"><a href="#下载软件包及其依赖" class="headerlink" title="下载软件包及其依赖"></a>下载软件包及其依赖</h3><p>首先，确定你需要的软件包和版本。使用APT的下载功能来获取软件包和它的所有依赖，但不安装它们。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt-get install --download-only &lt;package-name&gt;</span><br></pre></td></tr></table></figure><p>这会将软件包及其依赖下载到 <code>/var/cache/apt/archives/</code> 目录。</p><h3 id="复制下载的包"><a href="#复制下载的包" class="headerlink" title="复制下载的包"></a>复制下载的包</h3><p>将下载的 <code>.deb</code> 包复制到一个移动存储设备上。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cp</span> /var/cache/apt/archives/*.deb /path/to/your/usb-drive/debian-offline-packages</span><br></pre></td></tr></table></figure><h3 id="创建本地APT仓库"><a href="#创建本地APT仓库" class="headerlink" title="创建本地APT仓库"></a>创建本地APT仓库</h3><p>在移动存储设备的相同目录下，使用 <code>dpkg-scanpackages</code> 工具来创建 <code>Packages.gz</code> 索引文件。如果没有安装 <code>dpkg-dev</code>，需要先安装它。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt-get install dpkg-dev</span><br><span class="line"><span class="built_in">cd</span> /path/to/your/usb-drive/debian-offline-packages</span><br><span class="line">dpkg-scanpackages . /dev/null | gzip -9c &gt; Packages.gz</span><br></pre></td></tr></table></figure><h2 id="在离线的环境下"><a href="#在离线的环境下" class="headerlink" title="在离线的环境下"></a>在离线的环境下</h2><h3 id="准备本地APT仓库"><a href="#准备本地APT仓库" class="headerlink" title="准备本地APT仓库"></a>准备本地APT仓库</h3><p>将移动存储设备连接到离线的Debian系统。复制 <code>.deb</code> 包到本地文件系统，并保留目录结构。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p /path/to/local/repo</span><br><span class="line"><span class="built_in">cp</span> /path/to/your/usb-drive/debian-offline-packages/* /path/to/local/repo</span><br></pre></td></tr></table></figure><h3 id="添加本地仓库到APT源"><a href="#添加本地仓库到APT源" class="headerlink" title="添加本地仓库到APT源"></a>添加本地仓库到APT源</h3><p>创建一个APT源列表文件，以便APT可以使用本地仓库。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;deb [trusted=yes] file:///path/to/local/repo ./&#x27;</span> | <span class="built_in">sudo</span> <span class="built_in">tee</span> /etc/apt/sources.list.d/local-offline-repo.list</span><br></pre></td></tr></table></figure><h3 id="更新软件包列表"><a href="#更新软件包列表" class="headerlink" title="更新软件包列表"></a>更新软件包列表</h3><p>更新本地包数据库，使APT能够识别新的本地仓库。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt-get update</span><br></pre></td></tr></table></figure><h3 id="安装软件"><a href="#安装软件" class="headerlink" title="安装软件"></a>安装软件</h3><p>现在，使用 <code>apt-get install</code> 命令安装软件包，APT将解决所有本地依赖。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt-get install &lt;package-name&gt;</span><br></pre></td></tr></table></figure><h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><ul><li>这个过程假定你已经有了移动存储设备，如USB驱动器，并且它已经被挂载到了你的系统上。</li><li><code>&lt;package-name&gt;</code> 是你想要下载并安装的软件包名称。</li><li><code>/path/to/your/usb-drive</code> 是你USB驱动器的挂载点。</li><li><code>/path/to/local/repo</code> 是你在离线机器上创建的本地仓库目录的路径。</li></ul><p>按照这些步骤，你应该能够在Debian系列系统上进行离线软件安装。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;离线安装在 Linux 系统中是一个常见需求，特别是在无法联网的环境中。本文介绍了如何在 Debian 系列操作系统（如 Debian 和 Ubuntu）上创建和使用离线安装包的完整步骤。首先，我们在联网环境下下载所需的软件包及其依赖，并创建本地 APT 仓库。然后，我们将这些包复制到离线系统上，并配置本地仓库以便进行安装。通过这些步骤，你可以方便地在离线环境中安装所需的软件。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Offline" scheme="https://freemankevin.uk/tags/Offline/"/>
    
    <category term="Apt" scheme="https://freemankevin.uk/tags/Apt/"/>
    
    <category term="Debian" scheme="https://freemankevin.uk/tags/Debian/"/>
    
  </entry>
  
  <entry>
    <title>使用镜像 ISO 文件制做本地Yum 源</title>
    <link href="https://freemankevin.uk/2024/12/20/iso-yum/"/>
    <id>https://freemankevin.uk/2024/12/20/iso-yum/</id>
    <published>2024-12-20T04:17:25.000Z</published>
    <updated>2024-12-26T13:08:04.641Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;本文介绍了如何在 CentOS 7 环境中使用镜像 ISO 文件制作本地 YUM 源。通过详细步骤指导，用户可以下载并挂载 CentOS 7.9 的 ISO 文件，创建 YUM 仓库配置文件，清理 YUM 缓存，并测试新的 YUM 仓库配置。文章还介绍了如何设置开机自动挂载 ISO 文件，以确保每次启动时本地 YUM 源都可用。通过这些步骤，用户可以在本地环境中使用 YUM 安装、更新或删除软件包。</p><span id="more"></span><h3 id="上传ISO文件"><a href="#上传ISO文件" class="headerlink" title="上传ISO文件"></a>上传ISO文件</h3><p>   阿里云ISO 镜像下载地址：<a href="https://mirrors.aliyun.com/centos/7.9.2009/isos/x86_64/">https://mirrors.aliyun.com/centos/7.9.2009/isos/x86_64/</a></p><p>   将CentOS 7.9的ISO文件上传到你的CentOS 7.4服务器上。假设ISO文件位于<code>/path/to/CentOS-7.9.iso</code>。</p><h3 id="创建挂载点"><a href="#创建挂载点" class="headerlink" title="创建挂载点"></a>创建挂载点</h3><p>   创建一个目录来作为ISO文件的挂载点。</p>   <figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">mkdir</span> /mnt/cdrom</span><br></pre></td></tr></table></figure><h3 id="挂载ISO文件"><a href="#挂载ISO文件" class="headerlink" title="挂载ISO文件"></a>挂载ISO文件</h3><p>   使用<code>mount</code>命令将ISO文件挂载到刚创建的挂载点。</p>   <figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> mount -o loop /path/to/CentOS-7.9.iso /mnt/cdrom</span><br></pre></td></tr></table></figure><p>   <code>loop</code>选项用于将文件作为块设备挂载。</p><h3 id="创建YUM仓库配置文件"><a href="#创建YUM仓库配置文件" class="headerlink" title="创建YUM仓库配置文件"></a>创建YUM仓库配置文件</h3><p>   在<code>/etc/yum.repos.d/</code>目录下创建一个新的<code>.repo</code>文件。</p>   <figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vi /etc/yum.repos.d/CentOS-Base.repo</span><br></pre></td></tr></table></figure><p>   在该文件中添加以下内容：</p>   <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">[local-cdrom]</span><br><span class="line">name=CentOS-7.9 Local Repository</span><br><span class="line">baseurl=file:///mnt/cdrom</span><br><span class="line">enabled=1</span><br><span class="line">gpgcheck=0 # 跳过检查</span><br><span class="line">gpgkey=file:///mnt/cdrom/RPM-GPG-KEY-CentOS-7</span><br></pre></td></tr></table></figure><p>   按<code>ESC</code>，然后输入<code>:wq</code>保存并退出vi编辑器。</p><h3 id="清理YUM缓存"><a href="#清理YUM缓存" class="headerlink" title="清理YUM缓存"></a>清理YUM缓存</h3><p>   清理YUM缓存，确保YUM识别新的仓库。</p>   <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo yum clean all</span><br><span class="line">sudo yum makecache</span><br></pre></td></tr></table></figure><h3 id="测试新的YUM仓库"><a href="#测试新的YUM仓库" class="headerlink" title="测试新的YUM仓库"></a>测试新的YUM仓库</h3><p>   使用YUM repolist命令检查仓库列表，确保本地仓库已经被添加。</p>   <figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum repolist</span><br></pre></td></tr></table></figure><h3 id="设置开机自动挂载"><a href="#设置开机自动挂载" class="headerlink" title="设置开机自动挂载"></a>设置开机自动挂载</h3><p>   如果你希望每次开机自动挂载ISO文件，可以编辑<code>/etc/fstab</code>文件。</p>   <figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vi /etc/fstab</span><br></pre></td></tr></table></figure><p>   在文件末尾添加以下行：</p>   <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/path/to/CentOS-7.9.iso /mnt/cdrom iso9660 loop,ro 0 0</span><br></pre></td></tr></table></figure><p>   再次按<code>ESC</code>，然后输入<code>:wq</code>保存并退出vi编辑器。</p><h3 id="开始使用"><a href="#开始使用" class="headerlink" title="开始使用"></a>开始使用</h3><p>现在你可以使用本地YUM源来安装、更新或删除软件包了。例如：</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum install &lt;package-name&gt;</span><br></pre></td></tr></table></figure><p>确保在执行这些步骤之前，你有足够的权限，以防需要root权限执行某些命令。此外，ISO文件的路径应该是静态的；如果ISO文件移动到其他位置，你需要更新<code>/etc/fstab</code>和<code>/etc/yum.repos.d/CentOS-Base.repo</code>文件中的路径。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;本文介绍了如何在 CentOS 7 环境中使用镜像 ISO 文件制作本地 YUM 源。通过详细步骤指导，用户可以下载并挂载 CentOS 7.9 的 ISO 文件，创建 YUM 仓库配置文件，清理 YUM 缓存，并测试新的 YUM 仓库配置。文章还介绍了如何设置开机自动挂载 ISO 文件，以确保每次启动时本地 YUM 源都可用。通过这些步骤，用户可以在本地环境中使用 YUM 安装、更新或删除软件包。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Yum" scheme="https://freemankevin.uk/tags/Yum/"/>
    
    <category term="CentOS" scheme="https://freemankevin.uk/tags/CentOS/"/>
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
  </entry>
  
  <entry>
    <title>使用 Rufus 创建 Linux 启动 U 盘指南</title>
    <link href="https://freemankevin.uk/2024/12/20/rufus/"/>
    <id>https://freemankevin.uk/2024/12/20/rufus/</id>
    <published>2024-12-20T04:17:25.000Z</published>
    <updated>2024-12-25T15:55:06.307Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;Rufus 是一个免费、开源的工具，用于创建启动 U 盘，非常适合安装各种 Linux 发行版。本文介绍了使用 Rufus 为 Linux 发行版（如 Debian 或 CentOS）创建启动 U 盘的详细步骤。首先，准备好 U 盘、Linux ISO 文件和 Rufus 软件。然后，在 Rufus 中选择设备、配置启动选项和文件系统，并启动创建过程。完成后，使用启动 U 盘进行 Linux 安装。通过这些步骤，你可以轻松创建 Linux 启动 U 盘，并在目标电脑上安装所需的操作系统。</p><span id="more"></span><h2 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h2><p>在开始之前，请确保你已经准备好以下几样东西：</p><ul><li><strong>一个 4 GB 或更大容量的 U 盘</strong>：请注意，所有现有数据都将被清除。</li><li><strong>Linux ISO 文件</strong>：从 Linux 发行版的官网下载你需要安装的 ISO 文件，例如 Debian 或 CentOS。</li><li><strong>下载 Rufus</strong>：访问 <a href="https://rufus.ie/">Rufus 官网</a> 下载最新版本的 Rufus。</li></ul><h2 id="打开-Rufus"><a href="#打开-Rufus" class="headerlink" title="打开 Rufus"></a>打开 Rufus</h2><ol><li>插入 U 盘。</li><li>运行你下载的 Rufus.exe 文件。</li><li>如果系统请求权限，请确认。</li></ol><h2 id="配置-Rufus"><a href="#配置-Rufus" class="headerlink" title="配置 Rufus"></a>配置 Rufus</h2><p>在 Rufus 的界面中，进行以下配置：</p><ol><li><strong>设备</strong>：确保你的 U 盘在「设备」下被正确选择。</li><li><strong>启动选择</strong>：点击「选择」按钮，浏览并选择你的 Linux ISO 文件。</li><li><strong>分区方案</strong>：选择适合你系统的分区方案。<ul><li><strong>MBR</strong>：用于旧系统或传统 BIOS。</li><li><strong>GPT</strong>：用于较新系统或 UEFI 模式。</li></ul></li><li><strong>目标系统</strong>：根据你选择的分区方案，这里会自动填充「BIOS（或 UEFI-CSM）」或「UEFI（非 CSM）」。</li><li><strong>文件系统</strong>：一般选择「FAT32」（尤其是对于 UEFI 启动），这是大多数 Linux 启动 U 盘推荐的格式。</li><li><strong>卷标</strong>：你可以给 U 盘命名，例如「LINUXINSTALL」。</li></ol><h2 id="启动创建过程"><a href="#启动创建过程" class="headerlink" title="启动创建过程"></a>启动创建过程</h2><ol><li>确认所有设置正确无误后，点击「开始」按钮。</li><li>Rufus 将提示你所有数据将被清除。点击「确定」继续。</li><li>等待 Rufus 完成启动 U 盘的创建。这可能需要几分钟时间。</li></ol><h2 id="使用启动-U-盘安装-Linux"><a href="#使用启动-U-盘安装-Linux" class="headerlink" title="使用启动 U 盘安装 Linux"></a>使用启动 U 盘安装 Linux</h2><ol><li>完成 U 盘创建后，安全移除 U 盘。</li><li>将 U 盘插入你想要安装 Linux 的电脑。</li><li>重启电脑，并进入 BIOS&#x2F;UEFI 设置。</li><li>在 BIOS&#x2F;UEFI 中设置从 U 盘启动。</li><li>保存设置并退出 BIOS&#x2F;UEFI。</li><li>电脑将从 U 盘启动，并开始 Linux 安装过程。按照屏幕上的指示完成 Linux 的安装。</li></ol><h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><ul><li>确保在创建过程中不要移除 U 盘。</li><li>如果你的电脑在启动时没有提示进入 BIOS&#x2F;UEFI，你可能需要查找特定于你电脑型号的快捷键。</li><li>在使用 U 盘之前，确保备份 U 盘上的所有重要数据。</li></ul><p>通过以上步骤，你应该能够顺利使用 Rufus 创建一个 Linux 启动 U 盘，并使用它来安装 Debian、CentOS 或其他 Linux 操作系统。</p><p>当然，对于安装Windows 操作系统也是类似的过程。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;Rufus 是一个免费、开源的工具，用于创建启动 U 盘，非常适合安装各种 Linux 发行版。本文介绍了使用 Rufus 为 Linux 发行版（如 Debian 或 CentOS）创建启动 U 盘的详细步骤。首先，准备好 U 盘、Linux ISO 文件和 Rufus 软件。然后，在 Rufus 中选择设备、配置启动选项和文件系统，并启动创建过程。完成后，使用启动 U 盘进行 Linux 安装。通过这些步骤，你可以轻松创建 Linux 启动 U 盘，并在目标电脑上安装所需的操作系统。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="Rufus" scheme="https://freemankevin.uk/tags/Rufus/"/>
    
    <category term="Windows" scheme="https://freemankevin.uk/tags/Windows/"/>
    
  </entry>
  
  <entry>
    <title>如何在不同 Linux 系统下安装 VNC 服务</title>
    <link href="https://freemankevin.uk/2024/12/19/vnc/"/>
    <id>https://freemankevin.uk/2024/12/19/vnc/</id>
    <published>2024-12-19T02:00:00.000Z</published>
    <updated>2024-12-25T15:49:33.800Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;VNC（Virtual Network Computing）是一个远程桌面访问协议，允许用户通过网络远程访问 Linux 系统的图形化界面。本文介绍了如何在不同的 Linux 发行版（包括 Ubuntu&#x2F;Debian、CentOS&#x2F;RHEL、openSUSE 和 Arch Linux）中安装和配置 VNC 服务。通过详细的步骤指导，包括安装 VNC 软件、配置 VNC 密码、创建启动脚本和启动 VNC 服务，你可以在各个系统上实现远程桌面访问。此外，本文还提供了常见问题的排查方法，以帮助解决可能遇到的连接和显示问题。</p><span id="more"></span><h2 id="Ubuntu-Debian-系统"><a href="#Ubuntu-Debian-系统" class="headerlink" title="Ubuntu&#x2F;Debian 系统"></a>Ubuntu&#x2F;Debian 系统</h2><p>Ubuntu 和 Debian 系统可以通过 APT 包管理器安装常见的 VNC 服务。</p><h3 id="安装-TigerVNC"><a href="#安装-TigerVNC" class="headerlink" title="安装 TigerVNC"></a>安装 TigerVNC</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install tigervnc-standalone-server tigervnc-xorg-extension</span><br></pre></td></tr></table></figure><h3 id="配置-VNC"><a href="#配置-VNC" class="headerlink" title="配置 VNC"></a>配置 VNC</h3><ol><li><p>设置 VNC 密码：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncpasswd</span><br></pre></td></tr></table></figure><p>按提示输入密码。</p></li><li><p>创建配置文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ~/.vnc</span><br><span class="line">nano ~/.vnc/xstartup</span><br></pre></td></tr></table></figure><p>添加以下内容（以 GNOME 桌面为例）：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"><span class="built_in">exec</span> /usr/bin/gnome-session &amp;</span><br></pre></td></tr></table></figure><p>保存后为脚本添加可执行权限：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chmod</span> +x ~/.vnc/xstartup</span><br></pre></td></tr></table></figure></li><li><p>启动 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncserver</span><br></pre></td></tr></table></figure></li><li><p>停止 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncserver -<span class="built_in">kill</span> :1</span><br></pre></td></tr></table></figure></li></ol><hr><h2 id="CentOS-RHEL-系统"><a href="#CentOS-RHEL-系统" class="headerlink" title="CentOS&#x2F;RHEL 系统"></a>CentOS&#x2F;RHEL 系统</h2><p>CentOS 和 RHEL 通常通过 YUM 或 DNF 安装 VNC 服务。</p><h3 id="安装-TigerVNC-1"><a href="#安装-TigerVNC-1" class="headerlink" title="安装 TigerVNC"></a>安装 TigerVNC</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum install tigervnc-server</span><br></pre></td></tr></table></figure><h3 id="配置-VNC-1"><a href="#配置-VNC-1" class="headerlink" title="配置 VNC"></a>配置 VNC</h3><ol><li><p>编辑配置文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> nano /etc/tigervnc/vncserver.users</span><br></pre></td></tr></table></figure><p>添加如下内容：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">:1=username</span><br></pre></td></tr></table></figure><p>替换 <code>username</code> 为实际用户。</p></li><li><p>设置 VNC 密码：<br>切换到指定用户并设置密码：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">su - username</span><br><span class="line">vncpasswd</span><br></pre></td></tr></table></figure></li><li><p>配置桌面环境（以 GNOME 为例）：<br>编辑用户的 <code>~/.vnc/xstartup</code> 文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nano ~/.vnc/xstartup</span><br></pre></td></tr></table></figure><p>添加以下内容：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"><span class="built_in">exec</span> /usr/bin/gnome-session &amp;</span><br></pre></td></tr></table></figure><p>保存后为脚本添加可执行权限：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chmod</span> +x ~/.vnc/xstartup</span><br></pre></td></tr></table></figure></li><li><p>启动 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start vncserver@:1</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> vncserver@:1</span><br></pre></td></tr></table></figure></li></ol><hr><h2 id="openSUSE-系统"><a href="#openSUSE-系统" class="headerlink" title="openSUSE 系统"></a>openSUSE 系统</h2><p>openSUSE 系统也支持安装和配置 VNC 服务。</p><h3 id="安装-TightVNC"><a href="#安装-TightVNC" class="headerlink" title="安装 TightVNC"></a>安装 TightVNC</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> zypper install tightvnc</span><br></pre></td></tr></table></figure><h3 id="配置-VNC-2"><a href="#配置-VNC-2" class="headerlink" title="配置 VNC"></a>配置 VNC</h3><ol><li><p>设置 VNC 密码：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncpasswd</span><br></pre></td></tr></table></figure></li><li><p>创建配置文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ~/.vnc</span><br><span class="line">nano ~/.vnc/xstartup</span><br></pre></td></tr></table></figure><p>添加以下内容：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"><span class="built_in">exec</span> startkde &amp;</span><br></pre></td></tr></table></figure><p>根据桌面环境选择命令，例如 KDE 使用 <code>startkde</code>，GNOME 使用 <code>gnome-session</code>。</p></li><li><p>启动 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncserver</span><br></pre></td></tr></table></figure></li><li><p>停止 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncserver -<span class="built_in">kill</span> :1</span><br></pre></td></tr></table></figure></li></ol><hr><h2 id="Arch-Linux"><a href="#Arch-Linux" class="headerlink" title="Arch Linux"></a>Arch Linux</h2><p>在 Arch Linux 上，可以通过 <code>pacman</code> 包管理器安装 VNC 服务。</p><h3 id="安装-TigerVNC-2"><a href="#安装-TigerVNC-2" class="headerlink" title="安装 TigerVNC"></a>安装 TigerVNC</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pacman -S tigervnc</span><br></pre></td></tr></table></figure><h3 id="配置-VNC-3"><a href="#配置-VNC-3" class="headerlink" title="配置 VNC"></a>配置 VNC</h3><ol><li><p>设置 VNC 密码：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncpasswd</span><br></pre></td></tr></table></figure></li><li><p>配置 VNC：<br>编辑 <code>~/.vnc/xstartup</code> 文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nano ~/.vnc/xstartup</span><br></pre></td></tr></table></figure><p>添加以下内容：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"><span class="built_in">exec</span> /usr/bin/xfce4-session &amp;</span><br></pre></td></tr></table></figure><p>根据桌面环境选择启动命令，例如 XFCE 使用 <code>xfce4-session</code>。</p></li><li><p>启动 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncserver</span><br></pre></td></tr></table></figure></li><li><p>停止 VNC 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncserver -<span class="built_in">kill</span> :1</span><br></pre></td></tr></table></figure></li></ol><hr><h2 id="常见问题排查"><a href="#常见问题排查" class="headerlink" title="常见问题排查"></a>常见问题排查</h2><ul><li><p><strong>连接失败</strong>：</p><ul><li>检查防火墙是否允许 VNC 端口（默认 5901）。<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ufw allow 5901</span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong>显示空白屏幕</strong>：</p><ul><li>确保 <code>xstartup</code> 文件正确配置并具有可执行权限。</li></ul></li><li><p><strong>服务未启动</strong>：</p><ul><li>检查日志文件 <code>~/.vnc/*.log</code> 获取更多信息。</li></ul></li></ul><hr><p>通过以上步骤，您可以在不同的 Linux 发行版中安装和配置 VNC 服务，轻松实现远程桌面访问。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;VNC（Virtual Network Computing）是一个远程桌面访问协议，允许用户通过网络远程访问 Linux 系统的图形化界面。本文介绍了如何在不同的 Linux 发行版（包括 Ubuntu&amp;#x2F;Debian、CentOS&amp;#x2F;RHEL、openSUSE 和 Arch Linux）中安装和配置 VNC 服务。通过详细的步骤指导，包括安装 VNC 软件、配置 VNC 密码、创建启动脚本和启动 VNC 服务，你可以在各个系统上实现远程桌面访问。此外，本文还提供了常见问题的排查方法，以帮助解决可能遇到的连接和显示问题。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="VNC" scheme="https://freemankevin.uk/tags/VNC/"/>
    
  </entry>
  
  <entry>
    <title>如何在不同 Linux 系统下安装 Docker</title>
    <link href="https://freemankevin.uk/2024/12/19/docker/"/>
    <id>https://freemankevin.uk/2024/12/19/docker/</id>
    <published>2024-12-19T01:00:00.000Z</published>
    <updated>2024-12-25T15:45:46.550Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;Docker 是一种开源的容器化平台，可以简化应用程序的开发、部署和运行。本文介绍了如何在不同的 Linux 系统中安装 Docker，包括 Ubuntu&#x2F;Debian、CentOS&#x2F;RHEL、openSUSE 和 Arch Linux。我们详细讲解了每个系统的安装步骤，包括更新系统、安装依赖、添加 Docker 软件源、安装 Docker 引擎以及验证安装。最后，我们还介绍了如何配置非 root 用户使用 Docker。通过这些步骤，你可以在各种 Linux 发行版上轻松安装和配置 Docker。</p><span id="more"></span><h2 id="Ubuntu-Debian-系统"><a href="#Ubuntu-Debian-系统" class="headerlink" title="Ubuntu&#x2F;Debian 系统"></a>Ubuntu&#x2F;Debian 系统</h2><h3 id="更新系统"><a href="#更新系统" class="headerlink" title="更新系统"></a>更新系统</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt upgrade -y</span><br></pre></td></tr></table></figure><h3 id="安装依赖"><a href="#安装依赖" class="headerlink" title="安装依赖"></a>安装依赖</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install -y \</span><br><span class="line">    ca-certificates \</span><br><span class="line">    curl \</span><br><span class="line">    gnupg \</span><br><span class="line">    lsb-release</span><br></pre></td></tr></table></figure><h3 id="添加-Docker-的官方-GPG-密钥"><a href="#添加-Docker-的官方-GPG-密钥" class="headerlink" title="添加 Docker 的官方 GPG 密钥"></a>添加 Docker 的官方 GPG 密钥</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">mkdir</span> -p /etc/apt/keyrings</span><br><span class="line">curl -fsSL https://download.docker.com/linux/ubuntu/gpg | <span class="built_in">sudo</span> gpg --dearmor -o /etc/apt/keyrings/docker.gpg</span><br></pre></td></tr></table></figure><h3 id="设置-Docker-软件源"><a href="#设置-Docker-软件源" class="headerlink" title="设置 Docker 软件源"></a>设置 Docker 软件源</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> \&quot;deb [<span class="built_in">arch</span>=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \</span><br><span class="line">$(lsb_release -cs) stable\&quot; | <span class="built_in">sudo</span> <span class="built_in">tee</span> /etc/apt/sources.list.d/docker.list &gt; /dev/null</span><br></pre></td></tr></table></figure><h3 id="安装-Docker-引擎"><a href="#安装-Docker-引擎" class="headerlink" title="安装 Docker 引擎"></a>安装 Docker 引擎</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin</span><br></pre></td></tr></table></figure><h3 id="验证安装"><a href="#验证安装" class="headerlink" title="验证安装"></a>验证安装</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker --version</span><br></pre></td></tr></table></figure><hr><h2 id="CentOS-RHEL-系统"><a href="#CentOS-RHEL-系统" class="headerlink" title="CentOS&#x2F;RHEL 系统"></a>CentOS&#x2F;RHEL 系统</h2><h3 id="更新系统-1"><a href="#更新系统-1" class="headerlink" title="更新系统"></a>更新系统</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum update -y</span><br></pre></td></tr></table></figure><h3 id="安装依赖-1"><a href="#安装依赖-1" class="headerlink" title="安装依赖"></a>安装依赖</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum install -y yum-utils</span><br></pre></td></tr></table></figure><h3 id="添加-Docker-软件源"><a href="#添加-Docker-软件源" class="headerlink" title="添加 Docker 软件源"></a>添加 Docker 软件源</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo</span><br></pre></td></tr></table></figure><h3 id="安装-Docker-引擎-1"><a href="#安装-Docker-引擎-1" class="headerlink" title="安装 Docker 引擎"></a>安装 Docker 引擎</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum install -y docker-ce docker-ce-cli containerd.io</span><br></pre></td></tr></table></figure><h3 id="启动-Docker-服务"><a href="#启动-Docker-服务" class="headerlink" title="启动 Docker 服务"></a>启动 Docker 服务</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start docker</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> docker</span><br></pre></td></tr></table></figure><h3 id="验证安装-1"><a href="#验证安装-1" class="headerlink" title="验证安装"></a>验证安装</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker --version</span><br></pre></td></tr></table></figure><hr><h2 id="openSUSE-系统"><a href="#openSUSE-系统" class="headerlink" title="openSUSE 系统"></a>openSUSE 系统</h2><h3 id="更新系统-2"><a href="#更新系统-2" class="headerlink" title="更新系统"></a>更新系统</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> zypper refresh</span><br></pre></td></tr></table></figure><h3 id="安装-Docker"><a href="#安装-Docker" class="headerlink" title="安装 Docker"></a>安装 Docker</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> zypper install -y docker</span><br></pre></td></tr></table></figure><h3 id="启动-Docker-服务-1"><a href="#启动-Docker-服务-1" class="headerlink" title="启动 Docker 服务"></a>启动 Docker 服务</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start docker</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> docker</span><br></pre></td></tr></table></figure><h3 id="验证安装-2"><a href="#验证安装-2" class="headerlink" title="验证安装"></a>验证安装</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker --version</span><br></pre></td></tr></table></figure><hr><h2 id="Arch-Linux"><a href="#Arch-Linux" class="headerlink" title="Arch Linux"></a>Arch Linux</h2><h3 id="更新系统-3"><a href="#更新系统-3" class="headerlink" title="更新系统"></a>更新系统</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pacman -Syu</span><br></pre></td></tr></table></figure><h3 id="安装-Docker-1"><a href="#安装-Docker-1" class="headerlink" title="安装 Docker"></a>安装 Docker</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pacman -S docker</span><br></pre></td></tr></table></figure><h3 id="启动-Docker-服务-2"><a href="#启动-Docker-服务-2" class="headerlink" title="启动 Docker 服务"></a>启动 Docker 服务</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start docker</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> docker</span><br></pre></td></tr></table></figure><h3 id="验证安装-3"><a href="#验证安装-3" class="headerlink" title="验证安装"></a>验证安装</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker --version</span><br></pre></td></tr></table></figure><hr><h2 id="配置非-root-用户使用-Docker"><a href="#配置非-root-用户使用-Docker" class="headerlink" title="配置非 root 用户使用 Docker"></a>配置非 root 用户使用 Docker</h2><p>默认情况下，只有 root 用户或具有 sudo 权限的用户可以运行 Docker。你可以通过以下步骤允许非 root 用户使用 Docker：</p><h3 id="添加用户到-Docker-组"><a href="#添加用户到-Docker-组" class="headerlink" title="添加用户到 Docker 组"></a>添加用户到 Docker 组</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> usermod -aG docker <span class="variable">$USER</span></span><br></pre></td></tr></table></figure><h3 id="重新登录"><a href="#重新登录" class="headerlink" title="重新登录"></a>重新登录</h3><p>重新登录后，非 root 用户即可运行 Docker 命令。</p><h3 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run hello-world</span><br></pre></td></tr></table></figure><hr><p>通过以上步骤，你可以在不同的 Linux 系统中安装和配置 Docker。安装完成后，可以开始体验 Docker 的强大功能，如运行容器、构建镜像等。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;Docker 是一种开源的容器化平台，可以简化应用程序的开发、部署和运行。本文介绍了如何在不同的 Linux 系统中安装 Docker，包括 Ubuntu&amp;#x2F;Debian、CentOS&amp;#x2F;RHEL、openSUSE 和 Arch Linux。我们详细讲解了每个系统的安装步骤，包括更新系统、安装依赖、添加 Docker 软件源、安装 Docker 引擎以及验证安装。最后，我们还介绍了如何配置非 root 用户使用 Docker。通过这些步骤，你可以在各种 Linux 发行版上轻松安装和配置 Docker。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Docker" scheme="https://freemankevin.uk/tags/Docker/"/>
    
    <category term="Container" scheme="https://freemankevin.uk/tags/Container/"/>
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
  </entry>
  
  <entry>
    <title>如何在不同 Linux 系统下开启防火墙</title>
    <link href="https://freemankevin.uk/2024/12/13/firewalld/"/>
    <id>https://freemankevin.uk/2024/12/13/firewalld/</id>
    <published>2024-12-13T03:34:15.000Z</published>
    <updated>2024-12-25T15:46:16.765Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;防火墙是保护系统安全的重要组成部分，它可以帮助阻止未授权的访问。本文介绍了如何在不同的 Linux 系统中开启和管理防火墙，包括 Ubuntu&#x2F;Debian、CentOS&#x2F;RHEL、openSUSE 和 Arch Linux 系统。我们详细讲解了每个系统中常用的防火墙工具和命令，例如 ufw、firewalld、SuSEfirewall2、iptables 和 nftables。通过这些步骤，你可以在各种 Linux 发行版上有效地开启防火墙并配置规则，从而增强系统的安全性。</p><span id="more"></span><h2 id="Ubuntu-Debian-系统"><a href="#Ubuntu-Debian-系统" class="headerlink" title="Ubuntu&#x2F;Debian 系统"></a>Ubuntu&#x2F;Debian 系统</h2><p>Ubuntu 和 Debian 系统默认使用 <code>ufw</code>（Uncomplicated Firewall）作为防火墙工具。</p><h3 id="检查防火墙状态"><a href="#检查防火墙状态" class="headerlink" title="检查防火墙状态"></a>检查防火墙状态</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ufw status</span><br></pre></td></tr></table></figure><h3 id="开启防火墙"><a href="#开启防火墙" class="headerlink" title="开启防火墙"></a>开启防火墙</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ufw <span class="built_in">enable</span></span><br></pre></td></tr></table></figure><h3 id="禁用防火墙"><a href="#禁用防火墙" class="headerlink" title="禁用防火墙"></a>禁用防火墙</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ufw <span class="built_in">disable</span></span><br></pre></td></tr></table></figure><h3 id="配置示例"><a href="#配置示例" class="headerlink" title="配置示例"></a>配置示例</h3><p>允许 SSH 连接：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ufw allow ssh</span><br></pre></td></tr></table></figure><hr><h2 id="CentOS-RHEL-系统"><a href="#CentOS-RHEL-系统" class="headerlink" title="CentOS&#x2F;RHEL 系统"></a>CentOS&#x2F;RHEL 系统</h2><p>CentOS 和 RHEL 系统默认使用 <code>firewalld</code> 作为防火墙工具。</p><h3 id="检查防火墙状态-1"><a href="#检查防火墙状态-1" class="headerlink" title="检查防火墙状态"></a>检查防火墙状态</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl status firewalld</span><br></pre></td></tr></table></figure><h3 id="开启防火墙-1"><a href="#开启防火墙-1" class="headerlink" title="开启防火墙"></a>开启防火墙</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start firewalld</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> firewalld</span><br></pre></td></tr></table></figure><h3 id="禁用防火墙-1"><a href="#禁用防火墙-1" class="headerlink" title="禁用防火墙"></a>禁用防火墙</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl stop firewalld</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">disable</span> firewalld</span><br></pre></td></tr></table></figure><h3 id="配置示例-1"><a href="#配置示例-1" class="headerlink" title="配置示例"></a>配置示例</h3><p>允许 HTTP 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> firewall-cmd --permanent --add-service=http</span><br><span class="line"><span class="built_in">sudo</span> firewall-cmd --reload</span><br></pre></td></tr></table></figure><hr><h2 id="openSUSE-系统"><a href="#openSUSE-系统" class="headerlink" title="openSUSE 系统"></a>openSUSE 系统</h2><p>openSUSE 系统默认使用 <code>firewalld</code>，但某些版本可能使用 <code>SuSEfirewall2</code>。</p><h3 id="使用-firewalld"><a href="#使用-firewalld" class="headerlink" title="使用 firewalld"></a>使用 firewalld</h3><h4 id="检查防火墙状态-2"><a href="#检查防火墙状态-2" class="headerlink" title="检查防火墙状态"></a>检查防火墙状态</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl status firewalld</span><br></pre></td></tr></table></figure><h4 id="开启防火墙-2"><a href="#开启防火墙-2" class="headerlink" title="开启防火墙"></a>开启防火墙</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start firewalld</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> firewalld</span><br></pre></td></tr></table></figure><h4 id="配置示例-2"><a href="#配置示例-2" class="headerlink" title="配置示例"></a>配置示例</h4><p>允许 HTTPS 服务：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> firewall-cmd --permanent --add-service=https</span><br><span class="line"><span class="built_in">sudo</span> firewall-cmd --reload</span><br></pre></td></tr></table></figure><h3 id="使用-SuSEfirewall2"><a href="#使用-SuSEfirewall2" class="headerlink" title="使用 SuSEfirewall2"></a>使用 SuSEfirewall2</h3><h4 id="检查防火墙状态-3"><a href="#检查防火墙状态-3" class="headerlink" title="检查防火墙状态"></a>检查防火墙状态</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> SuSEfirewall2 status</span><br></pre></td></tr></table></figure><h4 id="开启防火墙-3"><a href="#开启防火墙-3" class="headerlink" title="开启防火墙"></a>开启防火墙</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> SuSEfirewall2 start</span><br></pre></td></tr></table></figure><hr><h2 id="Arch-Linux"><a href="#Arch-Linux" class="headerlink" title="Arch Linux"></a>Arch Linux</h2><p>Arch Linux 通常使用 <code>iptables</code> 或 <code>nftables</code> 来管理防火墙。</p><h3 id="使用-iptables"><a href="#使用-iptables" class="headerlink" title="使用 iptables"></a>使用 iptables</h3><h4 id="检查防火墙规则"><a href="#检查防火墙规则" class="headerlink" title="检查防火墙规则"></a>检查防火墙规则</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> iptables -L</span><br></pre></td></tr></table></figure><h4 id="添加规则并保存"><a href="#添加规则并保存" class="headerlink" title="添加规则并保存"></a>添加规则并保存</h4><p>允许所有来自端口 22 的连接：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> iptables -A INPUT -p tcp --dport 22 -j ACCEPT</span><br><span class="line"><span class="built_in">sudo</span> iptables-save &gt; /etc/iptables/iptables.rules</span><br></pre></td></tr></table></figure><h4 id="开启防火墙-4"><a href="#开启防火墙-4" class="headerlink" title="开启防火墙"></a>开启防火墙</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start iptables</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> iptables</span><br></pre></td></tr></table></figure><h3 id="使用-nftables"><a href="#使用-nftables" class="headerlink" title="使用 nftables"></a>使用 nftables</h3><h4 id="检查防火墙状态-4"><a href="#检查防火墙状态-4" class="headerlink" title="检查防火墙状态"></a>检查防火墙状态</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> nft list ruleset</span><br></pre></td></tr></table></figure><h4 id="添加规则并保存-1"><a href="#添加规则并保存-1" class="headerlink" title="添加规则并保存"></a>添加规则并保存</h4><p>允许 HTTP 流量：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> nft add rule ip filter input tcp dport 80 accept</span><br><span class="line"><span class="built_in">sudo</span> nft list ruleset &gt; /etc/nftables.conf</span><br></pre></td></tr></table></figure><h4 id="启用-nftables-服务"><a href="#启用-nftables-服务" class="headerlink" title="启用 nftables 服务"></a>启用 nftables 服务</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start nftables</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> nftables</span><br></pre></td></tr></table></figure><hr><h2 id="常见问题排查"><a href="#常见问题排查" class="headerlink" title="常见问题排查"></a>常见问题排查</h2><ul><li><p><strong>端口未生效</strong>：<br>确保规则已永久保存并重新加载防火墙配置。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ufw reload  <span class="comment"># 适用于 ufw</span></span><br><span class="line"><span class="built_in">sudo</span> firewall-cmd --reload  <span class="comment"># 适用于 firewalld</span></span><br></pre></td></tr></table></figure></li><li><p><strong>服务未运行</strong>：<br>确保防火墙服务已启动并设置为开机自启。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> firewalld</span><br><span class="line"><span class="built_in">sudo</span> systemctl start firewalld</span><br></pre></td></tr></table></figure></li></ul><hr><p>通过以上命令，你可以在不同的 Linux 系统中有效地开启防火墙并配置规则，从而增强系统的安全性。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;防火墙是保护系统安全的重要组成部分，它可以帮助阻止未授权的访问。本文介绍了如何在不同的 Linux 系统中开启和管理防火墙，包括 Ubuntu&amp;#x2F;Debian、CentOS&amp;#x2F;RHEL、openSUSE 和 Arch Linux 系统。我们详细讲解了每个系统中常用的防火墙工具和命令，例如 ufw、firewalld、SuSEfirewall2、iptables 和 nftables。通过这些步骤，你可以在各种 Linux 发行版上有效地开启防火墙并配置规则，从而增强系统的安全性。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Firewall" scheme="https://freemankevin.uk/tags/Firewall/"/>
    
    <category term="Ufw" scheme="https://freemankevin.uk/tags/Ufw/"/>
    
    <category term="Iptables" scheme="https://freemankevin.uk/tags/Iptables/"/>
    
  </entry>
  
  <entry>
    <title>GitHub 推送代码失败的解决方案</title>
    <link href="https://freemankevin.uk/2024/12/11/github-push-error/"/>
    <id>https://freemankevin.uk/2024/12/11/github-push-error/</id>
    <published>2024-12-11T04:17:25.000Z</published>
    <updated>2024-12-25T15:46:46.421Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在使用 Git 推送代码到 GitHub 时，常常会遇到推送失败的问题，即使网络带宽正常。主要原因是 Git 默认不会通过本地代理，即使代理设置为全局模式也无效。本文介绍了通过设置 Git 的代理来解决推送问题的具体步骤。首先，修改全局 Git 配置文件，添加代理设置。然后，通过命令验证代理配置是否生效。通过这些步骤，你可以解决 Git 推送代码到 GitHub 时遇到的问题。</p><span id="more"></span><h3 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h3><p>在使用 Git 推送代码到 GitHub 时，<strong>推送经常失败</strong>，但带宽测试显示网络正常，上传和下载速度都没问题。经过排查发现，<strong>Git 默认不会通过本地代理</strong>，即使代理设置为全局模式也无效。这导致 GitHub 的访问不稳定。</p><hr><h3 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h3><p>通过设置 Git 的代理，可以解决推送问题。</p><h4 id="修改全局-Git-配置"><a href="#修改全局-Git-配置" class="headerlink" title="修改全局 Git 配置"></a>修改全局 Git 配置</h4><p>执行以下命令编辑 Git 的全局配置文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global --edit</span><br></pre></td></tr></table></figure><p>添加如下内容，将 <code>127.0.0.1:7890</code> 替换为你的代理地址和端口：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[http]</span><br><span class="line">proxy = http://127.0.0.1:7890</span><br><span class="line"></span><br><span class="line">[https]</span><br><span class="line">proxy = https://127.0.0.1:7890</span><br></pre></td></tr></table></figure><h4 id="验证代理配置"><a href="#验证代理配置" class="headerlink" title="验证代理配置"></a>验证代理配置</h4><p>使用以下命令检查配置是否生效：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global --get https.proxy</span><br></pre></td></tr></table></figure><p>若返回代理地址（如 <code>https://127.0.0.1:7890</code>），说明设置成功。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在使用 Git 推送代码到 GitHub 时，常常会遇到推送失败的问题，即使网络带宽正常。主要原因是 Git 默认不会通过本地代理，即使代理设置为全局模式也无效。本文介绍了通过设置 Git 的代理来解决推送问题的具体步骤。首先，修改全局 Git 配置文件，添加代理设置。然后，通过命令验证代理配置是否生效。通过这些步骤，你可以解决 Git 推送代码到 GitHub 时遇到的问题。&lt;/p&gt;</summary>
    
    
    
    <category term="GitHub" scheme="https://freemankevin.uk/categories/GitHub/"/>
    
    
    <category term="GitHub" scheme="https://freemankevin.uk/tags/GitHub/"/>
    
    <category term="Git" scheme="https://freemankevin.uk/tags/Git/"/>
    
    <category term="Proxy" scheme="https://freemankevin.uk/tags/Proxy/"/>
    
  </entry>
  
  <entry>
    <title>数据盘挂载与 LVM 创建</title>
    <link href="https://freemankevin.uk/2024/12/11/mount-hd-lvm/"/>
    <id>https://freemankevin.uk/2024/12/11/mount-hd-lvm/</id>
    <published>2024-12-11T04:17:25.000Z</published>
    <updated>2024-12-26T13:08:04.642Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在服务器环境中，使用逻辑卷管理器（LVM）配置和挂载数据盘是一种灵活高效的方式。本文介绍了在 CentOS 和其他常见 Linux 系统中，如何使用 LVM 将新数据盘挂载到 &#x2F;data 目录。我们详细讲解了从安装 LVM 工具、初始化物理卷、创建卷组和逻辑卷，到创建文件系统、挂载逻辑卷以及设置开机自动挂载的步骤。此外，还提供了适用于不同 Linux 系统的注意事项和扩展管理技巧。通过这些步骤，你可以实现数据盘的灵活管理和高效利用。</p><span id="more"></span><h3 id="前置检查"><a href="#前置检查" class="headerlink" title="前置检查"></a><strong>前置检查</strong></h3><ol><li>确保目标硬盘（<code>vdb</code>）是新磁盘或非关键数据盘。</li><li>验证系统是否检测到硬盘：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsblk</span><br></pre></td></tr></table></figure>找到 <code>vdb</code>，确认其未分区或挂载。</li></ol><hr><h3 id="安装-LVM-工具"><a href="#安装-LVM-工具" class="headerlink" title="安装 LVM 工具"></a><strong>安装 LVM 工具</strong></h3><ul><li><p><strong>CentOS&#x2F;Red Hat 系统</strong>:</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum install -y lvm2</span><br></pre></td></tr></table></figure></li><li><p><strong>Debian&#x2F;Ubuntu 系统</strong>:</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install -y lvm2</span><br></pre></td></tr></table></figure></li><li><p><strong>OpenSUSE 系统</strong>:</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> zypper install -y lvm2</span><br></pre></td></tr></table></figure></li></ul><hr><h3 id="LVM-配置步骤"><a href="#LVM-配置步骤" class="headerlink" title="LVM 配置步骤"></a><strong>LVM 配置步骤</strong></h3><h4 id="初始化物理卷-PV"><a href="#初始化物理卷-PV" class="headerlink" title="初始化物理卷 (PV)"></a>初始化物理卷 (PV)</h4><p>将 <code>vdb</code> 初始化为 LVM 的物理卷：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pvcreate /dev/vdb</span><br></pre></td></tr></table></figure><p>验证创建是否成功：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pvdisplay</span><br></pre></td></tr></table></figure><hr><h4 id="创建卷组-VG"><a href="#创建卷组-VG" class="headerlink" title="创建卷组 (VG)"></a>创建卷组 (VG)</h4><p>创建一个新的卷组 <code>vg_data</code>，并将 <code>vdb</code> 添加到该卷组：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vgcreate vg_data /dev/vdb</span><br></pre></td></tr></table></figure><p>检查卷组信息：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vgdisplay</span><br></pre></td></tr></table></figure><hr><h4 id="创建逻辑卷-LV"><a href="#创建逻辑卷-LV" class="headerlink" title="创建逻辑卷 (LV)"></a>创建逻辑卷 (LV)</h4><p>在卷组 <code>vg_data</code> 中创建逻辑卷 <code>lv_data</code>：</p><ul><li>使用所有剩余空间：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> lvcreate -l +100%FREE -n lv_data vg_data</span><br></pre></td></tr></table></figure></li><li>或者指定大小（如 100GB）：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> lvcreate -L 100G -n lv_data vg_data</span><br></pre></td></tr></table></figure></li></ul><p>验证逻辑卷：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> lvdisplay</span><br></pre></td></tr></table></figure><hr><h4 id="创建文件系统"><a href="#创建文件系统" class="headerlink" title="创建文件系统"></a>创建文件系统</h4><p>为逻辑卷 <code>lv_data</code> 创建 EXT4 文件系统：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> mkfs.ext4 /dev/vg_data/lv_data</span><br></pre></td></tr></table></figure><hr><h4 id="挂载逻辑卷"><a href="#挂载逻辑卷" class="headerlink" title="挂载逻辑卷"></a>挂载逻辑卷</h4><ol><li><p>创建挂载点：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">mkdir</span> -p /data</span><br></pre></td></tr></table></figure></li><li><p>挂载逻辑卷到 <code>/data</code>：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> mount /dev/vg_data/lv_data /data</span><br></pre></td></tr></table></figure></li><li><p>确认挂载成功：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">df</span> -h | grep /data</span><br></pre></td></tr></table></figure></li></ol><hr><h4 id="设置开机自动挂载"><a href="#设置开机自动挂载" class="headerlink" title="设置开机自动挂载"></a>设置开机自动挂载</h4><p>编辑 <code>/etc/fstab</code> 文件，使逻辑卷在开机时自动挂载：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vim /etc/fstab</span><br></pre></td></tr></table></figure><p>在文件末尾添加以下内容：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/dev/vg_data/lv_data /data ext4 defaults 0 0</span><br></pre></td></tr></table></figure><p>测试 <code>fstab</code> 配置是否正确：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> umount /data</span><br><span class="line"><span class="built_in">sudo</span> mount -a</span><br></pre></td></tr></table></figure><p>如果没有错误，挂载设置无误。</p><hr><h3 id="其他-Linux-系统的注意事项"><a href="#其他-Linux-系统的注意事项" class="headerlink" title="其他 Linux 系统的注意事项"></a><strong>其他 Linux 系统的注意事项</strong></h3><ol><li><p><strong>文件系统支持</strong>:<br>不同发行版可能默认支持不同文件系统（如 <code>XFS</code> 或 <code>Btrfs</code>）。可以根据需求替换 <code>mkfs.ext4</code> 为适合的文件系统命令，例如：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> mkfs.xfs /dev/vg_data/lv_data</span><br></pre></td></tr></table></figure></li><li><p><strong>卷组管理</strong>:<br>如果有多个硬盘，可将多个物理卷（如 <code>/dev/vdb</code> 和 <code>/dev/vdc</code>）加入同一卷组：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vgextend vg_data /dev/vdc</span><br></pre></td></tr></table></figure></li><li><p><strong>调整逻辑卷大小</strong>:<br>如果需要扩展逻辑卷：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> lvextend -L +50G /dev/vg_data/lv_data</span><br><span class="line"><span class="built_in">sudo</span> resize2fs /dev/vg_data/lv_data</span><br></pre></td></tr></table></figure></li></ol><hr><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a><strong>总结</strong></h3><p>通过以上步骤，我们成功配置了数据盘并挂载到 <code>/data</code>。这种使用 LVM 的方式不仅提供了灵活的卷管理能力，还能在未来需要时动态扩展存储，避免繁琐的重新分区操作。</p><p>确保在生产环境操作前备份重要数据，执行步骤时严格按照实际需求调整命令。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在服务器环境中，使用逻辑卷管理器（LVM）配置和挂载数据盘是一种灵活高效的方式。本文介绍了在 CentOS 和其他常见 Linux 系统中，如何使用 LVM 将新数据盘挂载到 &amp;#x2F;data 目录。我们详细讲解了从安装 LVM 工具、初始化物理卷、创建卷组和逻辑卷，到创建文件系统、挂载逻辑卷以及设置开机自动挂载的步骤。此外，还提供了适用于不同 Linux 系统的注意事项和扩展管理技巧。通过这些步骤，你可以实现数据盘的灵活管理和高效利用。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="Mount" scheme="https://freemankevin.uk/tags/Mount/"/>
    
    <category term="LVM" scheme="https://freemankevin.uk/tags/LVM/"/>
    
  </entry>
  
  <entry>
    <title>在没有 Swap 的服务器上创建 Swap</title>
    <link href="https://freemankevin.uk/2024/12/11/swap/"/>
    <id>https://freemankevin.uk/2024/12/11/swap/</id>
    <published>2024-12-11T04:17:25.000Z</published>
    <updated>2024-12-25T15:48:44.889Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;在服务器上创建 Swap 文件是缓解内存不足的一种有效方法。本文介绍了如何在没有 Swap 分区的服务器上创建 Swap 文件，以提供额外的虚拟内存，从而保证系统的稳定性和性能。首先，我们检查系统是否已有 Swap 分区，然后通过创建和配置 Swap 文件来实现虚拟内存的扩展。最后，我们确保 Swap 文件在系统启动时自动启用。通过这些步骤，你可以在没有预配置 Swap 分区的服务器上创建 Swap 文件，确保系统在高负载情况下的稳定运行。</p><span id="more"></span><blockquote><p>这里以16核、32GB内存、300GB数据盘的服务器进行举例。</p></blockquote><h4 id="检查当前系统是否有-Swap"><a href="#检查当前系统是否有-Swap" class="headerlink" title="检查当前系统是否有 Swap"></a>检查当前系统是否有 Swap</h4><p>首先，我们需要检查当前系统是否有 Swap 分区。执行以下命令：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">swapon --show</span><br></pre></td></tr></table></figure><p>如果没有输出，说明当前系统没有 Swap 分区。</p><h4 id="创建-Swap-文件"><a href="#创建-Swap-文件" class="headerlink" title="创建 Swap 文件"></a>创建 Swap 文件</h4><p>为了在没有 Swap 分区的服务器上创建 Swap，我们可以使用文件来模拟 Swap。以下是创建 Swap 文件的详细步骤：</p><ol><li><p><strong>创建 Swap 文件</strong>：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">dd</span> <span class="keyword">if</span>=/dev/zero of=/data/swapfile bs=1M count=20480</span><br></pre></td></tr></table></figure><p>解释：</p><ul><li><code>if=/dev/zero</code>：从零设备读取数据，用于创建文件。</li><li><code>of=/data/swapfile</code>：指定目标文件名为 <code>/data/swapfile</code>，挂载在数据盘 <code>/data</code> 下。</li><li><code>bs=1M</code>：设置块大小为1MB。</li><li><code>count=20480</code>：指定文件大小为20GB（32GB内存 &#x2F; 1.6 &#x3D; 20GB）。</li></ul></li><li><p><strong>更改 Swap 文件权限</strong>：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">chmod</span> 600 /data/swapfile</span><br></pre></td></tr></table></figure></li><li><p><strong>设置文件为Swap</strong>：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> mkswap /data/swapfile</span><br></pre></td></tr></table></figure></li><li><p><strong>激活Swap文件</strong>：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> swapon /data/swapfile</span><br></pre></td></tr></table></figure></li></ol><h4 id="配置开机自启"><a href="#配置开机自启" class="headerlink" title="配置开机自启"></a>配置开机自启</h4><p>为了确保每次系统启动时自动启用 Swap 文件，我们需要在 <code>/etc/fstab</code> 文件中添加一行：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/data/swapfile   none    swap    sw    0   0</span><br></pre></td></tr></table></figure><p>使用以下命令将其添加：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> nano /etc/fstab</span><br></pre></td></tr></table></figure><p>在文件末尾添加上面的一行，并保存退出。</p><h4 id="验证-Swap-文件创建成功"><a href="#验证-Swap-文件创建成功" class="headerlink" title="验证 Swap 文件创建成功"></a>验证 Swap 文件创建成功</h4><p>最后，我们可以验证 Swap 文件是否创建并成功启用：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">swapon --show</span><br></pre></td></tr></table></figure><p>如果输出中出现 <code>/data/swapfile</code>，则说明创建成功。</p><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>通过本文中的步骤，你可以在没有 Swap 分区的服务器上创建一个Swap文件，从而有效缓解内存不足的情况。这种方法适用于内存充足但没有预配置Swap分区的服务器，确保系统稳定运行。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;在服务器上创建 Swap 文件是缓解内存不足的一种有效方法。本文介绍了如何在没有 Swap 分区的服务器上创建 Swap 文件，以提供额外的虚拟内存，从而保证系统的稳定性和性能。首先，我们检查系统是否已有 Swap 分区，然后通过创建和配置 Swap 文件来实现虚拟内存的扩展。最后，我们确保 Swap 文件在系统启动时自动启用。通过这些步骤，你可以在没有预配置 Swap 分区的服务器上创建 Swap 文件，确保系统在高负载情况下的稳定运行。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="https://freemankevin.uk/categories/Linux/"/>
    
    
    <category term="Linux" scheme="https://freemankevin.uk/tags/Linux/"/>
    
    <category term="Mount" scheme="https://freemankevin.uk/tags/Mount/"/>
    
    <category term="Swap" scheme="https://freemankevin.uk/tags/Swap/"/>
    
  </entry>
  
</feed>
